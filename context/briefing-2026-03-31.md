# PD2 PROJECT CONTEXT BRIEFING

**Generated:** 2026-03-31
**Branch:** `dev` @ `dc42ffc` (Build v0.0.14 - auto-commit before build)
**Build Health:** CLEAN (both client + server per S90; cannot verify from sandbox — requires MSYS2 build on Windows)

---

## Git State

- **Uncommitted changes:** YES — 18 modified files + 1 untracked (`context/sessions-79-86.md`)
  - Code changes: `CMakeLists.txt`, `net.c`, `net.h`, `netmsg.h`, `netmsg.c`, `netdistrib.c`, `pdgui_backend.cpp`, `server_main.c`, `server_stubs.c`, `playerreset.c`
  - Context changes: `README.md`, `bugs.md`, `constraints.md`, `match-startup-pipeline.md`, `infrastructure.md`, `roadmap.md`, `session-log.md`, `tasks-current.md`
  - **Net delta:** 199 insertions, 1082 deletions (significant cleanup/refactor from S90)
- **Unpushed commits:** 1 (`dc42ffc` — Build v0.0.14)
- **Stashes:** 3 (milestones WIP, release WIP, buildtool autostash — all old, non-critical)
- **Active worktrees:** 44 — ALL marked `prunable`. **No worktree has commits ahead of dev.** All are stale leftovers from previous Claude sessions.
- **Active branches:** ~55 `claude/*` branches. None contain unmerged work relative to `dev`.

### Worktree Cleanup Recommendation
All 44 worktrees are prunable. Run `git worktree prune` + delete stale `claude/*` branches to clean up. This is housekeeping only — no code is at risk.

---

## Active Workstreams

### 1. Session Catalog + Modular API (SA-series) — HIGHEST PRIORITY
- **Goal:** Replace all raw N64 index usage at interface boundaries with catalog-based identity. Per-system typed query API, network session catalog (catalog IDs ↔ wire hashes), load manifest system.
- **Task IDs:** SA-1, SA-2, SA-3, SA-4, SA-5
- **Status:** Design doc complete (S90). Implementation NOT STARTED.
- **Key files:** `port/src/assetcatalog.c`, `port/src/assetcatalog_base.c`, `port/include/assetcatalog.h`, `port/src/net/netmsg.c`, `port/src/net/net.c`, `port/include/net/netmsg.h`
- **Last known state:** S90 completed full codebase audit (~180 raw-index call sites, 20 patterns). Design doc at `context/designs/session-catalog-and-modular-api.md`.
- **Dependencies:** SA-2 required before SA-4 (wire protocol migration). SA-1 required before R-series room sync. Match Startup Pipeline (Phases B–F) consumes SA-2.

### 2. Catalog Activation (C-series) — NEARLY DONE
- **Goal:** Wire catalog override intercepts into all asset loading paths.
- **Task IDs:** C-5, C-6 (remaining); C-0/C-2-ext/C-4/C-7/C-8/C-9 done
- **Status:** C-5 (texture override) coded, wiring needs confirmation. C-6 (anim override) NEXT.
- **Key files:** `port/src/assetcatalog.c`, `src/game/tex.c`, `src/game/anim.c`
- **Last known state:** C-7 (sound override) completed S80. C-5/C-6 remain.
- **Dependencies:** None blocking, but feeds into SA-1.

### 3. Match Startup Pipeline (Phases A–F) — IMPLEMENTED, NEEDS PLAYTEST
- **Goal:** Unified 8-phase pipeline: Gather→Manifest→Check→Catalog→Transfer→Ready Gate→Load→Sync.
- **Task IDs:** Pipeline Phases A through F
- **Status:** All phases (A–F) coded and build-verified. Phase C.5 PARTIAL (SP catalog registration done, navmesh spawn + unlock gating remain).
- **Key files:** `port/src/net/netmanifest.c`, `port/include/net/netmanifest.h`, `port/src/net/netmsg.c`, `port/src/net/net.c`, `port/src/net/netdistrib.c`
- **Last known state:** S88 completed Phases D/E/F. S87 did Phase C.5. S86 did Phase C. S85 did Phase B. S84 did Phase A. All build clean.
- **Dependencies:** Needs end-to-end networked playtest to verify full pipeline.

### 4. Room Architecture (R-series) — R-1 DONE, R-2 NEXT
- **Goal:** Multi-room server with on-demand room creation, leader tracking, room-scoped matches.
- **Task IDs:** R-1 (done), R-2, R-3, R-4, R-5
- **Status:** R-1 foundation done (S52). R-2 (room lifecycle) is next — expand HUB_MAX_ROOMS=16, HUB_MAX_CLIENTS=32, add leader_client_id/room_id.
- **Key files:** `port/src/hub.c`, `port/include/net/net.h`, `port/src/net/net.c`, `port/src/net/netmsg.c`
- **Dependencies:** R-2/R-3 must complete before L-series (lobby UX). SA-1 feeds into R-series room sync.

### 5. Join Flow (J-series) — J-1/J-2/J-4/J-5 DONE
- **Goal:** Complete client-to-server join experience.
- **Task IDs:** J-3 (remaining), J-1/J-2/J-4/J-5 done
- **Status:** J-3 (SVC_ROOM_LIST protocol) is the remaining item — broadcast room state from server to clients.
- **Key files:** `port/src/net/netmsg.c`, `port/src/net/net.c`
- **Dependencies:** Depends on R-3 (room sync protocol).

### 6. Lobby / Room / Match UX (L-series) — BLOCKED ON R-2/R-3
- **Goal:** Client-facing UI for social lobby, room create/join, match setup.
- **Task IDs:** L-1 through L-6
- **Status:** Not started. Blocked on R-2 + R-3.
- **Key files:** `port/fast3d/pdgui_menu_lobby.cpp`, `port/fast3d/pdgui_menu_room.cpp`, `port/fast3d/pdgui_menu_matchsetup.cpp`

### 7. UI Scaling — CRITICAL BLOCKER for v0.1.0
- **Goal:** Make UI work at different resolutions/DPI.
- **Task IDs:** Blocker #1 in tasks-current.md
- **Status:** Not addressed yet. Required for v0.1.0.
- **Key files:** `port/fast3d/pdgui_*.cpp`

### 8. Playtest Backlog — Multiple items awaiting verification
- **Items:** T-7 (mod body/head/arena), T-8/T-9 (stage restore + texture flush), B-46 (void spawn), B-47 (exit freeze), Combat Sim save/load, B-50 (server match-end), plus many pre-S68 items.
- **Status:** All coded; need Mike's in-game playtest verification.

---

## Collision Map

### HIGH RISK: `port/src/net/netmsg.c`
- **Conflicting workstreams:** SA-4 (wire protocol migration), R-3 (room sync protocol), J-3 (SVC_ROOM_LIST), Match Startup Pipeline
- **Collision type:** Edit conflict + protocol version coupling
- **Mitigation:** **SERIALIZE.** Only one session should modify netmsg.c at a time. SA-4 is the biggest (~180 call sites). Defer R-3 and J-3 until SA-4 is done or coordinate protocol bump.

### HIGH RISK: `port/src/net/net.c`
- **Conflicting workstreams:** SA-2 (session catalog), R-2 (room lifecycle), Match Startup Pipeline (tick logic)
- **Collision type:** Edit conflict + shared state (global client/server state)
- **Mitigation:** **SERIALIZE.** Session catalog and room lifecycle both modify core net loop. Run sequentially.

### MEDIUM RISK: `port/include/net/net.h`
- **Conflicting workstreams:** R-2 (new constants/structs), SA-2 (session catalog types), protocol bumps
- **Collision type:** Struct layout + constant definitions
- **Mitigation:** If both R-2 and SA-2 need net.h changes, coordinate in same session or serialize.

### MEDIUM RISK: `port/src/hub.c`
- **Conflicting workstreams:** R-2 (room lifecycle), B-50 (match-end timer verification), SA-2 (catalog in hub tick)
- **Collision type:** Edit conflict in `hubTick()` and room management
- **Mitigation:** R-2 is the primary owner. B-50 is already coded (just needs playtest). Avoid concurrent hub.c edits.

### MEDIUM RISK: `port/src/assetcatalog.c` / `port/src/assetcatalog_base.c`
- **Conflicting workstreams:** SA-1 (modular API), C-5/C-6 (override wiring)
- **Collision type:** Logical dependency (SA-1 refactors the API that C-5/C-6 use)
- **Mitigation:** SA-1 should land first, then C-5/C-6 use the new API. Or run C-5/C-6 first (they're smaller, well-scoped) then SA-1 refactors around them.

### MEDIUM RISK: `CMakeLists.txt`
- **Conflicting workstreams:** Any task adding new source files
- **Collision type:** Edit conflict (file lists)
- **Mitigation:** Uses `file(GLOB_RECURSE)` auto-discovery — only manual edits for dependencies/flags collide. Low practical risk.

### LOW RISK: `port/fast3d/pdgui_menu_*.cpp`
- **Conflicting workstreams:** L-series (lobby/room UI), UI Scaling, B-56 (ImGui duplicate ID)
- **Collision type:** Edit conflict on same screen files
- **Mitigation:** L-series is blocked on R-2/R-3. UI Scaling is independent. B-56 is trivial (PushID/PopID). Low conflict likelihood right now.

### LOW RISK: `context/` files
- **Conflicting workstreams:** Any session updating task status
- **Collision type:** Merge conflict on status lines
- **Mitigation:** Use Edit tool (not Write) on context files. Small, targeted edits merge cleanly.

---

## Recommended Session Plan

### Safe to Run in Parallel (touch different files/systems)
1. **C-5/C-6 catalog wiring** (tex.c, anim.c, assetcatalog.c) — small, well-scoped
2. **B-56 fix** (pdgui_menu_room.cpp PushID/PopID) — trivial, isolated
3. **UI Scaling investigation** (pdgui_*.cpp) — research/design phase, no protocol files
4. **Worktree cleanup** (git housekeeping only)

### MUST Be Serialized (touch the same critical files)
1. **SA-1/SA-2** (session catalog + modular API) → then **SA-4** (wire migration) → then **R-2/R-3** (room lifecycle + protocol)
   - All touch net.c, netmsg.c, net.h
   - SA-4 alone touches ~180 call sites — massive surface area
   - R-2/R-3 add new protocol messages that SA-4 would need to know about

### Required Execution Order (dependencies)
1. SA-1 → SA-2 → SA-4 (each builds on the previous)
2. R-2 → R-3 → J-3 → L-1/L-2 (room lifecycle → room protocol → room list → lobby UI)
3. SA-1 before R-series room sync (R-series consumes modular catalog API)

### Should NOT Be Delegated Without Human Review
1. **SA-4 wire protocol migration** — 180 call sites across 20 patterns. Risk of subtle breakage. Needs careful planning session first.
2. **R-2 room lifecycle** — Architectural change to hub model. Design decisions needed (room creation policy, leader election on disconnect, etc.)
3. **Any NET_PROTOCOL_VER bump** — Must be coordinated; only one bump per merge cycle.

---

## Critical Build/Project Notes

Every code session MUST observe these rules:

1. **Use ONLY Edit tool on existing files, NEVER Write** — Write tool truncation has destroyed files before on this project.
2. **Build command:** `TEMP=/tmp` with MSYS2 shell — `C:/msys64/usr/bin/bash.exe -lc "cd /c/Users/mikeh/Perfect-Dark-2/perfect_dark-mike && TEMP=/tmp cmake --build build/client 2>&1 | tail -20"`
3. **types.h defines `#define bool s32`** — use `s32`, not `_Bool`, in game code (`src/game/`). Port code (`port/`) can use standard C++ bool.
4. **NET_PROTOCOL_VER is currently 25** — coordinate any bumps. Only one bump per merge cycle.
5. **Do not use worktrees** — changes must land in the main working copy directly.
6. **Run headless build check (BOTH client AND server) before reporting any task as complete.**
7. **EVERY code session must merge its changes into the main working copy, build-verify BOTH targets, and confirm the merge is clean before reporting done.** No handing off unmerged work.
8. **Asset references at boundaries use catalog IDs** — never raw N64 indices on wire, in save files, or public APIs. This is the #1 architectural constraint.
9. **Context files are code** — update `context/` immediately when making decisions, fixing bugs, or changing task status.

---

## Open Questions / Blockers

1. **Uncommitted S90 changes (18 files, net -883 lines):** These are significant code changes (B-51/52/53 fixes, protocol v25) that are staged in the working directory but NOT committed. A session should commit these before starting new work, or at minimum verify the diff is intentional.

2. **1 unpushed commit on dev** (`dc42ffc` Build v0.0.14): Should be pushed to origin before others pull.

3. **UI Scaling (v0.1.0 blocker):** No design or investigation has started. Mike needs to decide: ImGui DPI scaling? Manual scale factors? Resolution-independent layout? This blocks the v0.1.0 milestone.

4. **SA-4 scope is enormous (~180 call sites):** Needs a phased migration plan before anyone starts coding. S90 audit identified 20 patterns — suggest migrating one pattern at a time with build verification between each.

5. **Playtest backlog growing:** Multiple S74–S90 fixes await Mike's in-game verification. Some could be stale or masking new issues. Prioritize a playtest session.

6. **44 stale worktrees + 55 stale branches:** Housekeeping. Not blocking but adds noise to git operations.
