# Bug Tracker — One-Off Issues

> Open bugs only. For recurring architectural patterns, see [systemic-bugs.md](systemic-bugs.md).
> Back to [index](README.md)

---

## Open Bugs

Only entries that still need work or playtest verification. Fixed bugs live in
the compact reference below with commit SHAs.

| ID | Severity | Status | File | Verify |
|----|----------|--------|------|--------|
| **B-249** | HIGH | **OPEN -- kill-attribution mis-credits bot-vs-bot deaths to player 0 (2026-04-24, may also explain Issue 1's "working" run as false positive).** Mike verbatim: "during one of my earlier test matches, bot deaths were counting as kills for me, despite me never even having use of a weapon or fists." Bot-vs-bot deaths are being credited to player 0 in the kill counter, despite the player having no weapon use at all. This is a real bug separate from Issue 1, BUT it creates a possible explanation for the previously observed "working" run (`019dc2eb`): Mike inferred "weapons working" from the kill counter incrementing, but if those kills were bot-vs-bot mis-attributed, the actual FP rendering / fire / pickup chain may still have been broken in that run too. **If so, Issue 1 is one universal bug -- weapons broken in every match -- and the apparent variability was a kill-counter false positive masking it.** Investigation candidates: (a) default-attacker fallback in the damage handler -- when no real attacker chr is identifiable, the code may default `attackerplayernum = 0`. (b) match-end kill-summing -- if the kill table is keyed by victim and the attacker field is sometimes uninitialized, a memset/zero-init would credit player 0. (c) chr->player attribution path -- bot-vs-bot interactions where the attacker chr exists but its mp-player-num lookup returns 0 (the player slot) instead of -1 / a bot-specific value. Likely sites: `src/game/chr.c::chrHandleDamage` or equivalent, `src/game/mplayer/mplayer.c` kill counters, `src/game/bot.c::botInflictDamage` (if exists). Search keys: `attackerplayernum`, `kills[`, `chr->lastattacker`. | `src/game/chr.c`, `src/game/mplayer/mplayer.c` kill counter writes, `src/game/bot.c`. Specifically grep `attackerplayernum.*=.*0` and any `.kills[0]++` style increments without an explicit attacker check. | **Repro & verify:** start a 4-bot CS match, do not move or fire. After ~30 seconds when bots have killed each other, open scorecard. Player 0 kill count should be 0 (currently shows N>0 per Mike's report). After fix: scorecard accurately attributes kills to the actual attacker bot. Pairs as a regression: ensure player kills (when actually firing) still credit correctly. |
| **B-248** | MED | **OPEN -- Grid observer controller (queued post weapon-bug) 2026-04-24.** Mike: "The Dr Carroll character controller in The Grid should not have gravity applied to it." Even after B-247's spawn-pos fix, the Dr Carroll observer chr should be a 0G free-fly camera, not subject to gravity. Currently gravity drags the observer downward unless the user holds the ascend trigger -- not the intended observer experience. Fix: in `forgemode.c` / `forgeApplyFreeflyToPlayer`, ensure the player chr's gravity / falling tick is bypassed while `forgeIsFreefly()` is true (e.g. set `bondmovemode = MOVEMODE_CUTSCENE` already does this, but a separate gravity flag may also need clearing depending on the chr's physics state). Also confirm `bondonground` and `bondfalling` flags are not being read by gameplay paths during freefly. Investigation order: trace whether the existing `MOVEMODE_CUTSCENE` set in `forgeSetFreeflyMode` already disables gravity (bondmove.c:bcutsceneTick) -- if so, the issue is something else (free-look pitch driving Y velocity, or the observer's `prop->pos.y` is being modified by a per-frame gravity write somewhere outside bondmove). | `src/game/forgemode.c::forgeApplyFreeflyToPlayer`, `src/game/bondmove.c::bcutsceneTick`, possibly `src/game/playerprop.c` for any `prop->pos.y -= gravity * dt` writes. | **Verify after fix:** in Grid FREEFLY, release all controls -- the observer camera holds altitude exactly. WASD moves only horizontally (with pitch contribution per the existing 6DOF design); LT/RT raise/lower; idle = no drift. |
| **B-247** | HIGH | **OPEN -- Grid spawn position uninitialized (queued, candidate for this batch) 2026-04-24.** Mike's Grid log `019dc2f6-pdclient.log` line: `GRID: -> FREEFLY (session start (request)) pos=(0,-4294967040,0) yaw=0.0`. The Y coordinate `-4294967040` is `0xFFFFFF00` interpreted as signed -- a clear uninitialized-memory or 32-bit-signed-overflow signature, NOT a normal "spawned in the air with gravity" value. The body-swap-to-Dr-Carroll path in `forgeSetFreeflyMode` (`src/game/forgemode.c:139`) reads `s_forge.fly.pos` which was set by `forgeSnapFreeflyToPlayer` (`forgemode.c:126`) BEFORE the swap; that helper reads `p->prop->pos`. If `p->prop->pos.y` is uninitialized when entering FREEFLY (e.g. for a stage launched from the Grid menu where the player chr was spawned at uninitialized coords), the corrupt Y propagates into `s_forge.fly.pos.y` and back into the chr via `forgeApplyFreeflyToPlayer`. Mike fell through the level, hit the kill plane, "Mission Failed" triggered (B-244 / B-245 cascade). Fix: in `forgeSnapFreeflyToPlayer`, validate `p->prop->pos` -- if `pos.y < SOMETHING_REASONABLE_MIN` (e.g. -10000) treat the player position as invalid and fall back to the stage's default spawn pad center or the spawn-pool first valid pad. Also worth probing why the player chr position itself was uninitialized: stage `0x3e` (base stage from the bogus "All Missions" entry, B-243) may not have a normal spawn pipeline, and the player chr was created without a position set. | `src/game/forgemode.c::forgeSnapFreeflyToPlayer` (validate pos before copy), `src/game/forgemode.c::forgeTransitionToFreefly` (bail if validation fails). | **Verify after fix:** select a normal Grid map (NOT "All Missions"), enter session: log shows `pos=(X,Y,Z)` with reasonable coordinates. Select the bogus "All Missions" entry (until B-243 filters it out): the validation refuses to enter FREEFLY with a "PLAYER: Grid spawn pos invalid -- aborting session" warning. After B-243 lands, the bogus entry is unreachable so this is regression insurance only. |
| **B-245** | LOW | **OPEN -- Grid state-leak on Mission Failed transition (queued post weapon-bug) 2026-04-24.** Per Mike's log, after the bad-spawn cascade in Grid (B-247), he hit "Mission Failed" and pressed Restart, which took him back to Carrington Institute with the Grid UI still active and accessible. State-leak: the unexpected scene transition (Mission Failed -> CI hub) didn't fire `forgeTransitionToInactive`, so Grid session state survived the stage change. Fix: hook the failure path's scene transition (likely `mainEndStage` or wherever Mission Failed routes the player back to CI) so that when transitioning OUT of a Grid session stage, `forgeExitSession` (or equivalent) is called to tear down the session state and pop the Grid UI input contexts. This complements B-239's defensive-flag-reset pattern: any unexpected stage transition while Grid is active should fully tear down. | `src/game/forgemode.c::forgeTransitionToInactive`, `src/lib/main.c::mainChangeToStage` or the Mission Failed transition path (find via `MISSION_FAILED` or similar token). | **Verify after fix:** force a Mission Failed in Grid (use a bogus map, or modify the test to deliberately kill the player): Restart returns to CI hub WITHOUT Grid UI, and Grid sub-menu is no longer accessible from CI until re-entered through the Main Menu -> The Grid path. |
| **B-244** | LOW | **OPEN -- Grid Abort button non-functional after bad spawn (queued post weapon-bug) 2026-04-24.** Per Mike: after the bad-spawn cascade in Grid (B-247), the Mission Failed screen's Abort button did nothing. UI state machine doesn't recover from the corrupt-position state. Likely related to B-245 (the state-leak): if Grid session state is wedged after the bad spawn, the Abort button's handler can't tear it down cleanly. Fix may be downstream of B-245's cleanup path -- once `forgeTransitionToInactive` fires reliably on any unexpected stage transition, Abort should be able to route through the same teardown. | Likely `port/fast3d/pdgui_menu_endscreen.cpp` or similar Mission Failed UI handler; trace what Abort calls and verify each step succeeds when Grid state is corrupt. | **Verify after fix:** force a Mission Failed in Grid, click Abort: the screen tears down, returns to Grid main menu (or CI, depending on the design intent for "abort the failed session"). No silent-non-response. Pairs with B-245 as the same code path likely fixes both. |
| **B-243** | MED | **OPEN -- "All Missions" appears in Grid Arena picker (queued, candidate for this batch) 2026-04-24.** Mike: "in testing The Grid, there is an 'All Missions' option in Arenas. It shouldn't be there, that's not a single map." Per Priority A's data-source-cleanliness rule, meta / aggregate entries should be filtered at registration so the picker enumeration cannot expose them. Mike's Grid log `019dc2f6-pdclient.log` has `GRID.MENU: enter map='All Missions'` followed by `GRID: launching session (base stage 0x3e)` -- so the bogus entry routes to a real stagenum but is itself a meta token. The picker is built from the asset catalog (`pdgui_menu_forge.cpp` likely enumerates `ASSET_MAP` or similar). Fix: at registration time, identify the "All Missions" entry (probably has a sentinel name or a special bit) and either (i) skip its registration in the catalog, OR (ii) flag it as a non-pickable meta entry (`ext.meta = 1`) and filter the picker enumeration on `!entry->meta`. Option (i) is preferred per Priority A -- if the data shouldn't be picked, don't put it in the catalog at all. Investigate where `'All Missions'` originates (search the .pdmod / catalog manifest sources for the literal string); likely a legacy mods registration that should not have promoted to the picker after the catalog rewrite. | Search: `port/src/assetcatalog*.c` for `"All Missions"` or stage 0x3e meta-aggregate registration; `port/fast3d/pdgui_menu_forge.cpp::Arenas` enumeration. | **Verify after fix:** open The Grid -> Arenas. The list shows individual maps only; no "All Missions" entry, no other meta-aggregate entries. Selecting any map launches successfully. After B-247 lands as well, even if the meta entry slipped through, the session would refuse to launch with a pos-invalid warning. |
| **B-246** | HIGH | **DIAGNOSTIC INSTRUMENTATION ONLY 2026-04-24.** Does NOT fix the weapon-invisible / can't-fire / can't-pickup symptom Mike has reported across multiple builds (latest tested `5ee1b013`). Two earlier hypothesis trees were narrowed and discarded: (1) observer-chr / `MOVEMODE_CUTSCENE` leak from FREEFLY -- refuted by `playermgrAllocatePlayer` resetting `bondmovemode = MOVEMODE_WALK` per match plus log evidence showing no `GRID: -> FREEFLY` transition during the bug repro; (2) bot-count divergence (`bots == 0` vs `bots >= 1`) -- correlation broke on a fourth log where bots were present and the symptom still reproduced. Static read of the radial-menu introducing commit (`4fcc42ec`) found only additive changes (radial render gated on the same `pdguiActiveMenuIsOpen` predicate the legacy GBI wheel uses; no new "skip FP" flag, no fire dispatch hijack). The contradiction is sharp: log proves `BMOVE allowc1buttons=1`, `g_PlayersWithControl[0]=1`, `pausemode=0`, AND `DIAG fireVk action=20(FirePrimary) DOWN` arriving in the dispatcher; yet fire effect doesn't manifest in-world. Something downstream of `actionPressed/actionHeld(0, ACTION_FIRE_PRIMARY)` is silently dropping the trigger or the FP rig isn't reaching a renderable / fireable state. This commit lays down `LOG.WPN.DIAG:` lines at six choke points so the next playtest log fingerprints the failure point precisely: (a) `bgunEquipWeapon2` enter/exit (every weapon attach for player 0 logged with hand-state before and after); (b) `bgunTickGameplay` rising-edge entry (state of `triggeron`, `gunctrl.weaponnum`, `passivemode`, `tickmode`, `bondmovemode`, `g_PlayersWithControl[0]`, plus per-hand `wpn / inuse / visible / state` -- captured at the exact moment a Mike-pressed FirePrimary arms `triggeron`); (c) `bgunRender` entry (first 5 ticks of every match, hand visible / inuse / state / weaponnum); (d) `playerSpawn` end (chr ptr, `model00d4`, `haschrbody`, `gunctrl.weaponnum`, `switchtoweaponnum`, `passivemode`, hand R/L `wpn / inuse / visible`, `bondmovemode`, `invGetCount`); (e) `propsTestForPickup` gate (every ~120 ticks, three boolean gates and the resolved `gate_pass`). All gated on `currentplayernum == 0` to keep noise low. Channel filter: emits as `LOG_NOTE` so Mike's existing `0xFFFF` mask captures every line. Inspection plan after next playtest: if `bgunRender enter` shows `R(visible=0)` while `gunctrl_wpn=19` (Shotgun), the visibility gate is the bug -- chase the writer of `hands[HAND_RIGHT].visible`. If `fire handler enter` shows `triggeron=0` despite the `BMOVE` line showing `allowc1buttons=1` and `DIAG fireVk` arriving, `bondmove.c` consumed the action but the `triggeron` derivation broke -- chase `waitforzrelease` / `c1buttons` masking / `joybutinhibit`. If `pickup probe` shows `gate_pass=0` with `movemode_ok=0`, `bondmovemode` is leaking CUTSCENE despite the playermgr reset -- chase a setter we missed. If `playerSpawn done` shows `R(inuse=0)` and `bgunEquipWeapon2 exit` shows `now_gunctrl_wpn=19` but no transition into `inuse=1` ever logs, the chr-body / hand-attach pipeline is failing silently somewhere between `playerSpawn` end and `bgunRender` first call. **Body-correlation hypothesis WITHDRAWN 2026-04-24.** I previously claimed the working run (`019dc2eb`) had Dr Carroll selected and the broken runs had Dark Combat. That was a **misread of `pdgui_charpreview` log lines** -- those reflect the menu's preview rendering, not the player's actual selected character. Mike confirmed his Agent profile body has been Dark Combat the whole time across all test runs. The "working" run is back to genuinely unexplained. **B-249 (kill-attribution mis-credits bot-vs-bot deaths to player 0) provides an alternative explanation:** if the apparent "working" run had its kill counter inflated by mis-attributed bot-vs-bot kills, Mike would have inferred "weapons working" from a kill-counter increment that wasn't the player's. Under that interpretation, Issue 1 is a **single universal bug** -- weapons broken in every match -- and the per-match variability was a kill-counter false positive. The instrumentation in this commit still gives us a definitive answer either way: the next playtest log fingerprints the actual gate that drops fire / FP-render / pickup, regardless of whether B-249 is masking the symptom. The `playerChooseBodyAndHead` exit log is still useful as a sanity check that the resolved body / head / rig_class match what Mike intended (defends against any remaining "Agent profile didn't write through" scenario, since B-234's catalog-ID path is recent and could have edge cases). **No behaviour change in this commit; pure diagnostic plumbing.** Build-verified. | `src/game/bondgun.c` (include `system.h` + 3 sites: `bgunEquipWeapon2` enter/exit, `bgunTickGameplay` rising-edge, `bgunRender` entry); `src/game/player.c` (2 sites: `playerSpawn` end + `playerChooseBodyAndHead` normmp exit); `src/game/prop.c` (1 site: `propsTestForPickup` gate, every ~120 ticks). | **Not a fix to verify.** Mike rebuilds; reproduces the bug in CS combat sim with a non-random spawn weapon (e.g. Shotgun); attaches the resulting `Build/pdclient.log`. Diagnosis follows from the matrix above. After root cause identified and structural fix lands, this row converts to FIXED + the new row's instrumentation lines either stay (cheap, low-volume, useful future) or get removed in a cleanup commit. |
| **B-242** | MED | **OPEN -- Priority O queued 2026-04-24.** Mike: "we will need to work on our spawning system, as I have spawned inside a wall a few times. I think it would help once picking a spawn point to sweep the test capsule in a radius to ensure it isn't clipping, and if so, place it into a position where it isn't." After picking a spawn point (player or bot), perform a capsule sweep at the chosen position; if the player capsule reports clipping (overlap with static geometry), iterate a radial sweep outward (small radius increments rotating around the spawn point) testing capsule overlap at each candidate until a non-clipping position is found OR a max-radius / max-iterations bound is hit. If the bound is hit without finding a clean position, fall back to the next spawn point in the rotation rather than spawning into geometry. Apply uniformly to player and bot spawn paths -- bots clipping into walls would manifest as confused / stuck-in-wall AI behaviour. The `spawn-raycast-budget` system already validates candidates via multi-ray cast against backfaces and a distance-sum budget threshold; the capsule sweep is the next layer up (a backface cast can say "ground is here" but a tight wall corner can still produce a capsule overlap). Build on top of the existing validation, don't replace it. Queued behind the B-243 / Issue 1 weapon investigation. After that resolves, slots in alongside Priority K (input-authority discipline) and Priority L (flat menu navigation). | Future implementation site: `src/lib/capsule.c` for the sweep helper, `src/game/spawnpool.c::spawnPoolSelectTiered` for the player path, `src/game/bot.c::botSpawn` plus the orchestrator in `src/game/mplayer/mpspawn_orchestrate.c` for bot paths. | **Future verification:** spawn 16 bots on tight-corridor stages (Felicity, Skedar arenas) and confirm none manifest stuck-in-wall behaviour. Run capsule overlap check after every player respawn for one full match without false-positive false-clear (verify by deliberately re-using a clipping spawn pad and confirming the radial sweep finds a clean position within the bound). Log `SPAWN.CLIP: bot=N original=(x,z) sweep_iters=M placed=(x',z')` on each rescue, `SPAWN.CLIP: bot=N original=(x,z) sweep_failed -- falling back to next pad` on bound-exceeded fallback. |
| **B-239** | LOW | **DEFENSIVE HARDENING 2026-04-24.** Frame: this is a precautionary fix surfaced during the static-pass investigation of Mike's playtest regressions (`context/scratch/regression-investigation-2026-04-24.md`); it MAY OR MAY NOT address the reported "weapon invisible in Campaign + CS" symptom. Bug class: `forgeTransitionToInactive` calls `forgeRestorePlayerMode` only when `forgeCurrentPlayer()` returns non-NULL (`src/game/forgemode.c:369-372`); `forgeRestorePlayerMode` itself further guards the body-restore on `p->prop && p->prop->chr` (`forgemode.c:179`). When the inactive transition fires while the player chr is mid-teardown (typical on stage->TITLE transition), one or both guards skip; `s_forge.fly.has_saved_movemode` and / or `has_saved_body` stay true with stale `saved_*` values. The dangling chr is gone so the stale data never lands on a live chr in practice, but a future code path that reads the saved fields without re-validating would observe stale state. Fix: unconditionally clear both flags at the top of `forgeTransitionToInactive` regardless of player / chr state. Inactive transition is now idempotent. Integer `saved_*` fields left at last-known values (every consumer guards on the flag; clearing them was unnecessary churn). Also audited the codebase for stale ACTION_COUNT references after the 62 -> 68 bump in `29d05d49` (Issue 8b sidebar): all consumers reference the symbol; only literal hits on `62` are unrelated (chr-anim table data, F5 keyboard scancode, comment labels, sha256 buffer index). No stale hard-coded ACTION_COUNT values found. | `src/game/forgemode.c::forgeTransitionToInactive` (defensive flag reset block before the existing state assignment) | **Verify the fix is harmless:** (1) Enter Grid, exit cleanly via menu -> the existing happy path still restores chr->bodynum and chr->headnum correctly; flags are false post-transition. (2) Enter Grid, force a stage -> TITLE transition while in FREEFLY -> log shows `GRID: -> INACTIVE (stage left gameplay)`; subsequent Grid entry does not skip the body-swap save (because flags are false now, not stuck true). (3) Defensive nature means the absence of "broken thing happened" is the success condition; nothing observable should change in normal flow. **Does NOT claim to fix the regression Mike reported** -- if the weapon-invisible symptom persists in playtest after this lands, the root cause is elsewhere. |
| **B-238** | MED | **OPEN -- DESIGN APPROVED, IMPLEMENTATION QUEUED (Priority M, 2026-04-24).** Mike's architectural extension to Q18: `.pdmod` becomes the **canonical mod format for the entire system**, not only for shared / transferred mods. Mod tools output `.pdmod` (not loose folder trees), the mods folder contains `.pdmod` files, the loader interprets them at runtime through a virtual file system without on-disk extraction. Sharing surfaces (chat, Public Mods Page) use the artifact already on disk -- no repackaging step. Folder-tidiness goal: post-migration `data/mods/installed/` contains one file per mod. Mike addendum (file-system metadata): a Windows Property Handler DLL ships with the installer to expose `mod.json` headline fields (creator / version / description / tags) at the OS shell layer; right-click Properties in Explorer and column views render them without opening the archive. Loader uses the same path for fast-scan startup (large speedup for users with many mods). Defensive zip-comment mirror lets non-Windows tools (7-Zip, Linux file managers) read the same headlines. Single source of truth: `mod.json` inside the archive; OS-shell metadata is a projection. Phasing: M-1 dual-support loader + Property Handler + benchmark; M-2 mod tools migrate (Theme tool first); M-3 one-shot auto-package of legacy folder mods on first run after upgrade; M-4 (optional, contingent on Mike) retire folder loader. Trust model continuity: the manual-install gate from B-237 / Q18 applies uniformly to locally-authored, chat-shared, and Public-Mods-Page-downloaded `.pdmod` files; only `mods/installed/` is loader-trusted; `mods/shared/` is read-only-for-browsing. Total estimate ~4-5 weeks of focused work. | Design: `context/designs/pdmod-unified-mod-format.md`. Companion: `context/designs/connectivity-and-modern-main-menu.md` Section 7 (format + sharing) plus the file-system-metadata cross-reference. Touches: `port/src/modloader.c` (new), `port/src/modvfs.c` (new), `port/include/modloader.h` (new), `port/external/miniz.c` (or equiv), `port/src/modmgr.c` (refactor), `port/fast3d/pdgui_menu_mods.cpp` (new), `tools/pdmod_prophandler/` (new Win32 COM DLL `PD2ModPropHandler.dll`), `tools/themes/main.c` + `tools/skins/main.c` (M-2), `tools/_shared/modpack_save.c` (new), `port/src/modmigrate.c` (M-3, removed in M-4). Installer registers the Property Handler CLSID + extension. | **Verification matrix:** (1) Build a `.pdmod` from the Theme tool; loader enumerates it; right-click Properties in Explorer shows creator / version / description without the archive being opened (verify with Process Monitor that no zlib decompress fires for the listing). (2) Hot-disable a mounted `.pdmod` from the mod manager mid-main-menu; assets unload cleanly; re-enable and confirm assets are reachable on next stage load. (3) Run M-3 against an install with 5+ legacy folder mods; confirm each becomes a `.pdmod` with `_legacy_backup/` retained. (4) Bench the cold-read path on a representative mod (~512 KB texture); confirm <1 ms per first read; warm reads at memcpy speed. (5) Trust-invariant test: drop a `.pdmod` into `mods/shared/<friend>/` and confirm the loader does NOT mount it; only the user clicking "Install" or dragging to `mods/installed/` makes it active. (6) Non-Windows or no-installer portable build: confirm zip-comment mirror is readable by 7-Zip / Linux file manager so users without the Property Handler still see headline metadata. |
| **B-237** | MED | **FIXED-PENDING-PLAYTEST 2026-04-24 (Issue 4b music speed-lerp).** Mike approved a music-sync upgrade so peers in the same MP match playing the same track stay in lock-step. Implementation: bump NET_PROTOCOL_VER 39 -> 40; SVC_MUSIC_ADVANCE wire format gains a u32 match_clock_offset_ms after the existing track_id string. Authoritative host (NETMODE_SERVER_AUDIO) re-broadcasts the same track every MUSIC_SYNC_REBROADCAST_MS (2000 ms) with the elapsed-since-track-start offset, plus the existing track-change broadcast on playlist advance. Clients route the packet to audioMusicSyncReceive (audio.c) which on a track-change starts the new track and hard-seeks to the host offset (late-joiners pick up mid-song), and on a same-track receive caches the (recv_wallclock, host_offset_at_recv) pair for the correction tick. audioMusicSyncCorrectionTick runs once per audio frame from the end of audioEndFrame: it computes drift = expected_host_offset_now - local_position_ms, then either holds rate at 1.0 (drift inside +-30 ms dead-band), lerps rate via clamp(1.0 + drift_ms*0.001, 0.97, 1.03) for moderate drift, or hard-seeks to the host offset when drift exceeds +-5000 ms. modmusic backend rewrote the integer-cursor mix loop to use a fractional cursor with linear interpolation between adjacent stereo frames so sub-sample rate changes work cleanly; rate is reset to 1.0 on every modMusicPlay / Stop. New API: modMusicSetRate / GetRate, modMusicGetPositionMs / SetPositionMs, modMusicGetDurationMs. Diagnostic: throttled "MUSIC.SYNC: drift=N ms rate=X.XX" log every 2 s when actively correcting, plus "MUSIC.SYNC: hard-seek..." when the band is exceeded. | `port/include/net/net.h` (NET_PROTOCOL_VER 40 + comment), `port/src/net/netmsg.c` (SVC_MUSIC_ADVANCE write/read + broadcast), `port/include/net/netmsg.h` (signature change), `port/src/audio.c` (host re-broadcast tick + audioMusicSyncReceive + audioMusicSyncCorrectionTick), `port/src/modmusic.c` (fractional-cursor mix loop + rate/position API), `port/include/modmusic.h` (new API), `port/include/audio.h` (sync entry points), `port/src/server_stubs.c` (no-op stubs for pd-server) | **Playtest:** (1) Two listen-host clients in the same MP match: start a track on the host, observe both hear the track; check logs for "MUSIC.SYNC: drift=... rate=..." lines stabilising near rate=1.0. (2) Start a fresh client mid-track: track starts at the host's current offset, not from the beginning; check log for "MUSIC.SYNC: track-change -> '...' offset=N ms". (3) Force drift on the client (e.g. simulate a hitch via Audio.QueueLimit, or temporarily rate-disable the host re-broadcast): drift should exceed 5000 ms and the client should hard-seek -- log "MUSIC.SYNC: hard-seek (drift=... exceeded)". (4) Pitch shift at +-3% rate: audible if listening for it but transient -- the lerp deadband of +-30 ms and gain K=0.001 means rates only push toward the boundaries during sustained drift. |
| **B-236** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-24 (AUDIT-24-H2 + H3 closure).** 4-24 super audit traced that `JBTN_BACK -> ACTION_FORGE_TOGGLE`, `JOFS_RTRIG -> ACTION_FORGE_ASCEND`, and `JOFS_LTRIG -> ACTION_FORGE_DESCEND` were dual-bound in `g_ImcGameplay` alongside SCORECARD / FIRE_PRIMARY / FIRE_SECONDARY. `fireVk` (port/src/actionmap.cpp:469) is single-winner-per-IMC: lowest trigger slot, then lowest action enum on tie. The Forge actions all had higher enum ids and lost every press — gamepad Back never toggled Forge mode, gamepad triggers never raised/lowered the freefly camera. Same root caused LSHIFT/LCTRL Forge BOOST/PRECISION to lose to SPRINT/CROUCH on keyboard too. Fix: split into two new IMCs. `g_ImcForgeSession` (priority 6, active for whole forge session) hosts `JBTN_BACK -> FORGE_TOGGLE`; the existing `g_ImcForge` (priority 7, FREEFLY only) gains the LT/RT triggers and LSHIFT/LCTRL modifier binds. Gameplay IMC retains keyboard F7/E/Q (each is the sole binding for its key, no collision). With this split: outside session = original gameplay behaviour; in NORMAL playtest = Back enters FREEFLY, every other gameplay action keeps working; in FREEFLY = forge IMC shadows triggers/modifiers/D-pad/LB/RB/X for camera + sidebar control. Sticks are unaffected (axis polling bypasses IMC priority entirely). | `port/src/actionmap.cpp` (new `g_ImcForgeSession` + `setupForgeSessionDefaults`; bindings moved out of `setupGameplayDefaults` into `setupForgeDefaults` / `setupForgeSessionDefaults`), `port/include/actionmap.h` (extern decl), `src/game/forgemode.c` (`imcActivate(&g_ImcForgeSession)` on session entry, `imcDeactivate` on inactive transition) | **Playtest:** (1) Outside any forge session: Back shows scorecard, RT fires primary, LT fires secondary, LSHIFT sprints, LCTRL crouches — unchanged. (2) Enter Grid; you start in FREEFLY: Back returns to NORMAL playtest. RT raises camera, LT lowers it. LSHIFT boosts speed, LCTRL slows. X toggles sidebar. LB/RB cycle tabs. Sticks fly normally. (3) In NORMAL playtest within a session: Back re-enters FREEFLY. RT/LT fire weapons (gameplay). LSHIFT/LCTRL = sprint/crouch. X = USE / interact. LB/RB = WEAPON_PREV/NEXT. (4) Pause menu in FREEFLY: pause IMC priority 11 wins; Back closes pause. |
| **B-235** | MED | **FIXED + FOLLOW-UP PENDING-PLAYTEST 2026-04-23.** Initial fix (commit f4d8dc3a): Maian bots in Combat Simulator rendered with President's head instead of Elvis/Maian head. Root cause: `pdgui_menu_room.cpp` at two sites called `catalogMpHeadId(b)` where `b` is the **body** mp_index - not a head mp_index. `MPBODY_ELVIS1 = 12` and `MPHEAD_PRESIDENT = 12` share the same integer, so Maian body (mp_index 12) silently selected President head (12). Bug introduced in `3a055323` (Phase 8 O(n) conversion cleanup). Initial fix used `catalogGetBodyDefaultMpHeadIdx` / `catalogGetBodyDefaultHead` - returned deterministic paired head, so every selected bot got the SAME head (still wrong per Mike's intended workflow). **Follow-up (systematic pass, 2026-04-23):** Mike's workflow is "multi-select + Set Character" where heads should randomize per bot across valid heads for body. For `HEAD_RANDOM_GENDER` bodies (4 Bond actors, many SP NPCs) `catalogGetBodyDefaultMpHeadIdx` returned -1 so the UI either left head_id stale or hardcoded dark_combat. Now all three Set-Character sites call `mpDefaultHeadForBody(mpbodynum)` per-bot: that helper already handles both deterministic-paired-head bodies and HEAD_RANDOM_GENDER bodies (gender-pool random pick on each call). In matchsetup.c added new helper `pickHeadIdForBody(body_id)` wrapping the same logic for `matchConfigAddBot` and `pickRandomBodyHead`. Data-model caveat: bodies with ONE paired head (Maian, Skedar) still give uniform heads - only random-gender bodies get variety. UI follow-up Mike flagged: context menu exposes Set Character but not Set Head; consider adding "Set Head (Random)" / "Set Head (Specific)" submenu (noted as future scope). Build-verified. | `port/fast3d/pdgui_menu_room.cpp` (two sites: Set Character menu loop uses mpDefaultHeadForBody per-bot, individual bot edit modal same), `port/src/net/matchsetup.c` (`pickHeadIdForBody` helper + `matchConfigAddBot` + `pickRandomBodyHead`) | **Playtest:** (1) Add 10 bots, multi-select, Set Character = Connery: each bot gets a random male head (not all dark_combat). (2) Multi-select, Set Character = Maian: all get Maian head (data limit). (3) Add bot with body_id set, head_id NULL: gets appropriate random head for gender, not dark_combat. (4) Confirm deterministic bodies (Joanna Dark, Elvis) still pair to their specific heads. |
| **B-233** | MED | **FIXED-PENDING-PLAYTEST 2026-04-23.** Mike: "I died on the CI Main Menu mission (not normally able to, but I jumped out of bounds). It didn't crash, but it didn't respawn me at the initial start point, it should." S440 logged the intended behaviour (solo CI death should fade + respawn at scenario spawn via `dostartnewlife` / `playerStartNewLife` without `mainEndStage`), but the actual guard at `player.c:5590` (`if (!g_Vars.mplayerisrunning && g_Vars.stagenum == STAGE_CITRAINING) { canrestart = true; }`) lived INSIDE the else-branch of `if (g_Vars.mplayerisrunning)`, so `!mplayerisrunning` was unreachable by definition - dead code. Result: solo CI death played the death animation, fade completed, then the code dropped through with `canrestart=false`, `dostartnewlife` never flipped, player stuck dead forever. Fix: add an `else` branch to `if (g_Vars.mplayerisrunning)` at the `playerIsFadeComplete()` site; inside that else, when `stagenum == STAGE_CITRAINING`, hide the chr (`CHRCFLAG_HIDDEN`) and force `dostartnewlife = true`. Removed the unreachable check in the MP-else branch with a breadcrumb comment pointing to the new site. | `src/game/player.c` (fade-complete respawn branch around line 5460) | CI free-roam: fly / noclip / jump OOB so the player's chr takes a lethal hit (kill plane, bottomless-pit Y threshold). Expected after the existing death + fade animation: player respawns at the CI hub's scenario spawn point, hands and HUD return, movement works. No crash, no endscreen, no stage reload. |
| **B-232** | LOW | **FIXED v2 PENDING-PLAYTEST 2026-04-23.** Round 1: observed screen-wide "darken" when "Hold X" prompt appears, misread as the prompt's backdrop. Actual source is the CRT scanline overlay `pdguiThemeDrawScanlineFg` (semi-opaque horizontal lines, alpha up to 140, painted fullscreen). Round 1 fix gated scanlines on `pdguiIsActive()` so they only drew when a menu was up. Mike's correction (2026-04-23): scanlines SHOULD always be visible when enabled - gameplay and menus alike. The apparent "only when prompt visible" was just because `pdguiRender` only ran when an overlay reason was true. Round 2 fix: reverted the `pdguiIsActive()` gate; added `pdguiThemeGetScanlineEnabled()` as a reason in `pdguiAnyStandardOverlayReason` so `pdguiRender` runs every frame the toggle is on and scanlines paint over the 3D scene continuously. Also added **user-tunable vertical scale** per Mike's request: new `Video.ScanlineVerticalScale` in pd.ini (range 0.25..4.0, default 1.0), Settings -> Video "CRT Line Spacing" slider, `pdguiThemeSet/GetScanlineVerticalScale` API, applied as a multiplier on the stride in `pdguiResolveScanlineMetrics`. | `port/fast3d/pdgui_backend.cpp` (reason + draw call), `port/fast3d/pdgui_theme.cpp` (state + config + setter/getter + stride math), `port/include/pdgui_theme.h` (API), `port/fast3d/pdgui_menu_mainmenu.cpp` (Settings slider) | **Playtest:** (1) Enable CRT: scanlines visible in gameplay (no menu up) and in menus. (2) Disable CRT: no scanlines anywhere. (3) Adjust CRT Line Spacing slider: lines tighten at 0.5x, widen at 2x, 1x = default. Value persists across restart via pd.ini. |
| **B-234** | MED | **FIXED v2 PENDING-PLAYTEST 2026-04-23 (deep-dive sweep).** Extended beyond the initial two-layer fix after Mike's ask to "ensure nothing uses the legacy integer index, everywhere". Explore agent swept all reads of `mpheadnum`/`mpbodynum`/`chr->headnum`/`chr->bodynum` across src/ and port/. Additional sites patched this pass:<br/>- **`src/game/player.c::playerChooseBodyAndHead`** (two sites - normmp branch and net co-op branch, lines ~1831-1870): read `mpheadnum`/`mpbodynum` as primary and resolved to Joanna for any alien. Replaced both with a local `B234_RESOLVE_CHARCONFIG` macro that prefers `head_id`/`body_id` via `assetCatalogResolve` -> `ext.head.headnum`/`ext.body.bodynum`, falls back to `mpGetHeadId`/`mpGetBodyId` only when strings are empty.<br/>- **Deprecated `headnum`/`bodynum` fields removed from `struct lobbyplayer` + `struct lobbyplayer_view`** (both `pdgui_lobby.cpp` and `server_gui.cpp` copies). Writes in `port/src/net/netlobby.c:84-85` were derived from catalog ID via `runtime_index` but never consumed by the lobby UI (portrait path reads `head_id`/`body_id` directly). Removed the junk derivation and the fields. Updated byte-layout offsets in `port/fast3d/pdgui_bridge.c::lobbyGetPlayerInfo` and `port/src/server_bridge.c::lobbyGetPlayerInfo` to match the shrunk view (name shifted 6->4, isLocal 40->36, state 44->40, clientId 48->44).<br/>- **Deferred (separate bug class):** `port/fast3d/pdgui_menu_agentcreate.cpp::autoSelectHead` uses `catalogGetBodyDefaultMpHeadIdx` but the agent-create head picker only lists MP-registered heads. Aliens aren't selectable via this UI at all - that's B-227 territory (alien heads missing from MP list). Not a leak in isolation; queued behind B-227.<br/>- **Verified OK:** `src/game/menu.c::menuRenderModel` at line 2030-2040 has a `MENUMODELPARAMS_GET_MP_HEADNUM/BODYNUM` legacy fallback, but the B-234 code path flows through `MENUMODELPARAMS_SET_FILENUM` (via `catalogGetHeadFilenumByIndex`/`catalogGetBodyFilenumByIndex`) so our catalog-ID path skips that fallback.<br/>- **Verified OK:** `forge_runtime.c:157,170` guards `mp_index >= 0` before writing `mpheadnum`/`mpbodynum`, and writes `body_id`/`head_id` strings first (parallel catalog-ID path). Integer-skip behaviour is benign because the render path (post B-234 layer 2 in botmgr.c) reads the string. | `src/game/player.c` (playerChooseBodyAndHead macro), `port/include/net/netlobby.h` (struct shrink), `port/src/net/netlobby.c` (junk-derivation removal), `port/fast3d/pdgui_lobby.cpp` + `port/fast3d/server_gui.cpp` (view struct shrink), `port/fast3d/pdgui_bridge.c` + `port/src/server_bridge.c` (byte offset resync) | **Playtest round 2:** (1) Coop/anti net: pick Maian as character, start match - Joanna-as-coop regress to real Maian render. (2) Normal MP: same for local player render. (3) Lobby UI: player list shows names / team / ready correctly with the shrunk view struct (visual regression check, no offset drift). (4) All prior B-234 round-1 playtests still pass. |
| **B-231** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-23.** Mike: interact prompt pops up briefly as the Main Menu is opening (CI free-roam near the computer, press Start). Root cause: `playerPause()` flips `pausemode` UNPAUSED -> PAUSING immediately, but the ImGui main-menu ctx is not pushed until the menu's first IsWindowAppearing frame (1-2 frames later). During that gap, `pdguiIsActive()` is still false and the CI-specific gates (`var80087260`, `tickmode == TICKMODE_CUTSCENE`, `lvframenum <= 30`) don't fire, so the prompt renders for a frame or two before the menu takes over. Fix: extend `pdguiCiIntroBlocksInteractPrompt` in `pdgui_bridge.c` to also return 1 when `g_Vars.currentplayer->pausemode != PAUSEMODE_UNPAUSED`. That covers the pause transition cleanly and also catches any other pause-initiating path (eyespy, AI script). AND the `interactPrompt` activation boolean in `pdgui_backend.cpp` NewFrame + Render gates with `!pdguiCiIntroBlocksInteractPrompt()` so the ImGui overlay doesn't spin up from the prompt during the transition either. | `port/fast3d/pdgui_bridge.c::pdguiCiIntroBlocksInteractPrompt`, `port/fast3d/pdgui_backend.cpp` (both gate sites) | **Playtest:** CI free-roam, stand next to the computer so "Press F" is shown. Press Start. Menu opens instantly with zero prompt flash visible. Same for Start -> close -> Start rapid cycle. |
| **B-230** | MED | **FIXED-PENDING-PLAYTEST 2026-04-23.** Mike: on Main Menu, controller B needs two presses to close (keyboard Escape and mouse X work first press). Root cause is the same shape as the S306 X-button fix (the close handler needed two clicks): ImGui's internal nav consumes the first `ImGuiKey_Escape` edge that `pdguiDriveImGuiNav` injects from `actionPressed(ACTION_CANCEL_USE)`, so the renderer's `IsKeyPressed(ImGuiKey_Escape, false)` returns false on the first press. S306 added the `pdguiConsumeTitleClose` direct channel for the X-button case; this fix adds the parallel `actionPressed(0, ACTION_CANCEL_USE)` channel for the B-button case. `actionPressed` is hardware-level and cannot be consumed by ImGui's nav. The `closeGracePending` (150ms B-131 grace) guard is kept for the `IsKeyPressed` keyboard-Escape path only (that one still needs protection against queued opening-press edges); the new `actionCancelEdge` and existing `titleClose` paths are gated only by `!IsWindowAppearing()`. | `port/fast3d/pdgui_menu_mainmenu.cpp::renderMainMenu` close block around line 4062 | **Playtest:** Main Menu open, controller B: closes on first press. Keyboard Escape: closes on first press (unchanged). Mouse X: closes on first click (unchanged). Rapid open-close-open-close cycle: no lost or double-fired close. Settings / Modding / Online Play sub-views: B returns to the top-level main menu on first press. |
| **B-229** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-23.** Mike: "I set the spawn weapon to remote mine, in the match start it says I have Falcon (Silenced), though it isn't equipped either." Root cause: the B-181 fallback block in `setup.c` ("too few map pickups -> force spawn-with-weapon") was unconditionally overriding `g_MatchConfig.spawn_weapon_id` with the first valid weapon in the configured set, even when the user had explicitly selected a specific spawn weapon. User picked `base:remotemine` (spawnWeaponNum=34); with Chicago's markers=0 the fallback fired and replaced it with MPWEAPON_FALCON2 (which in the user's catalog resolved to Falcon Silencer). Fix: gate the override on `spawnWeaponNum == 0xFF` (random) OR `== 0` (unset). Only fire the "fill from set" logic when the user has no explicit preference. When the user has explicitly picked a weapon, MPOPTION_SPAWNWITHWEAPON is still enabled (so pickup-less arenas still arm players) but the user's choice is preserved. Log the preservation decision with `SETUP: user-picked spawn weapon '%s' num=%d preserved`. Pairs with B-219 v3 setup.c preload so any chosen spawn weapon has its FP model loaded regardless of map markers. | `src/game/setup.c` B-181 fallback block (around line 2483) | **Playtest:** (1) Chicago CS, spawn weapon = remote mine, start match: player spawns with remote mine visible, equipped, fires correctly. Log shows `SETUP: user-picked spawn weapon 'base:remotemine' num=34 preserved`. (2) Chicago CS, spawn weapon = random: fallback fires, first valid set weapon used, log shows `SETUP: random spawn weapon resolved from set: id='base:...' num=N`. (3) Arena WITH markers (Temple, Skedar): spawn-with-weapon not forced, map pickups still place normally. |
| **B-228a** | MED | **FIXED-PENDING-PLAYTEST 2026-04-24 (Option E -- transport-prop visibility).** Mike approved Option E from the audit's correction. After the main props loop in `setupCreateProps` (src/game/setup.c) runs against the MP setup blob, an additional pass loads the SP setup blob via `assetLoadToNew(stage.setup_handle, ...)` for the SP-in-MP class stages (CITRAINING, CHICAGO, VILLA, INFILTRATION, G5BUILDING, PELAGIC). The pass walks the SP props, accepts ONLY `OBJTYPE_LIFT` and `OBJTYPE_ESCASTEP`, and runs the same creation logic the main MP switch uses (lift accel/maxspeed unpack, modeldef preload, geocount, setupCreateObject, liftUpdateTiles, auto-register against `g_Lifts[]` via the lift's pad's liftnum; escastep matrix setup). `g_StageSetup.props` is temporarily repointed at the SP blob during this pass so `setupGetCmdByIndex` resolves SP-internal door cross-references correctly, then restored. The SP blob lives in `MEMPOOL_STAGE` and is reclaimed at stage unload, keeping `lift->doors[i]` pointers (which reference SP-blob `doorobj` structs) valid for the stage lifetime. We do NOT load the SP doors as live props -- the lift moves and players ride; door coupling reads coords from the doorobj struct fields (sufficient for tile extents) without animated open/close. Diagnostic: `SETUP.LIFT: SP-in-MP stagenum=0x%02x lifts=N escasteps=M`. No protocol change, no `pd-server` impact (server_stubs.c covers the load path; setup.c is client-only). | `src/game/setup.c::setupCreateProps` (new SP-overlay block after the main props loop, around line 2503+) | **Playtest matrix:** Per the readiness audit (`context/audits/sp-stage-mp-readiness-2026-04-24.md`), CI Training is the canonical target -- previously had no working lifts in MP. After Option E lands, host CI as a CS arena and confirm the lifts at the firing range / training rooms move when stepped on; log shows `SETUP.LIFT: SP-in-MP stagenum=0x59 lifts=N escasteps=0`. Repeat for Chicago / G5Building (which also have SP lifts on stair-traversal). Airbase is the fattest target (5 lifts + 16 doors + 40 escasteps) but lives behind a different stagenum class -- queued for a future audit if needed. Verify no regression on stages NOT in the SP-in-MP class (Combat Sim arenas like Skedar / Felicity / Temple should behave identically -- no SP overlay because their stagenum doesn't match). |
| **B-228b** | MED | **OPEN -- residual crash class.** AUDIT-24-H1 (`context/audits/2026-04-24-full.md`) revised the prior B-228 diagnosis: the actual crash root cause was never in the prop loader. Suspect paths: (a) AI lift-handle resolution returning NULL when an AI script expects a registered lift (mitigated for the SP-in-MP class by Option E's auto-register against g_Lifts[]); (b) pathfinding waypoint resolution through a missing escastep portal; (c) other model / collision lookups assuming a SP-authored prop exists when the stage is hosted as MP. B-228a (Option E) addresses transport-prop VISIBILITY; if the residual crash class is real, it surfaces during playtest of the now-loaded lifts. | Suspect: `src/game/lift.c::liftFindByPad`, pathfinding waypoint code, AI script handle resolution. | **Playtest gating:** if B-228a's transport overlay loads cleanly and the user can ride lifts without crash, B-228b is closed by absence-of-evidence -- mark FIXED-PENDING-LONG-PLAYTEST. If any crash recurs, capture the stack trace and re-open with the new diagnostic site. The auditor's recommendation: "Until traced, B-228 remains open" -- this is the b half of that recommendation. |
| **B-228** | MED | **SPLIT 2026-04-24 -- see B-228a (transport-prop visibility, Option E) and B-228b (residual crash class, awaiting playtest evidence).** Original framing was a no-elevators report on SP-in-MP stages; AUDIT-24-H1 (4-24 super audit) identified that the crash and the no-elevators are two different bugs sharing the symptom. Tracking each half independently so closure conditions are clear. The historic P5 mptransport_diffflag relax (commit `216fd27f`) is structurally retained as defence-in-depth; on the current codebase it is a no-op because base-game MP setup blobs ship without LIFT/ESCASTEP macros (audited in `context/audits/sp-stage-mp-readiness-2026-04-24.md`). The actual transport-prop visibility now comes from B-228a's SP-overlay path. | See B-228a, B-228b above. | See B-228a, B-228b above. |
| **B-228-historic** | MED | **REOPENED 2026-04-24 (P8 diagnosis correction).** P5 (commit `216fd27f`) shipped a `mptransport_diffflag` relax that turns off the `OBJFLAG2_EXCLUDE_2P/3P/4P` + difficulty filter for `OBJTYPE_LIFT` / `OBJTYPE_ESCASTEP` on SP-in-MP class stages. P8 static audit (`context/audits/sp-stage-mp-readiness-2026-04-24.md`) counted `lift(` / `escastep(` macros in every SP and MP setup C file and found the MP setups (`mp_setupdish.c`, `mp_setuppete.c`, `mp_setupeld.c`, `mp_setuplue.c`, `mp_setupdepo.c`, `mp_setupdam.c`) are EMPTY of those macros. SP setups contain the lifts (`setupdish.c` = 2 lifts + 8 doors, `setuplue.c` = 4 lifts + 8 doors, `setupcave.c` = 5 lifts + 16 doors + 40 escasteps, etc.). P5 is therefore a NOOP on the current codebase -- the filter rejects nothing because nothing is loaded to filter. P5 is still structurally correct as insurance (a future mod adding a lift to an MP setup with exclude bits would benefit) and the mptransport_diffflag path is kept. | Real fix sketch (Option E): in `setupLoadStage`, after loading the MP setup blob, for SP-in-MP class stages additionally load the SP setup via `assetLoadToNew(stage.setup_handle, ...)`, iterate its props, and run ONLY `OBJTYPE_LIFT` + `OBJTYPE_ESCASTEP` cases through the existing switch. Pad references inside lifts are stage-bound (same `padsfileid` for SP and MP), so they resolve cleanly. Costs: one extra setup blob in memory for those stages; no wire protocol change; no `pd-server` impact (server_stubs.c covers the load path). | **Playtest:** see the SP-setup transport-prop inventory in the readiness matrix for which stages the Option E fix would repopulate (Airbase is the fattest target at 5+16+40; Chicago / Villa / G5Building / Pelagic / CrashSite have zero SP lifts so Option E is a noop there too -- Chicago's fire-escape stairs are bg_pete tiles, not objects). |
| **B-227** | MED | **FIXED-PENDING-PLAYTEST 2026-04-23 (systematic pass, same commit as B-226).** Diagnostic was partly right, partly wrong: Dr. Caroll and Skedar WERE in the iterated body list (catalog registers 63 bodies at indices 0-62, UI iterates via `mpGetNumBodies() = modmgrGetTotalBodies()`), but they were labeled with junk strings from mis-pointing langids (`L_OPTIONS_355` "Need Space For Head" for DRCAROLL, `L_OPTIONS_356` "Choose a head to load over:" for SKEDAR). Mike could not find them BY NAME, so they appeared "missing". Resolved by the B-226 catalog `display_name` override: `s_BaseBodies` desc "Skedar" / "Dr. Caroll" now authoritative, `mpGetBodyName` reads it. No separate code change needed here. Structural note: "missing from list" and "wrongly named" can be the same bug when the list renderer shows all rows but one row's label makes it unrecognizable. Single-bug-class fix closed both. | See B-226 files | **Verify:** Character Select and "Set Character" context menu both show "Skedar" and "Dr. Caroll" entries by name. Selecting Skedar loads the Skedar body+head in the preview. |
| **B-226** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-23 (systematic pass).** Root cause: `g_MpBodies[]` in `mplayer.c` has wrong/shared langbank IDs for 6 bodies. `BODY_CONNERY/MOORE/DALTON/DJBOND` all share `L_OPTIONS_070` ("Dinner Jacket"); `BODY_SKEDAR` uses `L_OPTIONS_356` ("Choose a head to load over:"); `BODY_DRCAROLL` uses `L_OPTIONS_355` ("Need Space For Head"). `mpGetBodyName` returned these junk strings directly. Fix: add `display_name[64]` field to `asset_entry.ext.body`, populate from `s_BaseBodies[i].desc` for indices 57-62 via new `catalogSetBodyDisplayName` setter + `catalogGetBodyDisplayName(mpbodynum)` accessor. `mpGetBodyName` now prefers the catalog display_name when non-empty. Structural note: authoring contract was split between the decompiled `g_MpBodies[].name` (langid) and a comment-only `s_BaseBodies[].desc` string that was never read at runtime; this fix makes `desc` load-bearing and keeps i18n for the other 57 bodies via langbank fallback. Also resolves B-227 (Dr. Caroll / Skedar present in list but under junk labels). | `port/include/assetcatalog.h` (body struct + accessor proto), `port/src/assetcatalog.c` (setter), `port/src/assetcatalog_api.c` (accessor), `port/src/assetcatalog_base.c` (6-body override + Bond (Connery/Moore/Dalton/Brosnan) disambiguation), `src/game/mplayer/mplayer.c` (mpGetBodyName uses override) | **Verify:** every row in bot Character Select shows the name that matches the preview body/head. No "Dinner Jacket" duplicates; Skedar/Dr. Caroll named correctly. |
| **B-225** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-23 (systematic pass).** Root cause: `mpArenaIndexIsUsable` in `setup.c` treated "has a non-empty display name" as equivalent to "stage data is available", which is a false proxy - stale AllInOne-lineage names (L_MPMENU_319 "Kakariko Village", L_MPMENU_321 "Dark Noon Mod Valley") still pass the langbank check even though `FILE_BG_SEVX_*` / `FILE_BG_MP7_*` data did not ship with the PD2 base build. Fix: add `stagenumIsPlayableInMp(stagenum)` blacklist in `setup.c` and mirror the shell registrations as NULL in `s_ArenaNames[55]` / `[56]` in `port/src/assetcatalog_base.c` (extends the existing Paradox/STAGE_EXTRA25 NULL precedent). Structural note: the authoring contract is split across three tables (`g_MpArenas`, `s_ArenaNames`, langbank). Any divergence leaks orphan entries into the UI. Future consolidation: data-driven probe at `mpInit` (walk `g_MpArenas`, resolve each stage's required files via `catalogResolveFile`, cache a per-arena `.available` bit) so the three tables cannot drift. | `src/game/mplayer/setup.c` (new `stagenumIsPlayableInMp` + gate in `mpArenaIndexIsUsable`), `port/src/assetcatalog_base.c` (s_ArenaNames NULL for 55/56) | **Verify after fix:** Combat Sim arena select Bonus tab lists only arenas that actually load when chosen. Starting any listed Bonus arena succeeds (no "stage data missing" ERROR, no black screen). |
| **B-224** | MED | **FIXED v2 PENDING-PLAYTEST 2026-04-23.** Two-part playtest regression from the 4-21 stability batch. (a) Interact prompt label was a reason to run `pdguiNewFrame` / `pdguiRender` even when a non-gameplay ctx was on top (main menu, pause, hub), letting menu `pdguiPopupDarkenBehind` calls compound with the prompt frame -> unexpected dim any time a "Hold X" prompt appeared. (b) Same root caused the prompt to leak onto the CI title screen / main menu after spawn. **v1 fix** ANDed `interactPrompt` with `!pdguiIsActive()` in `pdgui_backend.cpp` at both gate sites (closes the menu-compound path). **v2 fix 2026-04-23** extended `pdguiCiIntroBlocksInteractPrompt` in `pdgui_bridge.c` to also block when `g_Vars.tickmode == TICKMODE_CUTSCENE` OR `g_Vars.lvframenum <= 30`. The 4-23 playtest log showed the prompt still leaking during the first-boot CI camera fly-in: the original gate only checked `var80087260 > 0`, which is set exclusively on the return-from-MP-endscreen path (`menutick.c:703`), not during the initial boot fly-in. Mirrors the inverse condition `menutick.c:315` already uses to gate "CI is ready for interaction". Orthogonal to B-222 and B-223 dim classes. | `port/fast3d/pdgui_backend.cpp` (NewFrame + Render gates), `port/fast3d/pdgui_bridge.c::pdguiCiIntroBlocksInteractPrompt` | **Playtest:** (1) Fresh boot into CI: camera fly-in plays with zero interact pill and zero darken. (2) CI main menu up at rest: no pill near the computer, no darken. (3) Actual gameplay (CI match or CS) walking to a door / weapon: prompt shows, world stays at normal brightness. (4) Return-from-MP-endscreen re-entry to CI: legacy `var80087260` path still blocks. (5) Pause mid-match near a door: prompt disappears while pause scrim is up, reappears after unpause. |
| **B-222** | MED | **FIXED-PENDING-PLAYTEST (modal + overlay class) 2026-04-21.** Covers: killfeed (`pdguiNewFrame`/`pdguiRender` when norm MP + pre-game-over; `pdguiActiveMenuIsOpen()` not `pdguiIsActive()`; **lower-left**); solo pause scrim gate in `pdgui_menu_pausemenu.cpp` (PAUSED/GAMEOVER or networked); **menupool** per-frame acquire after each successful `ImGui::Begin` (S435/S436 menus including room); **S440** — `pdguiPopupDarkenBehind` → single max-alpha full-viewport draw per frame (`pdguiPopupDarkenBeginFrame` / `Flush`). **Out of scope for B-222 close:** other full-frame `AddRectFilled` tints (countdown, endscreen, radial) that never went through `pdguiPopupDarkenBehind` — tracked as **B-223** so this row can close once the scoped playtest below passes. | `pdgui_backend.cpp`, `pdgui_layout.{h,cpp}`, `pdgui_menu_mpingame.cpp`, `pdgui_interact_prompt.cpp`, `pdgui_menu_pausemenu.cpp`, `pdgui_menu_*.cpp` (S435/S436 list) | **Playtest:** CS killfeed + pause dim; solo pause dim when paused; after CI/stage transitions no interact/killfeed bleed over ImGui; room/MP menus take input; **CI** main + nested settings — modal scrim not extra-dark vs single modal. **Listen host:** same menu stack + lobby without HUD bleed. |
| **B-223** | LOW | **OPEN / DEFERRED (dim architecture).** Full-viewport alpha drawn **outside** `pdguiPopupDarkenBehind` (e.g. `pdgui_menu_pausemenu.cpp` solo scrim path, `pdgui_countdown.cpp`, `pdgui_menu_endscreen.cpp`, radial / wheel tints) — may still **compound** with the coalesced modal scrim if two systems dim the same frame. No code change required until repro after **B-222** playtest; then either fold into a second accumulator (reason tags) or document intentional layering. | `port/fast3d/pdgui_menu_pausemenu.cpp`, `pdgui_countdown.cpp`, `pdgui_menu_endscreen.cpp`, `pdgui_hud.cpp`, grep `AddRectFilled` + `GetMainViewport` | After B-222 signed off: try **modal + pause + countdown** transitions; if double-dark remains, implement or close with UX sign-off. |
| **B-221** | MED | **FIXED-PENDING-PLAYTEST (implementation complete) 2026-04-21.** Former “partial” basket — all six items shipped in code; tracker used **PARTIAL** only because verify was split across sessions. **(1)** PC hoverbike single-tap (`CONTROLMODE_PC`). **(2)** Hold ring grace + pin + ImGui lerp. **(3)** Tap vs hold: defer `JO_ACTION_ACTIVATE` until hold consumed or USE release; hoverbike tap path unchanged. **(4)** Visual mapper zones: `SetCursorScreenPos(padOrigin + rel)`. **(5)** `fireVk` duplicate VK within one IMC: lowest trigger slot then lower `InputAction` id. **(6)** Scorecard: `ACTION_SCORECARD` excluded from `actionIsGameplayOnly()` for `actionHeld`. **Pool:** social lobby + team setup per-frame `menupoolAcquire*`. **(7) 2026-04-23 dismount parity:** Playtest confirmed mount was single-tap on PC but EXIT still needed double-tap because `bbikeHandleActivate` in `bondbike.c` kept the `TICKS(25)` double-tap window. Added the same `CONTROLMODE_PC` bypass that `currentPlayerTryMountHoverbike` uses, so single-tap dismount works on PC while the controller double-tap window stays intact. | `propobj.c`, `bondmove.c`, `bondbike.c`, `actionmap.cpp`, `pdgui_interact_prompt.cpp`, `pdgui_menu_mainmenu.cpp`, `pdgui_menu_lobby.cpp`, `pdgui_menu_teamsetup.cpp`, `menupool.{h,c}` | **Playtest:** mapper drag/drop + clear; duplicate binds order; **listen host** lobby + auto-team / team setup - menu ctx owns input; **CI** door dialog + use/hold prompts; **MP** scorecard **Back** hold; hoverbike tap mount AND tap dismount on PC without a double-tap window. |
| **B-220** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-21.** `fsFileSize` guard before `fsFileLoad` for `mod.json` in `modmgrParseModJson`, `modmgrRegisterModJsonContent`, and `modmgrParseBotNames` to avoid ERROR spam when the file disappeared after registry scan. | `port/src/modmgr.c` | Boot with stale mod dirs: no missing-mod.json ERROR flood. |
| **B-219** | HIGH | **FIXED v3 PENDING-PLAYTEST 2026-04-23 (architectural - thanks Mike).** 4-21 fix skipped `INTROCMD_WEAPON` when `normmplayerisrunning && MPOPTION_SPAWNWITHWEAPON`, which also skipped `modelmgrLoadProjectileModeldefs(param1)`. v2 added the load in `player.c` at spawn time - works but is the wrong layer. v3 moves the load to the right layer: end of `setupLoadStage` in `setup.c` iterates `g_MpSetup.weapons[]` (the active match weapon set) plus `g_MatchConfig.spawnWeaponNum` (user's chosen spawn weapon; 0xFF=random is already covered by the set iteration) and calls `modelmgrLoadProjectileModeldefs` for each. This matches the manifest contract: `manifestBuild` already registers every non-empty `g_MpSetup.weapons[]` slot as `MANIFEST_TYPE_WEAPON` (`netmanifest.c:446-463`), so the manifest drives both network distribution and local FP-model preload. Pickup-less arenas (Chicago CS: markers=0) and random-weapon configs now work without depending on map placement. v2 player.c edit kept as defense-in-depth; modelmgrLoadProjectileModeldefs is idempotent (setupPlaceWeapon already calls it multiple times per stage). Playtest log `pd-client-0efa2c04.log` at Chicago CS: line 16756 `SETUP: world pickups 0 below target 16 (markers=0); forcing spawn-with-weapon fallback id='base:falcon2'`, line 16771 `GAMELOOP.WEAPON: INTRO skipped`, line 16802 `SPAWN: player 0 spawned with weapon 2 (Falcon 2) -- auto-equipped to right hand` - but first-person hands were empty, fire did nothing. v3 fix closes this. | `src/game/setup.c` (MP weapon model preload block at end of `setupLoadStage`), `src/game/player.c` (belt-and-braces load in spawn-with-weapon branch), `src/game/playerreset.c` (4-21 INTROCMD_WEAPON skip comment anchor) | CS + pickup-less arena (Chicago) + spawn-with-weapon: (1) FP weapon model visible in right hand at spawn. (2) Fire button fires. (3) Ammo HUD matches. (4) Random spawn weapon picks any weapon from the set and ALL have their FP models loaded. (5) Regression check: arenas WITH pickup markers (Temple, Skedar) still load map weapon models via setupPlaceWeapon (the manifest loop is additive, not replacement). (6) Log shows `SETUP: MP weapon models preloaded from match config (spawnWeapon=N random=0|1)` in addition to any legacy fallback warnings. |
| **B-218** | MED | **FIXED v2 PENDING-PLAYTEST 2026-04-23.** 4-21 fix (`ORCH_RELAX_ITERS` 10->20, `min_sep` floor 35->50, post-relax duplicate detect + `botSpawn` live-spawn fallback on XZ proximity) addressed the symptom but not the root cause. Playtest 2026-04-23: bots still all spawned at one point on the initial match frame, respawn fine. Root cause traced: `mpOrchestrateMatchStartSpawns` runs from `lv.c` between the `playerReset` loop and the `playerSpawn` loop (so player match-start positions apply before `playerSpawn`). At that point bots are allocated as chrs but the orchestrator participant iteration in `mpspawn_orchestrate.c:408-420` sees empty `g_MpBotChrPtrs[i]->prop` and skips every bot. Orchestrator places ONLY players, sets `g_MpOrchestrateInitialSpawnDone = true`, and leaves every `g_MpOrchestrateBotPoolIdx[]` entry at -1. When `botSpawnAll` fires later (via AI-script `aiMpInitSimulants` opcode 0x0185) bots have valid props, but the orchestrator branch in `botSpawn` (~line 338) fails the `>= 0` check and falls through to `scenarioChooseSpawnLocation` at line 393. Called in rapid succession for 32 bots in the same tick, that function repeatedly returns the same first-valid pad -> pile-up. Respawns use the tiered pool (`spawnPoolSelectTiered`) and are unaffected. v2 fix: top of `botSpawnAll` now detects the "no bot has an orchestrated pool idx" state and re-runs `mpOrchestrateReset` + `mpOrchestrateMatchStartSpawns` so bots get their Hungarian-assigned slots now that props exist. Player positions remain stable because the orchestrator is deterministic on same seed + roster + stage. | `src/game/bot.c::botSpawnAll` (re-run guard at top), secondary: `src/game/mplayer/mpspawn_orchestrate.c` (reset preserves player pool placement by design) | Chicago / high-bot matches: log now shows `SPAWN.ORCH: botSpawnAll re-running orchestrator (bots missed initial pass, g_BotCount=N)` followed by the Hungarian placement log. Initial match frame: bots dispersed across the pool. `SPAWN.DIAG: post-botSpawnAll` variance WARNING no longer fires. Regression check: respawn after death continues to use tiered pool and remains dispersed. |
| **B-217** | LOW | **FIXED v2 PENDING-PLAYTEST 2026-04-23.** 4-21 fix (`return TICKOP_NONE`) made bots invisible because `chrTick` maintains the per-frame model / render / transform state that the bot path relies on (bot.c header at line 92 notes "movement, physics, and rendering still run every frame via chrTick"). New behaviour: on `g_BotUpdatesDisabled`, zero the aibot `speedmultforwards` / `speedmultsideways` then still return `chrTick(prop)` so bots stay on screen. Minor anim drift is accepted (see S431 "residual bot motion is expected") and is the cost of keeping spawns visible for verification. | `src/game/bot.c` | F6: bots stay fully visible at their spawn position; any drift is subtle and not a walk-run cycle. |
| **B-216** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-20**. Re-entering Grid from CI queued `mainChangeToStage(STAGE_CITRAINING)` even when already on CI, causing a full stage transition (`inputCtxShutdown`, reload) and broken menu state. Fix: `pdguiForgeStartSession()` skips `mainChangeToStage` when `g_StageNum == STAGE_CITRAINING`; `forgeRequestEnterSession()` still runs. | `port/fast3d/pdgui_menu_forge.cpp` | From CI: Main Menu → The Grid → session should start without a full CI reload; menus sane on exit. |
| **B-215** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-20**. Scale tool only iterated `ASSET_CHARACTER` (often empty vs modern `ASSET_BODY` registry). Now also collects `ASSET_BODY` entries with a resolved loose file path via `catalogResolveFile(source_filenum)`. | `port/fast3d/pdgui_menu_moddinghub.cpp` | Modding Hub → Scale Tool: list populates for mod body overrides with on-disk models. |
| **B-214** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20**. After delete, `modmgrRescanDirectory()` updated the registry but the asset catalog still held stale mod entries. Fix: `modmgrSyncCatalogToRegistry()` (rebuild catalog + `catalogLoadInit` + cache invalidation) after successful delete, before `pdguiModManagerRefreshSnapshot()`. | `port/src/modmgr.c`, `port/fast3d/pdgui_menu_mainmenu.cpp` | Delete mod from Installed Mods context menu → row disappears without restart. |
| **B-213** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20**. Skin Editor / Modding: (1) Closing hub no longer forced main menu top-level — view 3 shows Open Hub + Back. (2–3) `g_MenuData.unk5d5_01` skipped `menuRenderModel` with no legacy dialog — charpreview FBO stayed black; `pdguiCharPreviewNeedsMenuModel()` bypasses that skip. (4) `charPreviewSubmitParams` sets `menumodel.zoom=185` for character preview. Hub Escape exits paint session first (`pdguiSkinEditorTryConsumeHubEscape`). | `menu.c`, `pdgui_charpreview.*`, `pdgui_menu_mainmenu.cpp`, `pdgui_menu_moddinghub.cpp`, `pdgui_skin_editor.cpp` | CI title: Mods → Skin Editor → preview rotates; New Skin → capture; Escape leaves canvas then hub. |
| **B-210** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20**. Modding Hub: on `##modhub` appear, clear Enter/Space/FaceDown edges; ignore Escape close for 5 frames (B-131/B-198 class). | `port/fast3d/pdgui_menu_moddinghub.cpp` | Controller: Main Menu → Mods → hub stays open. |
| **B-209** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-20**. Root cause: `joyGetNumSamples()` can be **0** when the joy ring has no span (`curstart == curlast`). PC input is synthesized from the action map, but `bmoveProcessInput` still runs many subsystems inside `for (i = 0; i < numsamples; i++)` — when `numsamples == 0`, **reload** (`alt1tapcount` / `movedata.alt1tapcount`) and **use** (`btapcount`) never incremented despite valid `c1buttons` from the hold/tap discriminator. Fix: after `numsamples = joyGetNumSamples()`, clamp to at least **1** when `controlmode == CONTROLMODE_PC`. | `src/game/bondmove.c` | Solo/MP gameplay PC mode: tap X/F should reload; hold past 250 ms should interact. Log may still show only `action=24(Use)` for X (ACTION_RELOAD is 23 only for R key) — reload is via synthesized `X_BUTTON` / `alt1tapcount`, not `action=23`. |
| **B-208** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20**. Cached mod/base track list scans; only rebuild on dialog appear or catalog generation change. | `port/fast3d/pdgui_menu_mpsettings.cpp` | MP Select Tunes: smooth scrolling; no per-frame full catalog iteration. |
| **B-207** | MED | **FIXED 2026-04-20 (diagnostic)**. Late-add manifest log misclassified **heads** with `numparts==0` as "torn" using the **body** sp_body_108 class. Per `modelcatalog.c` / B-179, heads may have `parts=0` with valid rootnode+skel. `manifestEnsureLoaded` now logs heads with parts=0 as NOTE OK; bodies with parts=0 remain WARNING. (Neck rendering itself is unchanged — if still missing, track separately.) | `port/src/net/netmanifest.c` | Logs: no false torn WARNING for `base:head_*` late-adds. |
| **B-206** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-20**. Spawn pool AABB could be `valid=false` while still carrying a 0..1000 fallback volume, forcing L4 / last-resort to use the (0,100,0) world sentinel. Mark degenerate / missing-room-data fallbacks as valid so radial/grid use the real bbox center. | `src/game/spawnpool.c` | Area 52 + other arenas: bots should not all spawn at (0,100,0); check `SPAWNPOOL: build` logs. |
| **B-205** | MED | **MITIGATED-PENDING-PLAYTEST 2026-04-20**. Same-session mitigation as B-204: `lvupdate240` catch-up cap reduces worst-case per-tick work (helps audio mixer scheduling under load). Full fix may still need AI tick budgeting or audio thread. | `src/game/lv.c` | 31-bot stress: fewer max-gap audio hitches vs prior; still profile if bad. |
| **B-204** | HIGH | **MITIGATED-PENDING-PLAYTEST 2026-04-20**. No direct repro in-dev; cap `lvupdate240` after slowmo/unpause to `TICKS(8)` to reduce worst-case CHR.TICK load per frame (may help long-session stress crashes). | `src/game/lv.c` | Long MP bot matches: monitor for fewer crashes; ASan run still recommended. |
| **B-203** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20**. Two issues: (1) **vk=531** is physical **D-pad down** (`VK_JOY1_BEGIN+12`), not D-right — it had **no** gameplay bind. Added default `ACTION_FIRE_MODE` on `JBTN_DPAD_DOWN`. (2) **D-pad right** already had `ACTION_FIRE_MODE`, but **`ACTION_DPAD_RIGHT` was also bound to the same VK**; `fireVk` walks actions in enum order and `ACTION_DPAD_RIGHT` (19) won over `ACTION_FIRE_MODE` (22), so fire mode never received the button. Removed the duplicate `ACTION_DPAD_RIGHT` bind on `JBTN_DPAD_RIGHT`. | `port/src/actionmap.cpp` | Gameplay: D-pad right and D-pad down cycle fire mode; `C` still does. Verbose log should no longer show NO BINDING for vk=531 on D-down. |
| **B-202** | MED | **FIXED-PENDING-PLAYTEST 2026-04-20 (partial)**. (1) Aim suppression while radial open lives in bondmove (prior work). (2) `amRenderSlot` FOCUSED mode no longer uses alpha-only fill — warm visible tint for selected slot. | `src/game/activemenu.c`, `src/game/bondmove.c` | Weapon wheel: selection slot clearly highlighted; camera stable when open. |
| **B-199** | CRITICAL | **FIXED-PENDING-PLAYTEST 2026-04-19 (happy-blackburn-8eb2ff)**. `0xc0000005 ACCESS_VIOLATION` crashing the game 80 ms after loading Investigation (stagenum `0x33`, bgfile 31 `bg_ear.seg`). addr2line stack: `func0f00505c dlights.c:1836 → func0f004c6c dlights.c:1787 → lightsReset dlights.c:561 → lvReset lv.c:615 → mainLoop pdmain.c:594`. Crash at `var8006142c[portalnum2][portalnum]` with out-of-range `portalnum2` read from a garbage `g_RoomPortals[]` entry. Root cause: the B-185 safety in `bgLoadFile` (fbc276ce) used a single `memset(memaddr, 0, len)` on any `len > romsize - offset` condition — which fires for every call whose `len` is rounded up to 16-byte alignment even when the final 1–15 bytes are just off-file padding. Investigation's `bg_ear.seg` section 3 is 1254 bytes at offset 195850 but the aligned read length is 1264 (`((section3compsize-1)|0xf)+1`); file ends at 197104, so `offset+len` overshoots by exactly 10 bytes. The aggressive `memset` destroyed all 1254 valid bytes of the compressed portal blob, `bgInflate` then produced zeroes, and `g_Rooms[]` / `g_RoomPortals[]` came back with invalid portal indices. Triggered specifically on Investigation because `bg_ear.seg` section 3 is not 16-byte-padded in the ROM; other stages happened to be aligned. Pre-existing latent since B-185 (01:35, 2026-04-19); surfaced during Wave 1 Super Audit playtest when user loaded Investigation. Fix: `bgLoadFile` split into three paths — (1) `offset >= romsize` → zero all (offset past EOF, no valid data); (2) `len > romsize - offset` → partial read — `memcpy(memaddr, src+offset, romsize-offset)` then zero only the padding bytes; only log an ERROR when the missing bytes exceed 16 (alignment gap is silent, real corruption still logs); (3) else → full memcpy. Preserves B-185's deterministic-zero intent for true failure modes while letting aligned reads succeed with their real data. Build clean 2/2 (PerfectDark + PerfectDarkServer). | `src/game/bg.c` | Load Combat Sim → **Investigation** (stagenum 0x33). Stage should load without crashing in `func0f00505c`. No `BG.LOAD: partial over-read` ERROR should appear for Investigation's section 3 load (the 10-byte gap is alignment padding ≤ 16, below the warn threshold). Regression check: load Complex, Skedar Ruins, Villa, Temple, Sewers, Crashsite — every stage should still load correctly (aligned sections follow the full-`memcpy` path). If any stage logs `BG.LOAD: partial over-read ... copied=N` with a gap > 16 bytes, that's real data corruption in the seg file and needs separate triage. |
| **B-198** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-19 (infallible-goldberg-71b379 — S385)**. Combat Simulator End Game confirm + end-of-match input bug, two related issues in one entry. (1) CS pause → "End Game" row: controller activated the row and opened the DANGER confirm popup, but controller nav couldn't reach the red "End Match" / Confirm button — focus didn't latch onto Cancel on the popup's first frame (BeginPopupModal + `OpenPopup` same-frame race with ImGui's popup NavInit), and D-pad Right moved nothing. (2) CS end-of-match screen: after a match ended naturally (timer/score limit), the endscreen rendered but all input was dead — Escape/Enter/A/B did nothing, player couldn't return to the CI lobby. Root cause for (2) was a race between `menuPushRootDialog`'s `menupoolReleaseAll()` (which schedules deferred pops of any owned_ctx) and `renderMpEndscreen`'s fresh-entry gate: on the renderer's first frame `inputCtxIsActive(&g_CtxImGuiMenu)` still returned true (deferred pop not yet fired) so the push was skipped; end-of-frame the pop fired; next frame `freshEntry` was false so no re-push — `g_ImcMenu` never activated, so `ACTION_USE`/`ACTION_CANCEL_USE` couldn't produce ImGui Enter/Escape events. Compounding: `g_MpEndscreenIndGameOverMenuDialog` / `TeamGameOverMenuDialog` / `ChallengeCompletedMenuDialog` were unregistered in the menu pool (only Cheated + Failed were), so their push went through the unregistered fallback with no pool ctx management. Fix: (1) register all five MP endscreen dialogs + `g_MpEndGameMenuDialog` (as WARNING_MODAL) in `port/src/menupool.c`. (2) `renderMpEndscreen` now pushes `g_CtxImGuiMenu` unconditionally whenever it isn't active, not just on fresh entry — force-close sites also clear the dialog from the legacy menu stack, so there's no resurrect-loop risk. (3) `renderMpEndGameDialog` gains an `s_EndGameOpenFrame` counter: `SetKeyboardFocusHere(0)` before Cancel for the first 5 frames after `OpenPopup` forces focus onto Cancel regardless of popup NavInit timing, and button clicks + keyboard shortcuts are gated on a 3-frame debounce so the Enter/A press that opened the popup can't bleed through. All popup-close paths clear the frame counter for clean re-entry. Build clean 775/775. `PerfectDark.exe` 53,363,195 / `PerfectDarkServer.exe` 23,141,856. | `port/src/menupool.c`, `port/fast3d/pdgui_menu_endscreen.cpp`, `port/fast3d/pdgui_menu_warning.cpp` | (1) CS pause → "End Game" with controller: popup appears centered over scrim, focus ring on Cancel. D-pad Right moves focus to "End Match". A confirms. B/Esc cancels. First 3 frames reject clicks + shortcuts (bleed-through debounce). Keyboard: arrows/Enter/Esc/Space all work. (2) Play CS match to natural end (timer/score): endscreen appears with rankings. Enter → Play Again. Esc → Main Menu. Controller A/B mirror those via `pdguiDriveImGuiNav`. (3) Challenge mode regression: Completed/Failed/Cheated endscreens all still work (same renderer now covers all five variants through pool registration). (4) No new WARNINGs in pd-client.log related to `MENUPOOL: unregistered dialogdef` for endscreen / end game dialogs. |
| **B-197** | CRITICAL | **FIXED-PENDING-PLAYTEST 2026-04-19 (brave-bouman-13bd68)**. `ACCESS_VIOLATION` at `PC=0x7ff7bc5cef4a (+0x21ef4a)` crashing the game during CI (stage `0x26`) free-roam, ~1.5s after the user closed the main menu and started walking. Tester's log at `[00:22.99]`: `FATAL: ACCESS_VIOLATION PC=0x7ff7bc5cef4a (+0x21ef4a) CODE=0xc0000005 MODULE=0x7ff7bc3b0000`. Last breadcrumb `CHR.TICK slot=0 chrnum=0 action=1 race=0 model=…` at `22.992183s` followed by crash. addr2line resolves the stack to `gfx_sp_matrix port/fast3d/gfx_pc.cpp:1093 → gfx_run_dl gfx_pc.cpp:2489 (G_MTX case) → gfx_run gfx_pc.cpp:2900 → videoSubmitCommands video.c:133 → schedSubmitTask pdsched.c:241 → rdpCreateTask rdp.c:99 → mainTick pdmain.c:780`. Root cause: `gfx_sp_matrix` at line 1093 does `int32_t int_part = addr[i*2+j/2];` where `addr` is `seg_addr(cmd->words.w1)` decoded from a `G_MTX` display-list command in a BG room's GDL. When a room's display list was partially clamped by the B-195 bounds fix (`port/src/preprocess/gbi.c::gbiConvertGdl` + `convertRoomGfxData` — added this week), or when the N64 ROM data references a matrix via a cross-room offset that the PC port's per-room inflated buffer can't satisfy, the decoded pointer can land on NULL-adjacent memory. First `addr[...]` deref is an AV. Same pointer class as B-195 (Complex bg room preprocess overflow) — B-195 guarded the *preprocess* (allocation / DL conversion) side; B-197 guards the *render* (consumer) side. Unrelated to B-196 (imgui focus leak, also in the same session): log confirms B-196 fix worked — `MENUPOOL: released main_menu gen=1` + `INPUTCTX: imgui_menu on_pop` cascade fired cleanly at `[00:21.43]`, then `[00:22.57] DIAG axis: move=0.000,-1.000 back=1` shows WASD responsive 1s later, and the crash occurred 400ms after that while the player was walking backward. Fix: at the top of `gfx_sp_matrix` (`port/fast3d/gfx_pc.cpp`), early-return with an identity matrix when `(uintptr_t)addr < 0x10000` — any legitimate Win64 user pointer is ≥ 64 KB; anything below is guaranteed garbage. Logs `WARNING: B-197: gfx_sp_matrix addr=%p too low — skipping (corrupt BG display list? params=0x%02x, warn N/5)` capped at 5 warnings per session. Downstream matrix stack stays consistent (identity push is a no-op visually), game stays alive. Complements (doesn't replace) the B-195 preprocess bounds clamps — together they cover the full pipeline from inflated ROM data → GDL conversion → render. | `port/fast3d/gfx_pc.cpp` (guarded top of `gfx_sp_matrix`) | Play Combat Sim → Carrington Institute (stage 0x26) → close Agent Select / Main Menu → walk around with WASD for 30+ seconds. No AV at PC+0x21ef4a; game stays alive even if a corrupt G_MTX is encountered. Log may show one or more `WARNING: B-197: gfx_sp_matrix addr=…` lines (capped at 5) identifying corrupt-matrix events — affected rooms may render with one draw call using identity transform (minor visual glitch) but the scene continues. Regression-check: Complex, Skedar Ruins, Temple — no render-path crashes on any stage load. |
| **B-196** | HIGH | **FIXED — PLAYTEST CONFIRMED 2026-04-19 (brave-bouman-13bd68)**. Post-fix tester log `Build/pd-client.log` (03:56) confirms fix works: `[00:21.42] MENU_IMGUI: main menu CLOSE via ESC/B` → `[00:21.43] MENUPOOL: released main_menu gen=1 ctx=imgui_menu` → `[00:21.43] INPUTCTX: imgui_menu on_pop -- g_ImcMenu deactivated` cascade. At `[00:22.57] DIAG poll: topCtx='gameplay' menuActive=0 numActiveIMCs=1` + `DIAG axis: move=0.000,-1.000 back=1` — **WASD registered as backward movement**, previously dead. No `B-195: ImGui WantCaptureKeyboard=1` WARNING in the subsequent 1.5s of free-roam (game crashed 400ms later due to the unrelated B-197 gfx_sp_matrix bug, not anything input-related). (User originally filed conceptually as a follow-on to B-194; renumbered B-196 here because dev's B-195 was already allocated to the Complex-stage crash fix from `frosty-banach-0e3fc1`.) Input stuck: after closing a menu cold-boot (Agent Select → Main Menu → close with Esc), WASD / Space no longer move the player in CI free-roam. Only the pause/menu-toggle key (Escape / Start) still functions, and pressing it visibly "doubles up" the menu (opens and closes strangely). Distinct from B-194 (Agent Select ctx leak) which showed zero `MENUPOOL:` lines and a stuck `imgui_menu` ctx — B-194 is confirmed fixed in this session (clean `MENUPOOL: acquired/released` + `INPUTCTX: imgui_menu on_pop` cascade at `[00:14.63]`-`[00:14.64]` of tester log `Build/pd-client.log`). B-196 only manifests *after* the ctx pops cleanly. Tester's 03:32 log shows a clean close cascade at `[00:14.63]`-`[00:14.64]` with `INPUTCTX: imgui_menu on_pop` firing and gameplay IMC becoming top. In the ensuing 8 seconds of free-roam (until the user re-pressed pause at `[00:22.48] MENU: ACTION_PAUSE detected ... lvframenum=945`) `BMOVE: allowc1x=1 ... AXIS_MOVE=0.000,0.000` stays all-zero despite the input gate being open — movement keys never reached the action map. Root cause: `pdguiProcessEvent` in `port/fast3d/pdgui_backend.cpp:849-859` routes keyboard events through the game's actionmap **only if `!ImGui::GetIO().WantCaptureKeyboard`** (Escape and Enter are whitelisted through regardless). ImGui's `WantCaptureKeyboard` is derived from `NavWindow` / `NavId` / `ActiveId`, and these references can survive a menu's final `Begin`/`End` teardown — the window is gone but ImGui still remembers the last-focused widget as the nav anchor. With `WantCaptureKeyboard` stuck at 1 while the input-ctx stack is back at `gameplay`, WASD / Space get silently swallowed by the gate, and the next Esc press drives ImGui's nav action (causing the "menu doubling up" appearance because ImGui-side focus bounces through the phantom nav while the game's ACTION_PAUSE also fires). Fix — two parts: (1) new `pdguiClearImGuiFocusAndNav()` helper in `port/fast3d/pdgui_backend.cpp` calls `ImGui::FocusWindow(nullptr)` + `ImGui::ClearActiveID()`; (2) `imguiMenuOnPop` in `port/src/inputctx.c` invokes the helper right before the existing `imcDeactivate` + log line. Fires on the very last imgui menu close (when the shared `imgui_menu` input context pops back to depth 0), which is exactly when ImGui should release its keyboard claim. Belt-and-suspenders leak-class catcher: `pdguiProcessEvent` emits a rate-limited `WARNING: B-195: ImGui WantCaptureKeyboard=1 while top ctx is gameplay — swallowing sym=0x...` if the gate ever swallows a key while no menu is active, so any future regression of this leak class surfaces in the log instead of silently breaking WASD again (WARNING text retains the B-195 tag from the original filing — harmless, just a bug-id mismatch in the log string). | `port/fast3d/pdgui_backend.cpp`, `port/include/pdgui.h`, `port/src/inputctx.c` | Cold boot → CI free-roam appears (B-193 repro or clean) → open Agent Select → sign in or back out → open Main Menu → close with Esc → immediately press W/A/S/D → **player moves** (previously dead). Space jumps. Esc reopens the menu without the "doubling" flicker. Log should show `INPUTCTX: imgui_menu on_pop -- g_ImcMenu deactivated` on close; NO `WARNING: B-195: ImGui WantCaptureKeyboard=1 while top ctx is gameplay` in the subsequent free-roam. Repeat for 5 open/close cycles. For textbox flows (Interface theme search, Modding Hub filename entry): WASD STILL doesn't move the player while the textbox is focused — that's correct behaviour (gate still fires because imgui legitimately wants the keyboard); Esc closes the textbox, then WASD works again. |
| **B-195** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-19 (frosty-banach-0e3fc1)**. Hard crash (`sysFatalError`) loading **Complex** (stagenum `0x1f`, `bg_ref.seg` / `bg_ref_tilesZ`) in Combat Sim solo (1 player, 0 bots). Crash log `Build/pdclient-crash-complex-apr19.log` tail: stage setup reports success (`LOAD: bgReset done` + `LOAD: bgBuildTables done` at `[00:24.92]`, `SETUP: setupLoadStage complete`, `LV.DIAG: first-render stage=0x1f roomcount=45 player_room=7 camera_room=7 pos=(-3817,159,349) frame=0`) — then on the first tick's room GFX load: `ERROR: FATAL: overflow when trying to preprocess a bg room, size 1152 newsize 15528` from `port/src/preprocess/filebg.c:472` (`preprocessBgRoom`'s `sysFatalError` on `newSize > size`). Build version `dev f32d1e51`. Root cause: Complex room 7's inflated gfx data was 576 bytes (printed `size 1152` is the internal post-doubled scratch size) and the PC-port 32→64-bit preprocessing produced 15528 bytes of output — a ~27× expansion that far exceeds the 2× scratch multiplier hardcoded in `preprocessBgRoom`. The legitimate per-room 64-bit pointer expansion is ~2×, so the runaway output was reads past the end of the per-room inflated buffer: on N64 the whole BG segment was a single contiguous blob so `roomblock.ptr_gdl` / `ptr_vertices` / `ptr_colours` could reference offsets anywhere in the segment; in the PC port each room is inflated into its own independent buffer by `bgInflate`, so any offset pointing past this room's data walks into adjacent garbage. `gbiConvertGdl` (port/src/preprocess/gbi.c) read until it found a byte matching `G_ENDDL` (0xB8) and wrote 2× (8 → 16 bytes/cmd) of host output for every read — ~485 bogus commands = 7764 src bytes read, 15528 dst bytes written — blowing the scratch and the caller's allocation. Three-layer fix: (1) `gbiConvertGdl` now takes an explicit `src_size` and synthesizes an ENDDL when a read would cross that boundary (logged `WARNING: gbiConvertGdl: source overrun at srcpos=… src_size=…`). (2) `convertRoomGfxData` clamps `endpos` / `vtx_end` / `col_end` to the actual inflated `src_size` and skips any `ptr_gdl` whose offset is outside the buffer (logged `WARNING: convertRoomGfxData: gdl[i] src_offset=… outside src_size=… — skipping`). (3) `preprocessBgRoom` bumps the scratch dst allocation from 2× → 8× with a 4 KB floor; `sysFatalError` replaced with `LOG_ERROR` + `return 0` so a malformed room degrades rather than killing the process. `src/game/bg.c`'s caller allocation bumped from 2× → 8× with a 16 KB floor so the `memcpy(data, dst, newSize)` back into the allocation cannot overflow. Caller guard added: `if (inflatedlen == 0)` skips room load cleanly. Callers `filemodel.c:660` updated to pass `src_file_len`. `port/src/preprocess/gbi.c` now includes `system.h` for `sysLogPrintf`. | `port/include/preprocess/gbi.h`, `port/src/preprocess/gbi.c`, `port/src/preprocess/filebg.c`, `port/src/preprocess/filemodel.c`, `src/game/bg.c` | Boot build, play Combat Sim → Complex (solo, 1 player, 0 bots). Stage must load and render all 45 rooms without the `FATAL: overflow when trying to preprocess a bg room` crash. Log may include `WARNING: convertRoomGfxData: gdl[i] src_offset=… outside src_size=…` or `WARNING: gbiConvertGdl: source overrun at srcpos=… src_size=…` for the specific room(s) whose ROM data referenced cross-room GDL offsets — those rooms may render with minor visual glitches (missing a GDL) but the game stays alive. Repeat on other stages (Temple, Ravine, G5 Building, Skedar Ruins) to regression-check stage loading. |
| **B-194** | HIGH | **FIXED — PLAYTEST CONFIRMED 2026-04-19 (brave-bouman-13bd68)**. Post-fix tester log `Build/pd-client.log` (03:07) shows the clean acquire/release pair: `[00:15.75] MENUPOOL: acquired agent_select gen=1 def=… ctx=none(shared)` → `[00:15.77] INPUTCTX: pushed 'imgui_menu' (depth now 2)` + `MENUPOOL: attached ctx to active agent_select gen=1 ctx=imgui_menu`; then on close `[00:18.70] MENUPOOL: released agent_select gen=1 ctx=imgui_menu` → `[00:18.72] INPUTCTX: imgui_menu on_pop -- g_ImcMenu deactivated`. Input returns to gameplay cleanly on back-out. (User originally filed as B-192; renumbered B-194 here to avoid collision with dev's B-191/B-193.) Input stuck after backing out of cold-boot Agent Select / main menu at CI. Repro: boot game → title intro skips to CI (stage 0x26) → press Start/Esc → Agent Select appears → back out without signing in → player can neither move nor reopen any menu until the process is killed. Tester log `Build/pdclient-apr19-0236.log`: `INPUTCTX: pushed 'imgui_menu' (depth now 2)` at `[00:16.32]` followed by `PREFS.AGENT: prefs reset to defaults (Agent Select open)` at `[00:16.33]`, but **zero `MENUPOOL: acquired …` or `MENUPOOL: released …` lines appear for the entire 7-second menu session**, and no matching `INPUTCTX: imgui_menu on_pop` before `shutdown` at `[00:23.60]`. Meanwhile `BMOVE: allowc1x=1 allowc1y=1 allowc1buttons=1` starts firing at `[00:19.40]` (game thinks input is allowed), yet `DIAG poll: topCtx='imgui_menu' menuActive=1` stays active until shutdown — so bmove-level gating says "allowed" but the input-context stack still routes all input to the menu IMC at priority 10 instead of gameplay. Root cause: `g_FilemgrFileSelectMenuDialog` (the non-4MB Agent Select dialogdef in `src/game/filemgr.c:3428`, the only one actually pushed on modern PC builds — `menuPushRootDialog(&g_FilemgrFileSelectMenuDialog, MENUROOT_FILEMGR)` at `filemgr.c:2818` in `filemgrConsiderPushingFileSelectDialog`) was **never registered with the menu pool**. `port/src/menupool.c:500` only registered the `4Mb` twin `g_FilemgrFileSelect4MbMenuDialog` (plus `g_PakChoosePakMenuDialog` and `g_ChangeAgentMenuDialog`). Effect: when `renderAgentSelect`'s `IsWindowAppearing` calls `menupoolAcquireDialog(&g_FilemgrFileSelectMenuDialog, &g_CtxImGuiMenu)`, `lookupType()` returns `MENU_TYPE_NONE`, taking the unregistered-fallback branch at menupool.c:275-283 which silently `inputCtxPush(ctx)` (no `MENUPOOL: acquired` log — matches the tester's zero-line fingerprint) and returns 1. On close (`menuPopDialog → menuCloseDialog → menupoolReleaseDialog`), the mirror branch at menupool.c:288-294 is a no-op for `MENU_TYPE_NONE`, so the pushed `g_CtxImGuiMenu` is orphaned. The menu IMC at priority 10 then permanently eats every ACTION_PAUSE / ACTION_MOVE / ACTION_USE keypress. Fix: **register `g_FilemgrFileSelectMenuDialog` as `MENU_TYPE_AGENT_SELECT`** in `port/src/menupool.c::menupoolInit`, matching the `4Mb` twin. The S304/S300 cascade `menuPopDialog → menuCloseDialog → menupoolReleaseDialog → inputCtxPopDeferred` then fires cleanly on every close path (Escape at agentselect.cpp:406, select-agent at :361/:368, X/delete, etc.) since the slot now has a registered type + owned ctx. Also hardened the unregistered-fallback leak class: `menupoolAcquireDialog` now emits `WARNING: MENUPOOL: unregistered dialogdef <ptr> pushed ctx <name> without pool dedup — add to menupoolInit to avoid ctx leak` and records the def pointer + ctx so the mirror `menupoolReleaseDialog` unregistered branch can `inputCtxPopDeferred` it. Any future unregistered dialog that pushes g_CtxImGuiMenu surfaces as a WARNING instead of silently stranding input. Build clean. | `port/src/menupool.c` (register FileSelect + unregistered-leak warning / deferred-pop) | Cold boot into CI → press Esc/Start → Agent Select appears → press Esc without signing in → CI regains input; Esc reopens Agent Select cleanly; WASD moves player. Log should now show `MENUPOOL: acquired agent_select gen=1 def=… ctx=imgui_menu(owned)` on open and `MENUPOOL: released agent_select gen=1 ctx=imgui_menu` followed by `INPUTCTX: imgui_menu on_pop -- g_ImcMenu deactivated` on close. Repeat 3-5 open/close cycles — no `MENUPOOL: unregistered dialogdef … pushed ctx` WARNING, no `DIAG poll: topCtx='imgui_menu'` once the last menu closes. Sign-in flow (pick agent → Enter): same clean release pair fires through `filemgrSaveOrLoad` on agent load. |
| **B-193** | HIGH | **ROOT CAUSE IDENTIFIED + FIXED-PENDING-PLAYTEST 2026-04-19 (elated-hugle-7ec221 — S384)**. Commit `fe107e3e` (2026-04-17, M4 optimisation) collapsed `ALIGN16(val)` to `(val)` on the grounds that `mempAlloc` already returns aligned memory. That rationale held for SIZE-only uses but not for POINTER uses — `bg.c:1516` / `bg.c:1934` do `header = (u8 *)ALIGN16((uintptr_t)headerbuffer)` where `headerbuffer` is a `u8[0x50]` stack buffer that's meant to be advanced to a 16-aligned start before `preprocessBgSection{1,2,3}Header` parses structured fields out of it. With the no-op macro the parse runs from whatever offset the stack gave — on 64-bit the stack is usually 16-aligned so it MAY work (explaining the intermittent reproduction and the "not reproducing after a clean rebuild" brave-bouman observation on 5 cold boots — rebuilds shift stack frame layouts), but nothing guarantees it, and any function above in the call tree with misaligned locals shifts `headerbuffer` off. Identical unpatched pointer use at `bg.c:1537` (`scratch` inflate-buffer pointer) corrupts the primary-data buffer whenever the shift crosses a 16-byte lane. **Fix**: restore `ALIGN16(val)` to `((((val) + 0xf) | 0xf) ^ 0xf)` in `src/include/constants.h:79` so every pointer-alignment site gets a real round-up. 0–15 bytes of padding per allocation is trivial overhead on modern hardware. Prior status: intermittent, low-probability repro under incremental builds; brave-bouman-13bd68 Phase 5 diag (cam_pos/tickmode on frame 0+30) in `port/src/pdmain.c` retained for future regression-detection; frame-30 3-room CI output is the opening-cutscene TICKMODE_CUTSCENE which is correct-by-design (my earlier "3 rooms = broken" diagnosis was a false positive). Build clean 777/777. `PerfectDark.exe` 53,232,611 / `PerfectDarkServer.exe` 23,154,674. Renumbered from B-191 to B-193 to avoid collision with dev's Updater fix. | `src/include/constants.h` | Playtest: cold-boot CI 5× in a row — scene should render on every cold boot (no sky-only / invisible-geometry runs). Also try other stages that hit `bgLoadFile` (Skedar Ruins, Complex, Felicity arena, Temple) 2–3× each. LV.DIAG lines still emit at frame 0 and frame 30 so any future shortfall shows the exact state. |
| **B-191** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (admiring-chandrasekhar-ba2a52)**. Standalone `Updater.exe` (`pd-updater` target) hid the **Update / Install** button until the user manually resized the window. Root cause: `createControls` in `port/src/updater_standalone/updater_gui.c` stacked every child at fixed Y coordinates from the top, with the action row landing at `y≈528` of a `WINDOW_CLIENT_H=572` client. On systems where DPI scaling or system-caption metrics push the bottom edge slightly higher, the buttons clipped below the client edge — exactly the symptom Mike reported. Per the standing rule that action buttons must be docked, not scrolled. **Fix**: refactored layout into a docked-footer pattern. New `layoutControls(hwnd)` reads the live client rect and re-anchors every child via `DeferWindowPos` — Update + Close action buttons + status text + progress bar pinned to bottom; title / version row / check button / list / labels anchored to top; release-notes edit box stretches to fill leftover vertical space. Window is now resizable (`WS_THICKFRAME` restored) with `WM_GETMINMAXINFO` enforcing a `WINDOW_CLIENT_W × WINDOW_CLIENT_H` (720 × 620) floor so the docked layout always has room. `WINDOW_CLIENT_H` bumped 572 → 620 for headroom; static labels gained `IDC_*` IDs + dedicated HWND fields so layout can move them on resize. `WM_SIZE` calls `layoutControls` + invalidates; `createControls` also calls it once at the end so the initial layout is correct before Windows delivers its first `WM_SIZE`. Build clean: `Updater.exe` 12,846,748 bytes via `ninja -C Build pd-updater`. | `port/src/updater_standalone/updater_gui.c` | Run `Updater.exe`. Window opens at 720×620 with the Update + Close buttons clearly visible at the bottom-right (Update disabled until a release is selected). Drag the window edges to shrink/grow — buttons stay pinned to the bottom-right; release-notes box absorbs the vertical change. Window cannot be made smaller than 720×620 (the inner content area). Resizing horizontally widens the version label, list view "Title" column, and notes box. Click a release row → Update enables → click Update → confirm modal → progress bar appears between notes and status. |
| **B-190** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (friendly-kirch-688ea4)**. Combat Simulator Room header "Players in Room (X Player, Y Bot)" didn't appear to update when bots were added or removed. Root cause was UX, not state: `countBots()` was recomputed every frame and correctly, but the header lived INSIDE the `##room_players_list` scrollable child along with the team sort dropdown and all the player rows. Once the row list filled its viewport, the header scrolled off the top — the number WAS updating on each Add Bot press, but was invisible past ~8 bots. Fix: (1) move the header + Team Sort dropdown OUTSIDE the scrollable child into the outer `##room_panel_outer` panel so they stay pinned at the top; `listH` is now computed as `GetContentRegionAvail().y - btnH - 2*spacing` after the sticky header has been laid out. (2) Bake the count/cap into the Add Bot button label itself — `"Add Bot  (X / Y)"` — so the primary interaction point also shows the current state with zero scrolling risk. (3) Extend the header format to show the cap too: `"%d/%d Bot%s"` instead of bare `%d Bot%s`. Build clean 774/774. `PerfectDark.exe` 53,237,073 / `PerfectDarkServer.exe` 23,139,808. | `port/fast3d/pdgui_menu_room.cpp` (renderPlayerPanel) | Solo CI Room → add 10+ bots with the Add Bot button. The "Players in Room (1 Player, N/32 Bots)" header should remain pinned at the top of the right panel regardless of how many bot rows are added; the Add Bot button below the scrollable list should show `Add Bot  (N / 32)` and increment immediately on each click. Remove All via bot context menu → counter ticks back to 0 on the same frame. |
| **B-189** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (friendly-kirch-688ea4)**. The in-world interact prompt ("[E] Pick up" / "[A] Open" / etc.) was rendering over ImGui menu overlays — if you walked up to a pickup and then opened any menu (Settings, Pause, Forge editor, etc.), the `[KEY] Label` pill stayed on screen floating above the menu. Root cause: `pdguiInteractPromptRender` in `port/fast3d/pdgui_interact_prompt.cpp` drew on `ImGui::GetForegroundDrawList()` and only gated on "is there an interact target tracked right now?" via `propInteractPromptLabel() != NULL`. `g_InteractProp` is populated by the gameplay tick and persists across menu opens, so the prompt kept drawing. Fix: early-return when `pdguiIsActive()` is non-zero (any non-gameplay input context is on top of the stack) — same authority predicate used by every other gameplay-HUD-only overlay. The prompt now only renders when the player is actually in gameplay, restoring the documented HUD layer policy in `context/designs/hud-layer-order.md` §7. | `port/fast3d/pdgui_interact_prompt.cpp` (added `pdguiIsActive` gate + `pdgui.h` include) | Walk up to any pickup or door so the `[E] Pick up` / `[A] Open` pill appears at the reticle. Press Escape (or any menu toggle). Menu opens; prompt disappears on the same frame. Close menu; prompt reappears (if interact target still in range). Repeat with pause menu, Settings, Modding Hub, Forge editor — all hide the prompt. Gameplay behavior unchanged (prompt still tracks range / target). |
| **B-188** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (determined-easley-8489bc)**. Select Tunes base-game track clicks didn't add to the playlist — only mod tracks did. Repro: CI Room → Options → Soundtrack → Select Tunes → click any "Base Game" row → nothing appears in the right-hand "Selected" panel (mod track rows in the same screen toggle correctly). Root cause: `renderSelectTunes` in `pdgui_menu_mpsettings.cpp` had a legacy branch for base tracks that called `list_SetClick(mpSelectTuneListHandler, 0, i)` (the N64-era single-tune radio select) while mod tracks went through `audioAddModPlaylistEntry(catalog_id)`. Base tracks never touched the playlist so they never showed up in "Selected" and never survived to `mpChooseTrack`. Secondary hole: even if a base catalog_id had made it into the playlist, `mpChooseTrack` in `mplayer.c` only accepted entries with a non-empty `file_path`, so it would silently fall through to the legacy multitrack picker and ignore the playlist. Fix — three-part: (1) New `BaseTrackCollector` in `pdgui_menu_mpsettings.cpp` gathers every bundled ASSET_AUDIO / AUDIO_CAT_MUSIC catalog entry, indexed by `sound_id`; `baseCatalogIdByMusicnum()` maps the UI's `mpGetTrackMusicNum(slot)` to the catalog ID. (2) Base tracks now use the same playlist-toggle pattern as mod tracks — highlight when `inPl`, `audioAddModPlaylistEntry` / `audioRemoveModPlaylistEntry`, `netSendRoomPlaylistUpdate` for the room leader. Legacy `list_SetClick` retained only as a logged fallback if the catalog lookup ever misses. (3) `mpChooseTrack` (`src/game/mplayer/mplayer.c`) now handles bundled catalog entries with no `file_path`: `modMusicStop()` if mod music was playing, set `g_MusicLife60` from the matching `g_MpTracks[]` duration, and return `ar.sound_id` as the MUSIC_* enum so `musicStartPrimary` queues the N64 sequencer. The Selected panel also resolves base-track display names via `baseDisplayNameByCatalogId`. Build clean 774/774. | `port/fast3d/pdgui_menu_mpsettings.cpp`, `src/game/mplayer/mplayer.c` | CI Room → Options → Soundtrack → Select Tunes → click a Base Game row (e.g., "Dark Combat"). Row highlights in the Library; track appears in the right-hand "Selected" panel with its localized display name. Click again → removes. Mix base + mod tracks in the same playlist; start match → audio rotates through all of them (base via N64 sequencer, mod via PCM stream). Log should show `SELECTTUNES: open — base_tracks=N mod_tracks=M base_catalog=K` on dialog open. Network: in a Combat Sim room with a remote guest, room leader toggling base tracks should broadcast via `SVC_ROOM_PLAYLIST`. |
| **B-178** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (naughty-banach-638906 — S370)**. Theme colors don't persist across restarts: user changes theme in Main Menu → Settings → Interface, pd.ini saves, but the new theme reverts on the next launch (often silently, because the revert happens when Agent Select opens). Root cause: `prefsAgentResetVisuals()` hard-coded `base:theme_blue` / empty chrome/font as the "pre-sign-in default" — every Agent Select visit clobbered the pd.ini-saved preference. Two-part fix: (1) `prefsAgentInit()` now captures the *current* visual state (after `pdguiThemeLoaderInit` has applied pd.ini) into new baselines `s_BaseThemeId` / `s_BaseUiChromeStyleId` / `s_BaseUiChromeEnabled` / `s_BaseTitleBarStyle` / `s_BaseFontId` / `s_BaseScanlineEnabled` / `s_BaseScanlineAlpha`; `prefsAgentResetVisuals()` uses these. (2) New `prefsAgentRefreshVisualsBaseline()` exposed via `prefs_agent.h`, called from the Main Menu theme picker after `configSave("pd.ini")` so a mid-session pre-sign-in theme change also updates the baseline. NOTE: the existing **B-172** entry below refers to a separate mod-manifest bug; the user used "B-172" colloquially to refer to the theme issue — tracked as B-178 to avoid collision. | `port/src/prefs_agent.c`, `port/include/prefs_agent.h`, `port/fast3d/pdgui_menu_mainmenu.cpp` | Boot → Main Menu → Settings → Interface → pick a non-default theme (e.g., `base:theme_red`). Exit, restart. Main Menu should show the red theme. Open Agent Select → red stays (not blue). Sign in an agent with no `[Theme]` in their prefs_*.ini → red stays. Sign in an agent whose prefs_*.ini has `[Theme] ActiveId = base:theme_green` → flips to green. Sign out / Agent Select reopens → red (baseline) not blue. |
| **B-177** | LOW | **FIXED-PENDING-PLAYTEST 2026-04-19 (naughty-banach-638906 — S370)**. CI Room time-limit slider off-by-one vs label: slider at 29 showed "30 min" in the label; slider at 60 showed "No limit". Root cause: slider ran `0..60` with the value stored 0-based (`timelimit = minutes - 1`), but the companion `ImGui::Text` added `+1` for display. Fix: convert slider to 1-based (1..61) and the `+1` conversion to `-1` on write, mirroring the Score slider pattern a few lines below. `%d min` label now reads the same 1-based `tl` as the slider so handle and text always match. "No limit" still fires when stored `timelimit >= 60`. | `port/fast3d/pdgui_menu_room.cpp` (renderRoomScreen Limits section) | CI Room → drag Time slider from 1 to 61. Each step: handle and label match (1 / 1 min, 2 / 2 min, ... 60 / 60 min, 61 / No limit). No "off by one" between slider thumb number and the text to its right. |
| **B-176** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-19 (naughty-banach-638906 — S370)**. Match simulation didn't pause on MP endscreen — bots kept fighting / projectiles kept hit-testing in the background while the results dialog displayed. Root cause: `mpEndMatch` called `mpSetPaused(MPPAUSEMODE_GAMEOVER)` but never `lvSetPaused(true)`. `lvIsPaused()` and `mpIsPaused()` are two independent gates in the lv.c tick; only the former zeroes `g_Vars.lvupdate240` unconditionally, so parallel code paths that run outside the mpIsPaused branch continued ticking. Solo endscreen always set `lvSetPaused(true)` in `endscreenPrepare` (endscreen.c:1789) — MP endscreen just forgot. Fix: `mpPushEndscreenDialog` now calls `lvSetPaused(true)` and re-asserts `mpSetPaused(MPPAUSEMODE_GAMEOVER)` defensively. All three endscreen exit paths — `pdguiEndscreenExitToMainMenu`, `pdguiEndscreenStartMission`, `pdguiEndscreenNextMission` — call `lvSetPaused(false)` before the stage transition so the next level doesn't boot paused. | `src/game/mplayer/ingame.c::mpPushEndscreenDialog`, `port/fast3d/pdgui_bridge.c::pdguiEndscreen{ExitToMainMenu,StartMission,NextMission}` | MP Combat Sim → Foothold vs 3 bots, score-limit 3 → reach score limit → endscreen appears. Bots visible in background should FREEZE (no run animation, no combat). Press Return to Room → lobby loads, next match starts unpaused. Also: endscreen keyboard shortcuts (Enter / Esc / title X) all exit to lobby with the level unpaused. |
| **B-175** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (naughty-banach-638906 — S370)**. Killfeed missed simulant-on-simulant kills. User's words: "the killfeed isn't working, at least for simulants killing simulants, but their kills should be treated equally to a human players'." Root cause: `mpstatsRecordDeath` gated the `pdguiKillfeedPush` call on `g_Vars.normmplayerisrunning && ampchr && vmpchr`. Two separate holes — (a) `normmplayerisrunning` is false in co-op / counter-op so those modes never received killfeed entries at all, (b) the combined non-null guard silently dropped any kill whose attacker mpchrconfig lookup failed, which is exactly the shape of "sim kills sim but neither pushes" if `func0f18d074 → MPCHR` ever returns NULL for the attacker bot slot. Relaxed the guard to `mplayerisrunning && vmpchr`; missing `ampchr` now pushes with `attackerName=NULL` so the renderer draws a "?" attacker pill instead of discarding the event. Added `LOG_NOTE: KILLFEED: push ...` on every successful push and `LOG_WARNING: KILLFEED: skipped ...` when the relaxed guard still rejects — future playtest logs pin whether emit or render is the problem. | `src/game/mpstats.c::mpstatsRecordDeath` | MP Combat Sim → Foothold or any arena → 4 bots, no human participant OR human participates but observes a long bot-vs-bot streak. Killfeed pill in top-right should show `Bot_A killed Bot_B` for every sim-sim kill, identical format to human kills. Log should show a `KILLFEED: push aplayernum=... vplayernum=... a='...' v='...'` NOTE for every kill. If sim-sim still missing from UI, check for `KILLFEED: skipped` WARNING — that narrows it to the emit side; absence means the problem is in `pdguiMpIngameRender`. |
| **B-174** | HIGH | **PARTIAL-MITIGATION + DIAGNOSTIC INSTRUMENTED 2026-04-18 (clever-kepler-412853)**. Playtest report (build `a70a457a` / v0.0.131): "Chris played Car Park on Solo Combat Simulator and bots all were spawning in the same spot, and then running to a specific spot and congregating there without moving or otherwise acting." Two linked symptoms: (a) all bots spawn at identical position, (b) all bots pathfind to one spot and freeze. Specific to Car Park (`base:arena_mp_carpark`, stagenum=0x3d) per tester; other arenas worked. Investigation (code-path-only; no in-game repro): spawn pipeline is `scenarioChooseSpawnLocation → playerChooseGeneralSpawnLocation → playerChooseSpawnLocation(pads=g_SpawnPoints, numpads=g_NumSpawnPoints)` with a pool bypass `if (g_NumSpawnPoints >= needed + needed/2) return false;` (player.c:269). For Solo Combat Sim, `g_Vars.mplayerisrunning == true` so `playerTrySelectPoolSpawn` is active — pool should give farthest-first dispersal IF built. Build gate: `spawnPoolBuildGlobal` runs only when `g_Vars.mplayerisrunning || g_NetMode != NETMODE_NONE` (playerreset.c:476). Most likely root causes — (1) Car Park's MP setup file has ≤1 INTROCMD_SPAWN pad AND a sparse waypoint graph, so the g_SpawnPoints[] fallback at playerreset.c:319 finds few valid entries; the pool then either doesn't build enough points or repeatedly picks the same entry as "farthest"; (2) all bots landing on the same pad then sharing `myaction = MA_AIBOTMAINLOOP` pathfind to the same scenario objective (king hill, ctc home pad, etc.) or default roam waypoint; (3) cascade from B-167 (non-skeletal props lacking bbox) leaving room/portal geometry unregistered so `bgFindRoomsByPos` fails for all spawn positions and bots fall back to the same void-recovery pad. Stuck-detection relocation at bot.c:1240 could also converge all bots on the SAME random-but-deterministic "max distance" waypoint (same rng seed → same target) — consistent with the "run to a specific spot" observation. **Partial mitigation + instrumentation shipped this session**: `botSpawnAll` now emits `SPAWN.DIAG:` dumps after the wave (per-bot pos/room) and a WARNING `SPAWN.DIAG: bot collision — bot[i] and bot[j] spawned %.0fu apart … spawn pipeline is collapsing to a single pad` when any pair is closer than 150u. Next playtest log will pin which of the three hypotheses is live for Car Park. **Full fix deferred** pending the diagnostic log from a Car Park repro. | `src/game/bot.c::botSpawnAll` (instrumentation); investigation targets `src/game/playerreset.c:319-441`, `src/game/player.c:230-513`, `src/game/spawnpool.c::spawnPoolBuildGlobal`, `src/game/mplayer/scenarios.c:802-817`, `src/game/bot.c:1136-1290` (stuck relocation) | Playtest: Solo Combat Simulator → Car Park → add 4-8 bots → start. Look at `pd-client.log` for: (a) `SPAWN: summary stage=0x3d npts=N …` — is N == 1 or N >= 8? (b) the new `SPAWN.DIAG: post-botSpawnAll stage=0x3d bots=N numpads=M` + per-bot pos lines, (c) the WARNING if the collision-variance check fires, (d) `SPAWN.TIER:` lines from the pool selector. Share log. |
| **B-173** | MED | **FIXED-PENDING-PLAYTEST 2026-04-18 (clever-kepler-412853)**. Playtest report (build `a70a457a`/v0.0.131): "Forge seemed to just load back into the Carrington Institute and then the menus were buggier. It did say forge on the top left i think." + zero `GRID:` / `FORGE:` log lines in the tester's client log despite the HUD reportedly showing "forge". The stage transition to CI Training is **by design** (S307 F0 spec, `pdgui_menu_forge.cpp::pdguiForgeStartSession` hard-wires `mainChangeToStage(STAGE_CITRAINING)` — the stage browser was deferred to F3 and hasn't shipped). What was NOT by design: the Main Menu dialog sits on the menu stack for the entire Grid session because the "The Grid" button on the main menu never pops its own dialog before triggering the stage change. Same symptom class as B-168 (Team Setup falling through to Main Menu): during the session the Main Menu's ImGui window repeatedly goes hidden→visible across stage transitions, fires `IsWindowAppearing` and calls `SetWindowFocus()`, stealing focus back on top of the Grid HUD/editor; when the user exits the session the Main Menu lands in a half-stale state with stacked input context and the wrong menupool generation — which matches the "menus were buggier" symptom. Fix: mirror the S367 `pdguiSoloRoomIsActive()` pattern — new `forgeSessionIsActive()` early-return at the top of `renderMainMenu` (after the existing `!menuDialogIsCurrent` sibling guard, before the theme reload). Main menu render is suppressed while forge is active so the Grid owns focus; once `forgeExitSession()` clears the session flag, the next frame re-enters Main Menu rendering with a clean `IsWindowAppearing` pass. The zero-log-lines issue is likely either that the tester clicked something other than "The Grid" (there is no other path that produces "forge" HUD text) or log truncation from an in-game crash — the code does log `GRID: launching session` on the button press, `GRID: enter requested`, and a per-state-transition `GRID: -> NORMAL` / `GRID: -> FREEFLY` via `forgemode.c`. No separate fix for the log-zero side until a second repro with `/showlog` confirmed on. | `port/fast3d/pdgui_menu_mainmenu.cpp` (+13/−0) | Playtest: Main Menu → click "The Grid" → CI Training loads → HUD shows `THE GRID -- NORMAL` + controls reminder → press F7 → `THE GRID -- FREEFLY` → fly around → press F7 → NORMAL → exit via main-menu/pause button or stage transition. Main menu should return in a clean state (no darkened backdrop, no stacked dialog, no input ctx mismatch). Log should show `GRID: launching session …`, `GRID: enter requested …`, `GRID: -> NORMAL …` immediately after stage load. |
| **B-172** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-18 (fervent-hertz-b709be)**. Broken/audio-only mods were leaking into the match manifest as `MANIFEST_TYPE_COMPONENT` entries and stalling the pre-match ready gate. Repro (build `a70a457a`, v0.0.131): user hosts a Combat Sim match on Ravine with several mods enabled -- `of-wolf-and-man` (audio-only, audio.ini), `memory-remains` (audio-only), `user.pk-yellow.ui-chrome` / `user.pokemon-yellow.ui-chrome` (UI Chrome mods). Boot logs show `ERROR: fsFileLoad: could not find file: .../mods/<id>/mod.json` for every audio-only mod (harmless -- originated from `modmgrParseBotNames` unconditionally calling `fsFileLoad` on mod.json). At match start, `MANIFEST-HOST` includes all four as COMPONENT entries. Server's `streamComponentToClient` resolves each via `assetCatalogResolve(mod_id)` -- mod IDs are not asset catalog IDs -- and logs `WARNING: DISTRIB: unknown catalog_id '<id>' requested`. Client marks each MISSING; ready gate never goes READY; user ESCs during the hang. ESC in Room falls through `mainEndStage` branch logic (no coop, no anti, `normmplayerisrunning=false` because the match never actually launched) and calls `endscreenPrepare()`, producing the `GAMELOOP.CAMPAIGN: endscreenPrepare stage=0x26` log line and stranding the user in a campaign-ended main menu with locked input. Fix (three layers): (1) `manifestBuild` and `manifestBuildForHost` (`port/src/net/netmanifest.c`) now skip mods where `!mod->valid \|\| !mod->has_modjson` -- audio-only mods are already represented via `MANIFEST_TYPE_AUDIO` playlist entries, so adding them as COMPONENT is redundant and stalls the gate; invalid mods can't be streamed. (2) `modmgrParseBotNames` (`port/src/modmgr.c`) early-returns when `!mod->has_modjson` to stop the boot-time `fsFileLoad ERROR` spam on every audio-only mod. (3) `mainEndStage` (`src/lib/main.c`) adds a new branch: if no match type is active (coop<0, anti<0, normmp=false) but `g_NetMode != 0`, log `GAMELOOP.CAMPAIGN: mainEndStage in networked lobby -- skipping endscreen, routing back to lobby` and skip `endscreenPrepare`. The room/lobby UI (or SVC_MATCH_CANCELLED → CLSTATE_LOBBY) handles the recovery state. Build clean 776/776. `PerfectDark.exe` 53,067,593 / `PerfectDarkServer.exe` 23,154,162. | `port/src/net/netmanifest.c`, `port/src/modmgr.c`, `src/lib/main.c` | Playtest: host a Combat Sim match on Ravine with at least one audio-only mod (audio.ini, no mod.json) and one UI Chrome mod enabled. Expect (a) no `ERROR: fsFileLoad: could not find file: .../mod.json` for audio-only mods at boot, (b) `MANIFEST-HOST: skipping mod '<id>'` NOTE lines for each audio-only / invalid / UI Chrome mod, (c) no `WARNING: DISTRIB: unknown catalog_id` on server, (d) ready gate advances to LOADING cleanly, match starts. Negative test: cancel a match start with ESC during `MANIFEST_PHASE_CHECKING`/`TRANSFERRING` -- expect `SVC_MATCH_CANCELLED` → return to Room screen, NOT `GAMELOOP.CAMPAIGN: endscreenPrepare`. If any future caller fires `mainEndStage` outside a real match, the new WARNING `GAMELOOP.CAMPAIGN: mainEndStage in networked lobby` surfaces it. |
| **B-171** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-18 (nice-jackson-62879e — S369 solo pause input ctx attach)**. Repro (tester log `Downloads/Perfect Dark 2.0/pd-client.log`): during a solo mission, ACTION_PAUSE fires → `MENUPOOL: acquired solo_mission_pause gen=1 def=... ctx=none(shared)` → NO subsequent `INPUTCTX: imgui_menu on_push` line → several seconds of `WARNING: DIAG fireVk: vk=531 player=0 NO BINDING FOUND in 1 active IMCs` → eventually `released solo_mission_pause`. Symptom the user sees: pause menu draws but controller / keyboard input doesn't work on it; mouse is "invisible" (still in gameplay relative mode) unless holding RMB brings the cursor back. Child DANGER dialogs (Abort Mission confirm) also unresponsive because no menu IMC is on top. Root cause: `renderPauseMenu` in `pdgui_menu_solomission.cpp` did `SetWindowFocus()` on IsWindowAppearing but never called `menupoolAcquireDialog(def, &g_CtxImGuiMenu)` to attach the input context to the live pool slot. `menuPushRootDialog → menuPushDialog` pre-acquires the slot with `ctx=NULL`; the renderer is responsible for re-acquiring with the real ctx (S300 pattern, used by every mppause / mpadvanced / mpsetup / etc. renderer). Solo pause was missed in the S300 pool migration. Fix: add `pdguiPlaySound(PDGUI_SND_OPENDIALOG)` + `menupoolAcquireDialog(menupoolDialogDef(dialog), &g_CtxImGuiMenu)` in the IsWindowAppearing block. Close paths unchanged — `menuPopDialog → menuCloseDialog → menupoolRelease` pops the owned ctx automatically. Build clean. | `port/fast3d/pdgui_menu_solomission.cpp:2620+` | Playtest: play a solo mission (Defection is fastest), press Start to pause. Pause menu appears and controller / keyboard D-pad + A/B works — Resume, Restart, Inventory, Settings, Abort all reachable. Abort → confirm dialog appears with Cancel + Abort reachable. Mouse cursor is visible (no need to hold RMB). Log should show `INPUTCTX: imgui_menu on_push -- g_ImcMenu activated` immediately after `MENUPOOL: acquired solo_mission_pause` and `MENUPOOL: attached ctx to active solo_mission_pause gen=1 ctx=imgui_menu`. |
| **B-170** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-18 (nice-jackson-62879e — S368 controller nav + scrollbar sweep)**. Three-part fix: (1) **Cross-panel controller nav** — `pdguiBeginActionBar` used `BeginChild(id, size, false, NoScrollbar|NoScrollWithMouse)` with no NavFlattened, so every docked action bar sat in its own nav scope. Buttons like "Back" / "Save" / "Confirm / Cancel" became unreachable by D-pad once focus was in the body. Fix: pass `ImGuiChildFlags_NavFlattened` to the BeginChild, making the bar a flat continuation of the parent for nav purposes. Systemic — covers every dialog that uses the action bar primitive. Also added NavFlattened to ~25 `##xxx_body` BeginChild layout containers across `pdgui_menu_{mppause,mpadvanced,mpsetup,botsetup,cheats,mpsettings,mainmenu,solomission,playerconfig,challenges,warning}.cpp` so body → action-bar nav is seamless. (2) **End Game modal popup** — `renderMpEndGameDialog` in `pdgui_menu_warning.cpp` rewritten on top of `ImGui::BeginPopupModal` instead of a parallel `Begin()` window. The popup modal owns input exclusively so D-pad / stick can reach Cancel + End Match even while the MP Pause Control window is live behind it; it auto-pops via `menuPopDialog()` when Esc/B or the "Cancel" click dismisses the popup. Scrim + PD-authentic red danger frame + keybinding hint row all preserved. (3) **Scrollbar visibility** — `pdguiApplyPdStyle` in `pdgui_style.cpp` now: ScrollbarSize 12 → 18, GrabMinSize 10 → 14, ScrollbarBg alpha 0x87 → 0xCC (track more opaque), ScrollbarGrab/Hovered now use the accent colour (`dialog_border2`) at 0xE0 / 0xF5 instead of the dimmer `border1`. Overflowing lists are now obviously scrollable without requiring hover. Build clean 774/774. `PerfectDark.exe` 53,170,487 / `PerfectDarkServer.exe` 23,138,784. | `port/fast3d/pdgui_layout.cpp`, `port/fast3d/pdgui_style.cpp`, `port/fast3d/pdgui_menu_warning.cpp`, `port/fast3d/pdgui_menu_{mppause,mpadvanced,mpsetup,botsetup,cheats,mpsettings,mainmenu,solomission,playerconfig,challenges}.cpp` | Playtest: (a) MP Pause Menu → End Game → press D-pad Left/Right — focus moves between Cancel and End Match. (b) Any dialog with a docked action bar (Settings, MP Setup, Cheats, etc.) — D-pad down from the body's last widget should land on the action bar button. (c) Any list that overflows (Cinema tracks, Cheats list, MP Setup arena list) — scrollbar is visibly wider + more contrast, visible without needing mouse hover. (d) Controller-only session — end-to-end Main Menu → MP Setup → Start Match → Pause → End Game → Confirm, no dead-end dialogs. |
| **B-169** | MED | **FIXED-PENDING-PLAYTEST 2026-04-18 (pensive-lovelace-832160)**. Bot right-click context menu in the Combat Simulator Room player list had no way to assign a bot to a team even after "Teams" was enabled in the room options — only Bot AI / Bot Type / Character / Duplicate / Re-roll / Remove. Fix: added "Team" submenu to the ctx popup, gated on `MPOPTION_TEAMSENABLED` + `isLeader`. Header label shows "Team: N" when all selected bots share a team, just "Team" otherwise. Eight entries (Team 1..8), each colored with the `kTeamColors` palette already used for row tints via `ImGui::PushStyleColor(ImGuiCol_Text, ...)`. Selecting applies to every selected bot, plays `PDGUI_SND_SUBFOCUS`, and sets `s_RoomSettingsDirty = true` so the change broadcasts via `SVC_ROOM_SETTINGS 0x78` in network mode. | `port/fast3d/pdgui_menu_room.cpp:1982+` | CI Room (solo or network as leader) → add a few bots → enable "Teams" in right-panel Options → right-click a bot row. "Team" submenu appears; pick Team 3; row tints to green. Select multiple bots (Ctrl+click), right-click, pick Team 1; all flip to red. Disable Teams — submenu disappears. In network mode, confirm other clients see the team change via SVC_ROOM_SETTINGS. |
| **B-168** | MED | **FIXED-PENDING-PLAYTEST 2026-04-18 (pensive-lovelace-832160)**. Combat Simulator Team Setup close fell through to the Main Menu instead of returning to the Room. Repro log (build `c9473d54`): `Room OPEN (solo=1)` → user opens Team Setup (`MENUPOOL: acquired mp_team_setup gen=1`) → press Back/B (`MENUPOOL: released mp_team_setup gen=1`) → `MENU_IMGUI: main menu OPEN` fires on the same tick; Main Menu draws on top of the Room (still rendering in background). Root cause: the "Combat Simulator" button in `pdgui_menu_mainmenu.cpp` calls `pdguiSoloRoomOpen()` but never pops the Main Menu dialog — Main Menu sits under the Room overlay for the whole CI session. When a child dialog (Team Setup, Change Agent, etc.) is pushed then popped, Main Menu's ImGui window goes hidden → visible, fires `IsWindowAppearing`, calls `SetWindowFocus()` and jumps to the top of the Z-stack over the Room. Fix: new `pdguiSoloRoomIsActive()` accessor in `pdgui_lobby.cpp`; `renderMainMenu` early-returns (after the existing `!menuDialogIsCurrent` sibling guard) while the solo Room is active so the Main Menu does not draw / focus-steal. When "Back to Menu" clears `s_SoloRoomActive`, the Main Menu reappears cleanly with a fresh `IsWindowAppearing`. | `port/fast3d/pdgui_lobby.cpp`, `port/fast3d/pdgui_menu_mainmenu.cpp` | CI Room (solo) → open Team Setup, close with Back/B/Esc → returns to Room (NOT Main Menu). Open Change Agent / Cheats / other child dialogs from CI same way — close returns to Room. Click "Back to Menu" — Main Menu reappears normally. Log should show `MENUPOOL: released mp_team_setup` followed by NO `MENU_IMGUI: main menu OPEN` line. |
| **B-167** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-18 (cranky-lalande — sibling `numparts<=0` guards in `propobj.c` bbox lookups mirrored the B-166 over-aggressive chokepoint reject)**. Build `211cce3e` / v0.0.129 — B-165/B-166 landed, game no longer AVs on CI Training (0x26), but every door/simple-prop renders at identity scale (tiny) or with no collision bounds. Playtest log: zero `MODELDEF: loaded torn`, but many `WARNING: SETUP: door modelnum X has no bbox node — using identity scale` (modelnums 404, 380, 68, 333, 334, 156, 421, 174) + `DOOR.DIAG: doorGetBbox — no bbox for modelnum=X` every door tick. Root cause: when S312 added the torn-reject to `modeldefLoad`, it ALSO added the same `numparts <= 0 \|\| numparts > 500` guard to both sibling bbox lookup functions `modeldefFindBboxNode` (propobj.c:1300) and `modelFindBboxNode` (propobj.c:1369). B-166 relaxed the chokepoint guard at `modeldefLoad` but **didn't propagate the relaxation to the two walker sites**. A non-skeletal prop (door / title logo / static geometry) with `numparts=0` now loads cleanly, but any call to `setupCreateDoor` → `modeldefFindBboxRodata` → `modeldefFindBboxNode` (or `doorGetBbox` / `objFindBboxRodata` → `modelFindBboxNode`) returns NULL because of the stale guard — caller falls back to identity scale (doors) or skips geometry (collision). Fix: remove `numparts <= 0` from both walker guards in propobj.c; keep `rootnode == NULL`, `numparts > 500`, `scale <= 0.0f`, and the 10000-step walker cap. Body/head numparts sanity still lives in per-caller guards (body.c:206 `body0f02ce8c`, menu.c:2076 `menuRenderModel`, player.c:1986) — those are the correct place and are unchanged. Also confirmed the other `numparts <= 0` sites are correct as-is: `modelcatalog.c::validateModeldef` (only called for `g_HeadsAndBodies` bodies/heads), `netmanifest.c::manifestEnsureLoaded` (only logs, gated on `MANIFEST_TYPE_BODY/HEAD`). Build clean 774/774. `PerfectDark.exe 53,169,357` / `PerfectDarkServer.exe 23,139,296`. | `src/game/propobj.c:1289-1305, 1354-1374` | Cold boot → CI Training (0x26): doors render at correct size (no tiny 1:1 identity-scale doors), simple props visible with collision, minimal/no `SETUP: door modelnum %d has no bbox node` WARNINGs (only for props that legitimately lack a bbox node in their tree, not every simple prop), minimal/no `DOOR.DIAG: doorGetBbox — no bbox` rate-limited warnings. Body/head rendering unchanged — still goes through body0f02ce8c guard. |
| **B-166** | CRITICAL | **FIXED-PENDING-PLAYTEST 2026-04-18 (eager-benz — over-aggressive `numparts<=0` guard in `modeldefLoad` chokepoint rejected non-skeletal models)**. Build `99f23645` (v0.0.128) still logged `MODELDEF: file N loaded torn` on stage 0x26 after the B-165 `fileLoadRomToNew` fix landed. Repro: Nintendo logo (file 221, `MODEL_NINTENDOLOGO`) fails on FIRST load at title time — no stale `loadedsize` cache possible, no catalogValidateAll pre-load. Log: `root=000001acd1ab5398 scale=1868.335 parts=0`. Rootnode is a valid pointer into the allocated buffer, scale is a real value, only numparts=0 triggers the reject. Same pattern for every CI prop (file 1879, 1372, 563, 167, 834, 257, 181, 143, 145 — each with distinct plausible scales). Root cause: my S323 guard at `src/game/modeldef.c::modeldefLoad` rejected `numparts <= 0` as "torn", but non-skeletal models (title logos, doors, barrels, static geometry) legitimately have `numparts=0`. `parts[]` is the skeletal animation part list; simple non-animated models have none. The original B-161 repro was a body with a dangling rootnode — `body0f02ce8c` (`src/game/body.c:203`) already has its own `numparts<=0` reject for bodies, which is where that check belongs. Relaxing the chokepoint to only reject `rootnode==NULL` or `numparts>500` keeps truly-torn rejects while letting simple props through. Fix commit `fa5cefca`, merged to dev as new build. | `src/game/modeldef.c:228` | Cold boot → title intro sequence: Rare logo (1376), Nintendo logo (221), PD logos (224/222/229/228) all render. Enter CI Training (0x26): no `MODELDEF: loaded torn` lines on simple props, no `FIX-B.2: modelnum N failed to load`, no AV at PC+0x1487fa. Bodies/heads still rejected by `body0f02ce8c` on genuine body-parts=0. |
| **B-165** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-18 (eager-benz — file.c `fileLoadRomToNew` stale `info->loadedsize` cache strips EXTRAMEM scratch on re-entry)**. Cache bug fix stands — still a real issue for repeated EXTRAMEM loads. But was NOT sufficient to fix the stage 0x26 torn-models symptom; see B-166 for the over-aggressive guard that was the actual cause of the user-visible symptom. This is the true root cause of B-161's "WHY models load torn in stage 0x26" open question. Repro: dev build `34636b53`, 64 MB memp, title loads OK, enter CI Training (0x26) from main menu — every body/head/prop in `MANIFEST-SP: applied diff — load=56 unload=0 keep=3` comes back torn (`MODELDEF: file N loaded torn — parts=0`). S323's `modeldefLoad` reject + S317/S327/S358 call-site guards correctly converted the AV into WARNING log lines, but the underlying load was still corrupted. Root cause: `src/game/file.c::fileLoadRomToNew` gated `info->loadedsize` recomputation on `loadedsize == 0`, but `fileLoad` overwrites `info->loadedsize` with the post-preprocess size on exit — stripping the `+0x8000` rzipInflate scratch buffer. On re-entry, `mempAlloc(info->loadedsize, MEMPOOL_STAGE)` allocates only the post-preprocess size; inside `fileLoad`, `scratch = dst + allocationlen - aligned(romsize)` falls inside the dst buffer. In-place inflate corrupts the deflate stream as the writer catches the reader → `rzipInflate` returns 0 → `romdataFilePreprocess` skips on `size==0` → dst stays zeroed → `modeldef->numparts=0`, `rootnode=NULL`, "loaded torn". Trigger chain: `catalogValidateAll()` at startup pre-loads every body/head through `safeModeldefLoad → modeldefLoadToNew → fileLoadRomToNew` (populates `loadedsize` for 151+ files), title stage uses `fileLoadToAddr` (resets `loadedsize=0` each call — immune), `mempResetPool(STAGE)` wipes allocations on CI transition but leaves `g_FileInfo[]` intact, CI manifest re-loads the same bodies/heads via `modeldefLoadToNew` → hits stale cache path → corruption. Fix: drop the `loadedsize==0` gate entirely; always recompute. Correctness for both EXTRAMEM and DEFAULT in one stroke. Cost: one extra `fileGetInflatedSize` (0x40-byte DMA + rzip header check) on cached loads — sub-ms at stage-transition time. Commits `e1205034` (Option A gated fix) → `b88df691` (gate removed, final). Merged `2e4f07d6`. Build clean 780/780 then 2/2 incremental. | `src/game/file.c:257` | Cold boot → main menu → CI Training (0x26). Expect no `MODELDEF: file N loaded torn` lines, no `FIX-B.2: modelnum N failed to load` warnings, no `ACCESS_VIOLATION PC=+0x1487fa`. Also test mission → endscreen → Next Mission (already-loaded files re-entered from fresh stage pool). |
| **B-164** | MED | **FIXED — 2026-04-17 (S345 — pensive-bell). `serializeModPlaylist` in `prefs_agent.c` passed `(s32)outmax - off` as `size_t`. When the playlist buffer fills, the negative result wraps to a huge size_t, causing `snprintf` to write without bound (UB/overflow). Fix: `s32 rem = (s32)outmax - off; if (rem <= 1) break;` then `(size_t)rem` to snprintf. Build-verified 775/775. Commit `d0f7ee13`. | `port/src/prefs_agent.c:157` | No playtest needed — only triggered if AUDIO_MAX_PLAYLIST * 65 bytes of catalog IDs are present. Fix is belt-and-suspenders. |
| **B-163** | CRITICAL | **FIXED — S317+S327 addendum 2026-04-17 (crash guard landed; root load cause still open)**. ACCESS_VIOLATION at PC+0x15f83a / +0x161258 during stage 0x26 (CI Training) setup. Repro confirmed on dev machine. addr2line on HEAD binary resolved the door-path stack: `setupCreateDoor setup.c:1128 → setupCreateProps setup.c:1654 → lvReset lv.c:548 → mainLoop pdmain.c:609`. Root crash (door path): `bbox = modeldefFindBboxRodata(g_ModelStates[modelnum].modeldef)` returned NULL (model has no BBOX node or modeldef failed to load via FIX-B.2 path), then `bbox->xmax` dereferenced at line 1128 without any NULL check. S317 fix: two-layer guard in `setupCreateDoor` — (1) early return with WARNING if `g_ModelStates[modelnum].modeldef == NULL` after `setupLoadModeldef`; (2) identity-scale fallback (xscale=yscale=zscale=1) if `bbox` is NULL. **S327 addendum (magical-zhukovsky)**: tester logs confirmed a second crash site on build `acdf4062` at PC+0x15f83a along the prop/object creation path. `setupCreateObject` (setup.c:400+) had FOUR unguarded `obj->model->scale` dereferences after `setupLoadModeldef` — when FIX-B.2 rejects a torn modeldef, `objInit` gracefully sets `obj->model = NULL`, then the caller AVs. Fix mirrors S317: early return w/ WARNING at top of `setupCreateObject` if `g_ModelStates[modelnum].modeldef == NULL`. Proactive sweep added three more crash-proofing guards: (a) `modelAllocateRwData` (lib/model.c) now NULL-guards modeldef + rootnode defensively — all callers inherit; (b) `netmsg.c` laptop-deploy network path guards `laptopDeploy` NULL return (torn modeldef path); (c) `body.c` `bodyAllocateModel` guards `headmodeldef` NULL in both random-head (func0f18e57c returning NULL slot) and specific-headnum (`catalogGetHeadModeldef` returning NULL) paths — prevents `bodymodeldef->rwdatalen += headmodeldef->rwdatalen` AV. Build-verified 774/774. **Still unknown**: WHY models in stage 0x26 load torn — suspect manifest-diff pipeline + late-add race at double-transition 0x30→0x26. With S323's `modeldefLoad` torn-reject at load time + S317+S327 call-site guards, every known AV converts to a WARNING log line. | `src/game/setup.c:400+`, `src/lib/model.c`, `port/src/net/netmsg.c:2213`, `src/game/body.c:238-276` | Playtest: launch game → Defection → Next Mission → CI Training (0x26). No AV at PC+0x15f83a or +0x161258. If a prop is missing, check log for `SETUP: object modelnum %d modeldef NULL after load` or similar to trace which catalog entry. |
| **B-161** | CRITICAL | **FIXED-PENDING-PLAYTEST 2026-04-17 (S323 root-cause fix at modeldefLoad)**. AV (0xc0000005) during Mission 1 Obj 1 → Obj 2 transition playthrough. Repro: Play `base:defection` (0x30) to completion, click "Next Mission" on endscreen, seamlessly load `base:investigation` (0x33), play ~38 seconds of normal gameplay, crash. Backtrace (addr2line against Downloads/PerfectDark.exe at ImageBase 0x140000000) resolves to: `modelFindBboxNode propobj.c:1312 → modelFindBboxRodata → doorGetBbox propobj.c:19426 → doorUpdateTiles → doorsCalcFrac → doorTick → objTickPlayer → propsTickPlayer → lvRender → mainTick`. AV at `node->type` deref inside the rootnode walker = some door's `model->definition->rootnode` was dangling / pointed at non-modelnode memory. Log at stage-load flagged a parallel warning (`WARNING: body0f02ce8c: truly invalid bodymodeldef for bodynum 108 (file 0x004b) ... parts=0 -- skipping`) — suggests the 0x30 → 0x33 manifest-diff / late-add pipeline leaves some modeldefs in a torn state. **Defensive layer (S308 + S312)**: NULL-guarded the bbox traversal chain so downstream tick code can't AV on torn modeldefs. **Root-cause fix (S323 — magical-mahavira)**: `src/game/modeldef.c::modeldefLoad` now validates the fully-promoted modeldef before caching. `rootnode == NULL` or `numparts ∉ [1, 500]` → log `MODELDEF: file %u loaded torn` at ERROR and return NULL so the torn struct never lands in `g_ModelStates[].modeldef` or `g_HeadsAndBodies[].modeldef`. `scale <= 0` is clamped to 1.0 with a WARNING (matches the `modelcatalog.c::validateModeldef` CLAMPED behaviour so AllInOneMods scales of 700-2000 still pass). `g_LoadType` is reset on the reject path so the next load isn't mis-preprocessed. Single chokepoint — every caller (`catalogGetBodyModeldef`, `catalogGetHeadModeldef`, `setupLoadModeldef`, `title.c`, `safeModeldefLoad`) inherits the guarantee. Existing callers already handle NULL as "missing asset" via FIX-B.2 (`setuputils.c`) / S308 bbox-chain guards / `body0f02ce8c` body-skip, so the fix converts any AV into a missing-prop log line. **S358 addendum (dev direct, `be0935da`)**: `titleInitNintendoLogo` / `titleInitRareLogo` / `titleInitPdLogo` now handle `modeldefLoad()` returning NULL (cascade to next intro mode or SKIP), plus exit/render paths guard against NULL `g_TitleModel*` so a failed intro model can no longer AV on subsequent frames. | `src/game/modeldef.c`, `src/game/propobj.c`, `port/src/net/netmanifest.c`, `src/game/title.c` | Playtest: Play Defection → Next Mission → Investigation. Walk around 1-2 minutes. Crash should not fire. If any modeldef was torn at load time, log will now show `MODELDEF: file %u loaded torn — parts=%d root=%p scale=%.3f -- rejecting` at the exact site instead of the defensive downstream warning. |
| **B-162** | MED | **FIXED-PENDING-PLAYTEST 2026-04-17 (S308 — dev direct)**. Solo pause menu hardened: (a) `IsWindowAppearing` now resets `s_RestartConfirm` + `s_RestartSelectIdx` (prior: only `s_PauseSelectIdx`) — prevented a Restart-Confirm overlay from persisting across pause opens if the previous close happened mid-overlay; (b) title "StageName: Status" falls back to "Mission N: Status" when the stage name langid doesn't resolve; (c) button labels for Inventory + Abort Mission fall back to hard-coded English when `langSafe` returns empty; (d) `Options` renamed to `Settings` to match user's pause-menu convention. Also: `soloMenuDialogPauseStatus` MENUOP_OPEN handler now zeros the entire `g_Briefing.objectivenames[]` / `objectivedifficulties[]` before repopulating (prior: only wrote up to `objectiveGetCount()`, leaving tail indices potentially stale from prior mission if setupCreateProps didn't run between). | `port/fast3d/pdgui_menu_solomission.cpp`, `src/game/mainmenu.c` | Playtest: Open pause menu across multiple missions (including cross-stage mission advance). Confirm: buttons are never blank, title never reads `: Status`, Restart Confirm overlay doesn't leak between opens, objectives list reflects ONLY current mission. |
| **B-160** | CRITICAL | **FIXED-PENDING-PLAYTEST 2026-04-16 (S304 — focused-roentgen)**. Menu pool slot leak → darkened/dead main menu on reopen + input authority desync. Playtest repro: open main menu, press Esc to close, reopen — fails ("pool slot [main_menu] already active" cascade) and subsequent opens render a darkened/stacked menu with broken input routing. Root cause: `pdgui_menu_mainmenu.cpp`'s top-level close handler called `inputCtxPopDeferred(&g_CtxImGuiMenu)` + `menuPopDialog()` as two separate steps — if anything between open and close already popped the legacy stack (depth already 0), `menuPopDialog` early-returned on underflow WITHOUT releasing the pool slot acquired by `menuPushDialog`. Every subsequent `menuPushDialog(&g_CiMenuViaPcMenuDialog)` was then rejected by the pool's structural dedup (S299). Fix (three layers): (a) `menuPopDialog`'s underflow branch now calls `menupoolReleaseAll` if any slots remain, recovering the leak class; (b) new `menuPoolConsistencyCheck()` runs per-frame from `menuTick` — if the legacy `g_Menus[]` stack is empty but pool slots are active, they're released with WARNING + `menupoolDumpActive()` for diagnostics; (c) main menu renderer migrated to the S300 pool-owned ctx pattern — IsWindowAppearing now calls `menupoolAcquireDialog(menupoolDialogDef(dialog), &g_CtxImGuiMenu)` (attaches ctx to the live slot), close path drops the manual `inputCtxPopDeferred` and lets `menuPopDialog` → `menuCloseDialog` → `menupoolReleaseDialog` cascade pop the ctx atomically. | `src/game/menu.c`, `src/game/menutick.c`, `src/include/game/menu.h`, `port/fast3d/pdgui_menu_mainmenu.cpp` | Open main menu from CI free-roam; close with Esc or controller B; reopen 3-5 times. Expect: no `pool slot [main_menu] already active` warnings, clean `MENUPOOL: acquired main_menu gen=N` → `MENUPOOL: released main_menu gen=N` pairs across each cycle. Then stress the underflow path: rapid open/close during transition, or force-close while main menu open (match start, stage change) — watchdog WARNING `MENU: watchdog — legacy stack empty but N pool slot(s) active` should be absent on healthy flow, and if it ever fires, confirms auto-recovery rather than sticky leak. |
| **B-145** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (S295)**. Jump suppressed on ascending slopes — grounded heuristic in `bondwalk.c:924-928` required `bdeltapos.y < 2.0f`; active slope climb left residual upward velocity above that ceiling, so `impulse > 0.0f && grounded` failed and the jump was eaten. Fix: relaxed thresholds — `groundgap < 20.0f`, `bdeltapos.y < 6.0f` (stays under FIXED_JUMP_IMPULSE 8.2f, so mid-jump still reads as airborne). | `src/game/bondwalk.c` | Walk up a Skedar ramp / any sloped surface; spam jump — every press should produce the standard hop impulse. |
| **B-146** | HIGH | **RE-FIXED-PENDING-PLAYTEST 2026-04-19 (friendly-kirch-688ea4)**. Original S295 fix ORed `OBJFLAG3_WALKTHROUGH` into the `weapon()` / `ammocrate()` / `ammocratemulti()` macro expansions in `props.h`, plus added an explicit set at `weaponCreateForChr` (network receive path). But setup-file pickups on **base-game maps** (Grid, Car Park, etc.) load from pre-compiled binary in ROM, not from source — the macro change only affects code recompiled from C source, so the `flags3` byte for those pickups predates the fix and has no WALKTHROUGH bit. On Grid, 2 ammo crates and 1 ground weapon remained standable like terrain. Regression fix: force `OBJFLAG3_WALKTHROUGH` at runtime inside `setupCreateObject` (`src/game/setup.c:430+`) when `obj->type` is `OBJTYPE_WEAPON` / `OBJTYPE_AMMOCRATE` / `OBJTYPE_MULTIAMMOCRATE`, matching the macro intent regardless of whether the setup data came from source build or ROM binary. The `propobj.c` auto-floor synthesizer check at 2321 is unchanged — it correctly suppresses the floor tile when WALKTHROUGH is set. | `src/game/setup.c` (setupCreateObject prologue), prior: `src/game/propobj.c`, `src/include/props.h` | Load Grid (or any base-game arena) via Combat Sim. Walk into the side of every ground weapon and every ammo crate — capsule should slide past / through, not ride up onto the top. Confirm the regression specifically on Grid's two ammo crates + one weapon pickup that Mike flagged. Forge-placed weapons/ammo were already correct (they route through `weaponCreateForChr` network path). |
| **B-147** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (S295)**. Jump clipped through ceilings along diagonal edges — the pre-move ceiling probe was a single point-probe at player XZ centre, so a capsule grazing the edge of a ceiling tile missed it entirely. Fix: sample `cdFindCeilingRoomYColourFlagsAtPos` at center + 4 points at ±capsule-radius in X and Z, take the min. Does not close all ceiling gaps (mesh-ceiling system still not wired; capsule sweep still WALL-only) but removes the most common clip. | `src/game/bondwalk.c` | Jump up against the edge of any angled roof (e.g. Skedar temple ramps, CI ceiling corners); head should stop at the ceiling instead of clipping through. |
| **B-148** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (S295)**. Carrington Institute tables (and any prop with a non-covering `MODELPART_BASIC_0065`) were solid at the rim but walkable through the middle — the PC auto-floor synthesizer was suppressed whenever a 0065 part existed, even when the part didn't cover the bbox. Fix: tightened the eligibility test — require the 0065 floor part to cover ≥ 50% of the bbox XZ extent at ymax; otherwise still emit the full-bbox auto-floor. Tracked via new `OBJH2FLAG_AUTOFLOOR` so `func0f069b4c` updates the tile correctly when 0065 is present but non-covering. | `src/game/propobj.c`, `src/include/constants.h` | Stand on top of a Carrington break-room table; player should not fall through into the rim hole. |
| **B-149** | MED | **FIXED-PENDING-PLAYTEST 2026-04-16 (S295)**. L4 radial spawn layer accepted last-resort candidates with zero validation — could commit spawn points into no-room, below-ground-sentinel, or inside geometry. Additional fixes in same S295 drop: added 4 lower diagonal rays to `s_RayDirs` (catch overhangs below the candidate), ground-sentinel guard in `spawnPoolValidateCandidate`, and switched the vertical clearance check from `CDTYPE_BG` to `CDTYPE_ALL` so props (crates, dropped weapons) are seen. | `src/game/spawnpool.c`, `src/include/game/spawnpool.h` | Multi-bot match on a small arena with lots of props; scan spawn points over 5 rounds — no mid-air, no stuck-in-wall, no stacked-on-pickup spawns. Cross-verify Issue #5 fixes compound with B-146 (pickups are now invisible to spawn validation via CDTYPE_ALL and also non-standable). |
| **B-150** | MED | **FIXED-PENDING-PLAYTEST 2026-04-16 (S295 — menu dead-input desync)**. (Originally tracked as B-145 in the menu-system investigation; renumbered to B-150 after B-145..B-149 were claimed by the S295 collision drop.) Seven fixes addressing the direction-A/B dead-input class from the menu-system investigation (2026-04-16). F1: dropped `g_PdguiActive` mirror in backend; debug-overlay state is now solely `inputCtxIsActive(&g_CtxDebugOverlay)`. F2: purged dead `#if 0` body inside `pdguiGameOverRender` (stray `inputCtxPush` that distorted audits). F3: added `inputCtxEndFrame` watchdog — rate-limited warning on deep stacks + bottom-not-gameplay, force-reset at `MAX_STACK-1` depth. F4: leak guard on `Begin()==false` in 9 renderers (agentselect, botsetup, cheats, mpadvanced, mppause, mpsettings, mpsetup, playerconfig, room, training FR-weapon). F5: replaced MpEndscreen aggressive per-frame push with fresh-entry frame-gap detection. F6: documented force-close contract in `inputctx.h`. F7: removed dead `s_MainMenuPushedCtx` bool and its writers. | `port/fast3d/pdgui_backend.cpp`, `port/fast3d/pdgui_menu_pausemenu.cpp`, `port/src/inputctx.c`, `port/include/inputctx.h`, `port/fast3d/pdgui_menu_{agentselect,botsetup,cheats,mpadvanced,mppause,mpsettings,mpsetup,playerconfig,room,training,endscreen,mainmenu}.cpp` | Build-verified pd + pd-server. Playtest: open/close main menu, pause menu, MP endscreen; verify no dead-input state on rapid toggles and on alt-tab boundary. Log should show no `INPUTCTX watchdog:` warnings under normal play. |
| **B-151** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (festive-saha — match-pipeline GAP-1)**. (Originally tracked as B-145 in the match-pipeline investigation; renumbered to B-151 after B-145..B-150 were claimed.) Deep Sea co-op next-mission branch in `menutick.c` called `mainChangeToStage()` without `manifestClear(&g_ClientManifest)`. Matches the SP-13 / Bug A crash pattern exactly — stale MP manifest diffed by `manifestMPTransition()` → AV. Fix: `manifestClear(&g_ClientManifest)` inserted immediately before `mainChangeToStage()`. | `src/game/menutick.c:~598` | Online co-op campaign: complete Deep Sea co-op mission with endscreen-driven advance; no crash on stage transition. |
| **B-154** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (silly-jepsen — textbox input leak to game actions)**. Playtest repro: open any menu with an `ImGui::InputText` focused (Nine-Slice Chrome "Mod Name", Audio Mod filters, Skin Editor name, connect-code field). Typing `E` there fires `ACTION_USE` (or whichever action the key is bound to) on the background player — letters leak into the game. Root cause: `pdguiProcessEvent` calls `actionmapDispatch(ev)` unconditionally on `SDL_KEYDOWN/KEYUP`. The `fireVk` gameplay-IMC gate (`gameplayInputSuppressed()`) only skips `g_ImcGameplay`/`g_ImcVehicle`; menu and debug IMCs still see the scancode, so mappings bound in those contexts still fire during text entry. Fix: gate `actionmapDispatch` on `ImGui::GetIO().WantCaptureKeyboard` for KEYDOWN/KEYUP. Esc and Enter (Return / KP_Enter) still pass through so InputText submission and dialog-cancel actions work unchanged. Event is also consumed (`return 1`) when ImGui eats the key, so nothing else downstream (legacy paths, context dispatch) sees it. | `port/fast3d/pdgui_backend.cpp` | Playtest: open Modding Hub → Nine-Slice Chrome, click in the Mod Name box, type "Esteemed" / "Weapon" — no ACTION_USE / ACTION_LOOK_UP / etc. fires on the background player. Esc still closes; Enter/Return submits. Re-verify with the Skin Editor name field and any MP connect-code entry.
| **B-155** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (silly-jepsen — chrome mod absent from Mods list without restart)**. Nine-Slice Chrome "Save as Mod" writes `mods/UI Chrome/<slug>/mod.json` + tga, registers the chrome style via `pdguiThemeRegisterChromeModDir`, then reports "Saved & activated" — but the new mod didn't appear in Modding Hub → Mods until the next full `modmgrInit` at startup. Root cause: `modmgrScanDirectory()` is static and only runs during `modmgrInit`; no hot-rescan API existed. Fix: new public `modmgrRescanDirectory()` in `modmgr.c` — snapshots `{id, enabled, loaded}`, calls `modmgrScanDirectory()`, sorts, restores flags from the snapshot so pending in-memory toggles survive. Chrome save path now calls `modmgrRescanDirectory()` + `modmgrSetEnabled(idx, 1)` (for the new `user.<slug>.ui-chrome` id) + `modmgrSaveConfig()` + `pdguiModManagerRefreshSnapshot()` so the Mods tab shows the entry, enabled, pre-persisted to pd.ini. | `port/include/modmgr.h`, `port/src/modmgr.c`, `port/fast3d/pdgui_menu_moddinghub.cpp` | Playtest: Nine-Slice Chrome → load image → Save as Mod → open Modding Hub Mods tab — new mod appears checked/enabled immediately; quit + relaunch → mod is still enabled (config persisted). Also verify existing pending enable/disable toggles on other mods survive the rescan (should not be reverted).
| **B-156** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (silly-jepsen — chrome mod missing from Video dropdown even after enable+save)**. After "Save as Mod", the new chrome texture never appeared in Settings → Video → UI Chrome Style alongside Procedural / Classic (base-game), even after toggling enabled + applying + restarting. Root cause: the chrome-manifest tokenizer `cjson_next` in `port/fast3d/pdgui_theme.cpp` consumed only digits for NUMBER tokens — no fractional part, no exponent. The mod.json the Nine-Slice tool writes includes a `chrome_authoring` block with `border_scale: 1.000` and `inset_pct` floats. When the top-level parser hit `chrome_authoring` it called `cjson_skip_value`, which entered the LBRACE branch; on the first `.` inside `1.000` the tokenizer returned `CJT_ERROR`, `cjson_skip_value` bailed early, and the outer parser resumed from the wrong position. `components` was never seen, `has_nineslice`/`out_tex_id` stayed empty, and `s_parseChromeManifest` returned 0 — so `pdguiThemeRegisterChromeModDir` + `s_scanModChromeStyles` both silently dropped the mod. The failure was restart-proof because the same parser runs at startup. Fix: extend `cjson_next`'s NUMBER branch to consume `.digits` and optional `[eE][+-]?digits` so `cjson_skip_value` can skip over floats inside unknown top-level objects. `cjson_int` still uses `strtol` (unchanged) — all chrome-manifest int reads are pure integers so this is safe. | `port/fast3d/pdgui_theme.cpp` | Playtest: Nine-Slice Chrome → Save as Mod → Settings → Video → UI Chrome Style dropdown now lists the new mod name between Procedural and any other discovered chrome style. Select it — the chrome applies live. Restart the game and confirm the selection persists and the style is still in the list.
| **B-152** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (vigilant-robinson — stuck WASD after menu close)**. Playtest repro: back out of main menu, press+release W/A/S/D, character keeps walking that direction indefinitely. Re-opening and closing the menu clears it. Root cause: `actionmapPollFrame()` WASD→AXIS_MOVE synthesis block only writes `s_State[0][ACTION_AXIS_MOVE_X/Y].value` when at least one WASD key is held. When no controller is enumerated on player 0 (keyboard-only setup, or controller not plugged in for P0 slot), the controller-polling branch at line 803 is skipped, so nothing else resets `.value` each frame. After the user presses and releases W, the synthesis wrote `.value=1.0` during press; on release the block short-circuited (`mx=my=0`) and `.value` stayed at 1.0. The feedback loop: next frame reads stale `.value=1.0`, sets `analogStickActive=true`, skips synthesis — axis pegged forever. Menu open clears AXIS_MOVE via the `menuActive` branch at line 860, hence the "reopen fixes it" symptom. Fix: (a) track `p0CtrlDroveAxis` — true only when the controller actually wrote AXIS_MOVE this frame — and gate `analogStickActive` on it, so stale `.value` never suppresses the synthesis; (b) always assign the computed `mx/my` (including zero) when `!analogStickActive`, so WASD release writes `.value=0.0` and clears the stuck axis. | `port/src/actionmap.cpp` | Playtest: keyboard-only; open main menu → close → press+release W, A, S, D individually. Character should stop moving on each release. Verify the fix survives alt-tab, menu cycling, and mixed keyboard+controller input. |
| **B-153** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-16 (vigilant-robinson — double main menu on reopen)**. Playtest repro: close main menu, reopen with Esc/controller — the new instance renders overlaid (two copies) or with an unusually dark backdrop. Root cause: `menuPushDialog()` auto-opens every `dialogdef->nextsibling` at the same layer (menu.c:1553-1576). `g_CiMenuViaPauseMenuDialog`'s nextsibling is `g_CiOptionsViaPauseMenuDialog` — so pushing the main menu also loads the CI Options sibling. `menuRenderDialogs` renders the "other" sibling alongside curdialog whenever `type != 0 || transitionfrac >= 0`; both dialogdefs route through `pdguiHotswapCheck` and each queues its renderer (`renderMainMenu` for CiMenu, `renderCiSettingsRedirect` for CiOptions). CiSettings renderer calls `pdguiPopupDarkenBehind(0.55f)` — compounding with the main menu's own frame, producing "darker background / two copies overlaid". Normal operation has `transitionfrac=-1` → sibling skipped, but under rapid close+reopen the transition state can land in the visible window. Fix: new `menuDialogIsCurrent(dialog)` predicate in `menu.c` (scans `g_Menus[i].curdialog` across player slots) + guard at the top of `renderMainMenu` and `renderCiSettingsRedirect` — early-return when invoked for a sibling preload. Renderers consumed (return 1) so legacy fallback is not triggered. | `src/game/menu.c`, `src/include/game/menu.h`, `port/fast3d/pdgui_menu_mainmenu.cpp` | Playtest: open main menu → Esc to close → Esc to reopen immediately (< 1s). Single menu copy, normal backdrop. Also test swipe transitions to verify legitimate sibling renders during active swipe are still skipped by this guard (intentional — the ImGui hotswap world doesn't swipe; confirm no visual regression on CI Options swipe paths, should there be any). |
| **B-144** | MED | **FIXED-PENDING-PLAYTEST 2026-04-16 (S294)**. `matchConfigAddBot()` and `SVC_ROOM_SETTINGS` client read both called `matchConfigMaxBotsForHumans(1)` — hardcoded human count of 1 let the cap sit too high in 2-8 human matches. Fix: new `matchConfigCountHumans()` helper, both callsites switched. SVC_ROOM_SETTINGS client rebuild also switched to `matchConfigChooseBotTeam(2)` (matching host) and now zeros stale slot entries beyond the new bot count. | `port/src/net/matchsetup.c`, `port/src/net/netmsg.c` | 2-4 humans + bots room; add bots up to cap and confirm cap respects actual human count; team assignment matches host after host-initiated bot count change. |
| **B-141** | MED | **FIXED-PENDING-PLAYTEST 2026-04-17 (S323 — magical-mahavira, three-tier pacing)**. Audio skips / pauses, not reproducible on demand — `~200` underruns per 30s window per the S301 summary. Root cause identified: original N64 amgrFrame brake threshold of **1100 stereo samples = 50ms cushion** was smaller than typical PC main-thread hitches (50–100ms), so queue drained below the 128-sample SDL underrun threshold routinely. Producer/consumer math at 22050 Hz stereo × 60 Hz NTSC is 368 vs 367.5 stereo samples/frame = net +0.5/frame drift, so from a cold queue it took ~36 seconds to reach the 1100 target — meaning startup + hitch-recovery were both slow. **Fix**: three-tier production pacing in `src/lib/audiomgr.c::amgrFrame`. (tier 1) queue > **3000** → push 184 brake. (tier 2) queue 2500–3000 → push 368 steady (legacy drift rate). (tier 3) queue < 2500 → push **736** fast-fill (4 naudio chunks of 184; fits in the 3072-byte info->data allocation). Steady state now oscillates 2800–3000 (~128–136ms cushion, 3× original). Cold fill to 2500 takes 7 frames (~115ms) instead of 36 seconds. PAL retains its 552/frame rate (already sufficient for 50Hz). | `src/lib/audiomgr.c` | Playtest: play any arena for 60s, tail `pd-client.log` for `AUDIO[B-141]: 30s summary` lines. Expect `underruns=0` steady state; any residual underruns correlate with actual main-loop stalls > 135ms. `nullProducer` count should stay zero if `sndInit` ran. `buffered(samples) min` should now hover 2800–3000 instead of 900–1100. |
| **B-126** | HIGH | **MITIGATED S234 (FIX-A)**. Silent crash ~8 min in MP. Root cause: deep AI chains exhaust 8 MB stack → `-fstack-protector-strong` canary smash → SIGABRT → handler double-faults on corrupt stack → silent death. A.1 stack depth cap, A.2 generation tokens, A.3 SIGABRT handler hardened, A.4 per-chr stack watermark. | `chr.c`, `chraction.c`, `crash.c`, `main.c` | 31-bot repro test to confirm crash eliminated. |
| **B-112** | HIGH | **MITIGATED S234 (FIX-A). ROOT CAUSE CONFIRMED S357.** Chr rbx corruption in `chraTick` during 31-bot matches. Two classes: (1) stack depth — single-bot `chraTick→chraiExecute→action dispatch→collision/combat` chain can exceed available stack in extreme 31-bot combat scenarios; `chraiExecute` is iterative (not recursive), AI commands never call `chraTick`, all array bounds correct. FIX-A.1 (512 KB guard) skips bots when stack is low. (2) stale pointer — bot respawn never frees/reallocates chr slots (`botSpawn` teleports in place), so class 2 is rare; FIX-A.2 generation tokens detect the edge case. B-12 Phase 3 cleared: participant pool sized to `MAX_MPCHRS=40`, chr slots sized to `PLAYERCOUNT()+numchrs+10=42`, `AVOID_UB=1` guards the known N64 `mpCalculateAwards` `playerrankings[1]` overflow. No new fix needed. | `chraction.c`, `chr.c`, `types.h`, `crash.c` | 31-bot repro test to confirm FIX-A eliminates crashes in practice. |
| **B-142** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-13** (merge `4d1e13c1`). False kill counter at match start — root cause: `mpPlayerGetIndex(NULL)` returned 0 when `g_MpAllChrPtrs[0]==NULL` (NULL==NULL match at index 0). Fix: `if (chr == NULL) return -1;` early guard. Defensively correct regardless of whether it is the sole root cause. | `src/game/mplayer/mplayer.c:3734` | Fresh 32-bot Chicago match, idle 30 s, open pause → kill counter should be 0/0. |
| **B-18** | MED | **LIKELY FIXED S243 (FIX-C.1)**. Pink sky on Skedar Ruins — cross-frame GBI env-color leakage into cloud LERP. FIX-C.1 canonical state reset at frame start resets env color to white before `skyRender()`. | `lv.c` | Skedar Ruins playtest — sky should be blue-purple, not pink. |
| **B-19** | MED | **PARTIAL FIX (S125 F.1 anti-repeat)**. Bot spawn stacking on Skedar Ruins — all bots at same pad. | `player.c` | Skedar-specific playtest. |
| **B-21** | MED | **CLOSED-TENTATIVE 2026-04-13**. Menu double-press / Esc registers multiple times. Two-layer fix: S124 push_tick 100 ms grace in `inputCtxShouldSuppressKey`, S208 150 ms `MAIN_MENU_CLOSE_GRACE_MS` guard + explicit `io.AddKeyEvent(Escape, false)` on appear. | `inputctx.c`, `pdgui_menu_mainmenu.cpp` | Next playtest; promote to FIXED if no recurrence. |

### Open without B-number (2026-04-13 Chicago playtest, carried forward)

These are in `tasks-current.md` punch list and `context/scratch/archive/2026-04-13/README.md` — promotion to formal B-numbers deferred until the 2026-04-13 drop is playtest-verified (several may have already cleared).

- **Bug C (post-game endscreen partial render)** — scrim + title-bar rectangle render but content body is invisible. Six hypotheses in `scratch/archive/2026-04-13/session-state-endgame-crash.md` §4c. **Instrumented S295 (festive-saha)** + **ENHANCED S301 (confident-chaum)**: `renderMpEndscreen` now emits comprehensive `ENDSCREEN.DIAG:` lines tagged for grep — fresh-entry with `g_MpPlayerNum / g_NetMode / challengeResult`, full geometry dump (`sf / menu / pad / win / contentAvail / scroll`), body-child geometry (`contentW / contentH / innerCRA`), rankings-count (`rankings built count=N teamsMode=…`), awards-path (SHOW|SKIP with award strings + medals bitfield), and actions-section checkpoint. Capped at 80 prints per match via `ENDSCREEN_DIAG_MAX_PRINTS` so oscillation can't flood the log. Existing `ENDSCREEN:` LOG_WARNINGs on `Begin=false` and `contentH clamped` retained. Next repro should produce enough log evidence on the first frame to pick which of the six hypotheses fired.
- **Bug D (invisible networked bots on Chicago)** — bots visible on minimap and audible but not rendered in world. Likely chr generation token mismatch (FIX-A.2 area); may have cleared with recent drops. **Instrumented S301 (confident-chaum)**: `CHR.DIAG:` prefix on three points — `botmgrAllocateBot` logs chrnum / body / head / body_id / head_id at entry and the returned model ptr (with WARNING if NULL), `bodyAllocateModel` logs body_canon / head_canon lookups (WARNING if catalog ID is NULL for a non-negative bodynum/headnum), `botSpawn` before (`BOT.SPAWN` breadcrumb + `CHR.DIAG: botSpawn …`) and after (`CHR.DIAG: botSpawn DONE …`) with the full invisible-state check: `WARNING chrnum=… is INVISIBLE after botSpawn (model=NULL|HIDDEN|VOID(-1))`. An invisible bot now leaves an explicit fingerprint identifying which field was the cause.
- **Chicago silent crash ~9 s** — needs VEH log + symbolify. **Instrumented S301 (confident-chaum)**: new `port/src/crashbreadcrumb.c` ring buffer (256 slots × 112 bytes, static BSS) dumped by VEH / UEF / SIGABRT / Linux `sigaction` paths directly into `pd.crash.log` / `pd.log` / `pd-client.log` under a `CRASH.DIAG: breadcrumb ring dump follows:` heading. Push sites: mainTick heartbeat per frame, lvTick per frame, chraTickBg per frame, chraTick per chr (slot + action + race), bwalkTick per player (pos + room + floorroom), botSpawn, botmgrAllocateBot, mainChangeToStage, SVC_STAGE_START send/receive. Ring holds ~130 ms of CHR.TICK pushes (32 bots × 60 Hz = 1920/s), enough to identify which chr + action was being ticked at the moment of crash. No dependency on ordinary log output — even a crash that corrupts sysLogPrintf will still dump the ring.
- **Airbase Start-Match 0xc0000005 / no-response** — log shows manifest OK but no SVC_STAGE_START. May share root cause with Bug A (fix shipped `d37e9677`). **Instrumented S301 (confident-chaum)**: `MATCHSTART.DIAG:` tag on every decision point in the match-start chain — `matchStart` entry / pre-mpStartMatch / post-mpStartMatch, `netServerStageStart` entry + send outcome (`SVC_STAGE_START sent to room 0x… (combat sim)` or `…broadcast to all clients`), `netmsgSvcStageStartWrite` begin, `netmsgSvcStageStartRead` begin. Each bail has an explicit reason message (`BAILED reason=empty_stage_id`, `SVC_STAGE reject — client state=… not in LOBBY/GAME/PREPARING`). With both sides tagged, one Airbase repro is enough to pin the decision point that stopped progress.

---

## Fixed Bugs — Compact Reference (newest first)

| ID | Description | Fixed |
|----|-------------|-------|
| B-201 | Device Training (DT) and Holo Training (HT) drill-in broken by the same pool-dedup collision as B-200. `g_DtListMenuDialog` + `g_DtDetailsMenuDialog` (and `Ht*` counterparts) all shared `MENU_TYPE_TRAINING`; Details is pushed on top of List via `menuPushDialog` so the second push was rejected silently and the Details screen never appeared. Fix: split into `MENU_TYPE_DT_LIST` / `MENU_TYPE_DT_DETAILS` / `MENU_TYPE_DT_RESULT` and `MENU_TYPE_HT_LIST` / `MENU_TYPE_HT_DETAILS` / `MENU_TYPE_HT_RESULT`, mirroring the FR split. Completed + Failed share RESULT (mutually exclusive per flow). Bio / Hangar stay on `MENU_TYPE_TRAINING` (no stacked Details dialog at this time). | 2026-04-19 -- menupool.h, menupool.c |
| B-200 | Firing Range menu accepted weapon click but silently did nothing (CI firing range non-functional). Root cause: `g_FrWeaponListMenuDialog`, `g_FrTrainingInfoPreGameMenuDialog`, `g_FrCompletedMenuDialog`, `g_FrFailedMenuDialog` all registered as `MENU_TYPE_TRAINING`. The FR flow legitimately stacks Weapon List -> Difficulty -> Pre-Game Info, so the second push was rejected by pool dedup (`pool slot [training] already active`). Fix: split FR sub-dialogs into dedicated pool types (`MENU_TYPE_FR_WEAPON_LIST`, `MENU_TYPE_FR_DIFFICULTY`, `MENU_TYPE_FR_INFO`, `MENU_TYPE_FR_RESULT`) following the MP_SETTINGS precedent. Registered `g_FrDifficultyMenuDialog` (was missing). Addressed the S300 concern at tasks-current.md:1547. **Follow-up (DT/HT)**: fixed in B-201. | 2026-04-19 -- 57c33870 -- menupool.h, menupool.c |
| B-161 sweep | **B-161 class — proactive bbox/modeldef NULL deref sweep (S318).** Comprehensive audit of all `modeldefFindBboxRodata`, `modelFindBboxRodata`, `objFindBboxRodata` callsites. Five unsafe sites found and fixed: (1) `bondbike.c:bbikeHandleActivate` — early return if `objFindBboxRodata` returns NULL (can't compute dismount distances); (2) `chr.c:3050` — struct copy `thing->bbox = *bbox` guarded with zero-init fallback; (3) `propobj.c:glassDestroy` — `shardsCreate` now skipped if bbox NULL (object removed without shards); (4) `propobj.c:doorIsObjInRange` — returns `doorIsPosInRange(..., 0, isbike)` (point check) if bbox NULL; (5) `propobj.c:func0f069850` — `objCalculateGeoBlockFromBboxAndMtx` now skipped with empty block fallback if bbox NULL and rodata19 NULL. All other callsites confirmed safe (NULL-guarded or passing through S308-patched `objGetLocal*`/`objGetRotatedLocal*` helpers). | 2026-04-17 — S318 — bondbike.c, chr.c, propobj.c |
| Bug B | Countdown-cancel-on-room-close — server `s_ReadyGate` kept ticking after all clients left the room, firing `mainChangeToStage()` into an empty room; clients stuck on "GO!". Fix: `netReadyGateOnClientLeft()` + `netReadyGateAbortForRoom()` called from `roomLeave()` in `room.c`; defensive "room missing" guard at top of `readyGateTickCountdown()`. No protocol bump. See systemic-bugs.md SP-14. | 2026-04-14 — S254 — netmsg.h, netmsg.c, room.c |
| B-143 | End-Game-Crash 0xc0000005 — `netDisconnect()` called `mainChangeToStage(STAGE_CITRAINING)` while `g_ClientManifest` still held the match manifest → `manifestMPTransition()` diffed torn-down entries → AV. Fix: `manifestClear(&g_ClientManifest)` before stage change (pattern-match to F-0.4 / L1-1). Also: controller-friendly modal confirm UX. See systemic-bugs.md SP-13. | 2026-04-13 — d37e9677 — net.c, pdgui_menu_warning.cpp |
| B-134 | Chicago MP fire-escape railing trap — `spawnPoolRaycastBudget()` 5-unit near-hit threshold passed railings inside player capsule (30 units). Fix: threshold = `SPAWNPOOL_CAPSULE_RADIUS` (30.0f). L1 rejection log upgraded to LOG_WARNING. | 2026-04-13 — 0b44b2b8 — spawnpool.c, spawnpool.h |
| B-140 | Mod music playlist auto-advance never broadcast — `NETMODE_SERVER_AUDIO` was `#define`d 2 (= NETMODE_CLIENT) instead of 1 (= NETMODE_SERVER); `audioNetworkMusicTick()` fired on clients not host. | S249 / 2026-04-13 — e13c2d1f — audio.c:17 |
| B-139 | Chicago countdown 3-2-1 overlay stayed visible on main menu after disconnect. Fix: `pdguiCountdownReset()` called from mode→NONE in `pdgui_lobby.cpp`. | S248 / 2026-04-13 — 16de65e6 — pdgui_lobby.cpp, pdgui_bridge.c |
| B-138 | Mod Manager Apply Changes disabled when only mod-level toggles dirty; no unsaved-changes guard on close. Fix: `modmgrIsDirty()` check + `Apply Changes*` label + `Unsaved Changes` modal. | S248 / 2026-04-13 — 16de65e6 — pdgui_menu_modmgr.cpp |
| B-137 | Room screen title bar always static "Room" regardless of room name. Fix: `g_RoomCache`/`g_LocalRoomId` lookup; title shows "Room: <name>". | S248 / 2026-04-13 — 16de65e6 — pdgui_menu_room.cpp |
| B-136 | All mods had to be re-enabled every run — `modmgrSetEnabled()` never followed by `modmgrSaveConfig()` in mod-level toggle paths. Fix: save after each toggle site. | S248 / 2026-04-13 — 16de65e6 — pdgui_menu_modmgr.cpp |
| B-135 | `base-ui` mod shows [INVALID] — `mod.json` missing required `"id"` field. Fix: added `"id": "base-ui"`. | S248 / 2026-04-13 — 16de65e6 — mods/base-ui/mod.json |
| B-132 | Saved action bindings populated UI but not live input — `actionmapLoadBinds` applied saved string to every IMC with `has_mapping[a]` set, cross-contaminating menu/pause/text-input IMCs. Fix: `break` after first parseBindStr match, matching first-match-wins serialiser semantics. | 2026-04-11 (Opus 1M playtest batch) — actionmap.cpp |
| B-131 | Double-menu reopen in CI — reopening main menu after back-out immediately closed on next frame. Fix: 150 ms `MAIN_MENU_CLOSE_GRACE_MS` guard on ESC/B close + explicit `io.AddKeyEvent(Escape/GamepadFaceRight, false)` on appear to force prev=false/cur=false. | 2026-04-11 (Opus 1M playtest batch) — pdgui_menu_mainmenu.cpp |
| B-130 | Theme editor X / Close / click-outside all ineffective — hand-rolled overlay + per-frame `SetNextWindowFocus()` fought ImGui. Fix: full rewrite with native `BeginPopupModal`; X via `&p_open`, click-outside via rect-test (suppressed by IsAnyItem{Active,Hovered}), Escape via modal default. Side effect: fixes ColorEdit4 sub-popup dismissal. | 2026-04-11 (Opus 1M playtest batch) — pdgui_menu_theme_editor.cpp |
| B-129 | Agent save path broken — `saveInit()` never called; `s_SaveDir` stayed empty; saves wrote to `/agent_smarch.json` (drive root). `titleSetNextStage(0x30)` retried current stage because `besttimes[]` never persisted. Fix: `saveInit()` call after `saveMigrateInit()` in main.c and after `configInit()` in server_main.c. | S198 / 2026-04-10 — 3fc345bf — main.c, server_main.c, savefile.c |
| B-128 | Sky tearing / transparent sky tris — cloud rendering inherited stale `G_RM_AA_XLU_SURF` from sun flares. Point fix: `gDPSetRenderMode(OPA_SURF)` at sky.c:1244. Systemic defense: FIX-C.1 canonical GBI state reset at frame start in lv.c eliminates SP-10 cross-frame state leakage class. | 2026-04-10 + S243 (FIX-C.1) — sky.c:1244, lv.c:1407 |
| B-127 | WASD held not registering — KBM axis synthesis set `.value` but not `.held`. S188 deeper fix removed device gate entirely. | S187 / S188 — actionmap.cpp:793 |
| B-125 | Weapons not spawning in online MP — `spawn_weapon_id` catalog string never serialized in CLC_LOBBY_START or SVC_STAGE_START. | S187 — netmsg.c |
| B-124a | Escape closes pause menu immediately — parallel `inputKeyJustPressed(VK_ESCAPE)` paths in bondmove/player fired alongside `actionPressed(ACTION_PAUSE)`; no cooldown guard. Fix: removed parallel paths, added close-cooldown guard. | S188 — bondmove.c, player.c, pdgui_menu_pausemenu.cpp |
| B-124b | Controller can't navigate menus — `pdguiDriveImGuiNav()` injected `ImGuiKey_Gamepad*` but `NavEnableGamepad` disabled. Fix: map to keyboard-nav keys (UpArrow/Enter/Escape etc.) which work with `NavEnableKeyboard`. | S188 — pdgui_backend.cpp |
| B-124c | WASD/left stick only works when another input fires — KBM synthesis gated on `s_LastDevice==KBM`; gamepad noise events flipped device. Fix: removed device gate; WASD synthesis always runs. | S188 — actionmap.cpp |
| B-124d | Right stick moves camera while menu open — `actionmapPollFrame()` polled SDL sticks regardless of context. Fix: context check; zero gameplay axes when top ctx != gameplay. | S188 — actionmap.cpp |
| B-123 | "Next Mission" reloads same stage — endscreen advance used `missionSetStagenum()` (passing stagenum as runtime_index, B-120 pattern). Fix: new `missionSetStageByCatalog()` resolves from catalog. | S167 (M0.1a) — pdgui_menu_endscreen.cpp, mainmenu.c |
| B-122 | Endscreen mouse unresponsive — deferred flush in `pdgui_backend.cpp` only checked `pauseMenuOpen`, missed endscreen's `g_CtxImGuiMenu`. Fix: flush guard → `!pdguiIsActive()`; `inputCtxSyncMouseMode()` per-frame in endFrame; manual SDL calls removed from endscreen. | S170 (M1.2) — pdgui_backend.cpp, inputctx.c, pdgui_menu_endscreen.cpp |
| B-121 | Failed/complete mission endscreen not interactive — `renderSoloEndscreen()` released SDL grab but never pushed `g_CtxImGuiMenu`. Fix: push context on appear, matches pause pattern. Same fix for MP endscreen. | S166 — pdgui_menu_endscreen.cpp |
| B-120 | Wrong stage loaded for solo — `catalogIdByRuntime(ASSET_MAP, stagenum)` passed stagenum instead of stage-table index. Fix: convert via `bgGetStageIndex()` first. | S166 — pdgui_menu_solomission.cpp, mainmenu.c |
| B-119 | stagenum=0x00 crash on solo mission — `sm_missionconfig` shadow struct missing `stage_id[64]` field, all field accesses at wrong offsets; `stagenum` write went to `stage_id[0]`. Fix: add field + set from catalog + resolve stagenum via `catalogResolveStage()` at consumption. | S165 — pdgui_menu_solomission.cpp, mainmenu.c |
| B-117 | Hard crash on match exit — `pdguiEndscreenExitToMainMenu()` never popped `g_CtxImGuiMenu`; stale context survived stage transition. Fix: `inputCtxPopDeferred(&g_CtxImGuiMenu)`. | S175 — pdgui_bridge.c |
| B-99 | Updater extraction failure / parse failure — FIX-F.1: `curlGet()` returns HTTP code; 403 rate-limit path with user-visible message. FIX-F.2: `fsFullPath("$E/")` fallback when `installDir` empty. FIX-F.3: 1 MB minimum size check on extracted `PerfectDark.exe`. | S245 / 2026-04-13 — 205c74a7 — updater.c |
| B-98 | Solo mission pause menu fell back to OG rendering — ImGui menu not fully implemented. Fix: `renderPauseMenu()` fully implemented via hotswap registration. | S164 — pdgui_menu_solomission.cpp |
| B-97 | Special Assignments not separated from main mission list. Fix: sections existed from M1.1 (S168); S242/S245 added completion counters (done/total) + SeparatorText headers. | S242 / S245 — pdgui_menu_solomission.cpp |
| B-96 | Mission select difficulty flow wrong. Fix: two-panel (list / detail with inline difficulty + objectives + Start), single-screen flow. | S168 (M1.1) — pdgui_menu_solomission.cpp |
| B-95 | Update notification banner persists during active gameplay. Fix: `pdguiPauseGetNormMplayerIsRunning()` suppresses banner during combat sim / solo missions; reappears on menu/lobby. | S221 / 2026-04-13 — pdgui_menu_update.cpp |
| B-93 | Pause menu missing Abort/Restart/objectives. Fix: 5-button menu + objectives checklist with difficulty filter + completion icons. | S164 — pdgui_menu_solomission.cpp |
| B-91 | Mission detail popup "(No objectives)". Fix: right panel loads objectives via `soloLoadBriefingForStageId()` on select; filters by difficulty. | S168 (M1.1) — pdgui_menu_solomission.cpp |
| B-90 | Mission select shows all missions regardless of unlock. Fix: locked missions grayed out / non-selectable; `isStageDifficultyUnlocked()` gates accessibility. | S168 (M1.1) — pdgui_menu_solomission.cpp |
| B-86 | enet_peer_send return unchecked — failed sends undetected. Fix: check return + destroy on failure. | S185 — netdistrib.c |
| B-83 | Incomplete shutdown sequence — quit didn't flush saves / ENet / SDL audio. Fix: H-7 reorder `mempPCFreeAll` after subsystem shutdowns. | S185 — main.c |
| B-82 | Audio 22020 Hz (should be 22050). Fix: M-4 corrected. | S185 — audio.c:66 |
| B-80 | `archive_bytes` not validated at BEGIN time — stored without cap until END. Fix: C-4 256 MB cap on decompression buffer. | S185 — netdistrib.c |
| B-79 | Mod distribution chunk ordering ignored — out-of-order delivery silently corrupted archive. Fix: `expected_chunk` validates ordering; C-5 compressed_cap overflow guard. | S185 — netdistrib.c |
| B-72 | SVC_LOBBY_STATE raw stagenum u8 on wire. | CLOSED 2026-04-13 — confirmed fixed by v27 protocol refactor (catalog ID string + `assetCatalogResolve(arena_id)`) — netmsg.c |
| B-60 | Stray 'g'+'s' behind Video/Audio tabs in Settings — title text drawn without clipping; font descenders extended below title bar. Fix: `dl->PushClipRect()` around title text + glow. | S223 — pdgui_menu_mainmenu.cpp |
| B-118 | CI intro cutscene 56 late-added models — manifestSPTransition ran before setupLoadFiles, g_StageSetup.props NULL, CHR/prop scan missed all setup characters. Fix: manifestSPRescanSetup post-load hook + manifestMenuTransition for title stage. | 2026-04-13 — netmanifest.c, lv.c, pdmain.c |
| B-133 | Inline Vp in display list caused GBI crash — charpreview embedded Vp data inline in GBI display list; interpreter tried to execute it as opcode 0x02 (vscale bytes), fatal error. Fix: static Vp. | S210 — pdgui_charpreview.c: s_PreviewVp static + gfx_pc.cpp: PRIxPTR format fix |
| B-81 | JSON tokenizer unbounded recursion — crafted save nesting → stack overflow | S200 — savefile.c: S_MAX_DEPTH 64 guard + 256KB file size cap |
| B-78 | Chat rebroadcast DoS amplification — no message size cap; rate limiter alone allowed 160KB/s per attacker | 2026-04-11 — netmsg.c: CHAT_MSG_MAX_LEN 255 + length check before rate-limit ring (cb6f4763) |
| B-84 | Dead `tmp[1024]` in `netmsgSvcChatRead` | 2026-04-11 — removed alongside B-78 fix |
| B-115 | Post-game mouse unresponsive — legacy Save Player + Confirm Name dialogs rendered native, stealing input from ImGui endscreen. Suppressed both as noop. | S176 — pdgui_menu_mpingame.cpp, pdgui_menu_warning.cpp |
| B-114 | CMakeLists.txt corruption — ~30MB of garbage bytes injected at lines 181 and 532 by devtools encoding bug; broke all builds | S148 — CMakeLists.txt (b84c6ba) |
| B-113 | Stack overflow → silent crash in 31-bot matches — 2MB default stack exhausted in deep AI/collision chains; UEF double-faulted (8KB on stack); process died with no log. Fixed: 8MB reserve + VEH with static buffers | S150 — CMakeLists.txt / crash.c / system.c (85928d9) |
| B-111 | Bot stuck-detection all 31 bots fire simultaneously at frame 180 — `s_BotStuck` zero-initialized, bogus distance-from-origin comparison on first check → all bots marked stuck at frame 180 | S150 — bot.c (87b3388) |
| B-110 | Bot spawn void geometry crashes — bots spawned at underground/invalid positions (y=-634 on Chicago) due to AIDROP filter collapsing all valid pads to padnum=0; then 31-bots-on-24-pads fallback stacking; then room==-1 persistence. Root-cause fix: remove AIDROP filter (S149); pad-scoring skip of unspawned bots + jitter fallback (S149); room resolution hardening + botSpawnAll failsafe (S148) | S148–S149 — playerreset.c / navspawn.c / botSpawn / botSpawnAll (59818e3, d2e558e, e03a990, a81926e) |
| B-109 | Stale `s_SelectedBotSlot` reference in room screen reset — crash hazard on room screen revisit | S144 — pdgui_menu_room.cpp |
| B-108 | Authority client chr desync detection active on dedicated server — triggered continuous resync storm with many bots | S142AYTEST 2026-04-16**. Truncated entry — see session log for details. |  |  |
| **B-179** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-19 (sweet-chandrasekhar-7d64da — S371)**. 22/32 bots defaulting to mphead=0 (Joanna Dark head) in MP. Six specific heads load with parts=0 (torn modeldefs): `head_dark_snow`, `head_ddshock`, `head_carrington`, `head_ddsniper`, `head_president`, `head_cassandra`. Root cause: `validateModeldef` in `port/src/modelcatalog.c` was the remaining `numparts <= 0 ⟹ INVALID` guard that B-166 (`modeldefLoad`) and B-167 (`propobj.c` bbox walkers) did not propagate to. When a head's modeldef had parts=0 but valid rootnode/skel (the six heads above), the catalog marked it INVALID, and every `catalogValidateHeadId()` call (SVC_STAGE_START read paths in `netmsg.c:1366/1421/4792`) fell back through `catalogGetSafeHead → CATALOG_FALLBACK_HEAD` to head 0 (dark_combat = Joanna Dark). 22 of 32 bots either picked one of those six heads directly or had their default paired head routed through this fallback. Fix: `validateModeldef` now only rejects `numparts <= 0` when `category == MODELCAT_BODY` (bodies need skeletal parts for `body0f02ce8c` merge; falling back to dark_combat is the correct UX for torn bodies). Heads pass through with parts=0 — rendering walks rootnode DL nodes, `modelAttachHead` only needs rootnode, `modelGetPart` already returns NULL on parts=0 and every caller tolerates that (sunglasses/hudpiece toggles simply skipped). Build clean 774/774. `PerfectDark.exe` 53,202,559 / `PerfectDarkServer.exe` 23,141,856. | `port/src/modelcatalog.c` | Playtest: MP Combat Sim with 30+ bots — bots should wear their actual assigned heads (Cassandra, President, Carrington, DD-variants, Dark Snow) rather than 22 identical Joanna Dark heads. `CATALOG: [idx] file 0x%04x — numparts=0 out of range (INVALID)` warnings at validation time should disappear for the six head filenums. |
| **B-180** | HIGH | **FIXED-PENDING-PLAYTEST 2026-04-19 (cascade of B-179 — S371)**. Missing necks on all character models. Cascades from B-179: when 22/32 bots were being downgraded to the Joanna Dark head, the head-to-body fit was wrong and neck geometry (part of each head's own rootnode mesh) appeared missing or mismatched. With B-179 fix installed, each bot now loads its actual head (including neck geometry) and attaches via `modelAttachHead` to the body's headspot. `bodyCalculateHeadOffset` still runs for cross-gender/cross-type pairings to realign the head vertically. | `port/src/modelcatalog.c` (cascade fix) | Playtest: after B-179 playtest passes, verify each bot has a visible neck connecting head to body — no floating heads, no disconnected meshes. |
| **B-181** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (interesting-nobel-184c6c)**. Wrong weapons spawning in match — map has 10 weapon markers vs target 16, fallback path was injecting unexpected weapons outside the selected weapon set. Root cause: `setupCreateProps` fallback (`src/game/setup.c:2467`, added in commit `0b62fc68` to force spawn-with-weapon when world pickups < desiredPickups) used `catalogIdByRuntime(ASSET_WEAPON, g_MpSetup.weapons[i])` to look up a catalog ID from an MPWEAPON_* index — but the runtime cache for ASSET_WEAPON is indexed by catalog *array position* (`e->runtime_index = i` in `assetcatalog_base_extended.c`, not by weapon_id), so MPWEAPON_FALCON2 (0x01) resolved to `"base:falcon2_silencer"` (MPWEAPON_FALCON2_SILENCER at array position 1). Additional issue: the fallback set `spawn_weapon_id` but never re-derived `spawnWeaponNum` (matchStart already ran), so the current match continued to use the pre-fallback spawn weapon, and the wrong ID persisted into the NEXT match when matchStart re-read it. Fix: replaced the `catalogIdByRuntime` lookup with a catalog scan that matches `ext.weapon.weapon_id == mpw` (same pattern as `savefile.c:816` and `buildSpawnWeaponList`). Skip MPWEAPON_NONE/SHIELD/DISABLED. Always override (not just if empty) so any stale out-of-set spawn weapon is corrected. Re-derive `spawnWeaponNum` immediately so the current match's player/bot spawn respects the fallback choice. Build clean 774/774. `PerfectDark.exe` 53,195,903 / `PerfectDarkServer.exe` 23,140,832. | `src/game/setup.c` | Playtest: Car Park solo sim with a small weapon set (e.g. Pistols) + enough bots to trigger the fallback — only weapons from the configured weapon set should appear, both on pads (already correct) and on player/bot spawn. Check `SETUP: world pickups ... forcing spawn-with-weapon fallback id='...'` log line names a catalog ID that is in the configured set. |
| **B-182** | CRITICAL | **OPEN (cascade of B-179)**. Chris' client crash: ACCESS_VIOLATION 0xc0000005 at PC+0x35b107. Backtrace all within main module. Likely null deref when rendering/animating a model with parts=0 (torn modeldef). Should resolve with B-179 fix. | Unknown — needs symbol resolution | Playtest after B-179 fix — crash should not recur. |
| **B-183** | HIGH | **OPEN**. Bad character geometry — disconnected triangles, holes in mesh. Likely same torn-modeldef class (parts=0 causing incomplete geometry upload). May resolve with B-179. | `src/lib/model.c`, `port/fast3d/` | Playtest after B-179 — check character model geometry integrity. |
| **B-184** | MED | **ROOT CAUSE IDENTIFIED + FIXED-PENDING-PLAYTEST 2026-04-19 (elated-hugle-7ec221 — S384)**. Commit `fe107e3e` (2026-04-17, M4 optimisation) collapsed `ALIGN16(val)` to `(val)` — was valid for SIZE-only uses but broke POINTER uses at `src/game/gfxmemory.c:154` (`g_GfxMemPos = (u8 *)ALIGN16((uintptr_t)g_GfxMemPos)`). `Vtx` is 12 bytes, so `gfxAllocateVertices(count)` advances `g_GfxMemPos` by `count*12`; for odd counts the no-op macro leaves `g_GfxMemPos mod 16 != 0`. The next `gfxAllocateColours()` then uses `g_GfxMemPos` as the base for the `Col` array the subsequent `G_COL` command binds as `vcn`, so `gfx_sp_set_vertex_colors` reads misaligned bytes from an offset-by-12-mod-16 source — each draw call gets a consistent-but-wrong vertex-colour array, producing the per-object tints Mike screenshotted (yellow computer props, cyan elevator top, olive character faces, dark-red `env={40 0a 0a ff}` dominant on ~14% of draws in the S382.3 diag). Also explains the S382 diag "`vtx0` = `ff ff f0 f0` with `lighting=0`" rows starting at G_COL #101 — same misread pattern from a drifted pointer. Fix is the same one-line change as B-193 — `src/include/constants.h:79` restored to `((((val) + 0xf) | 0xf) ^ 0xf)`. The existing S382 `GFX.DIAG: G_COL` instrumentation in `port/fast3d/gfx_pc.cpp::gfx_sp_set_vertex_colors` is retained; post-fix log should show `vtx0` stabilising to authored colour bytes (typically `ff ff ff ff` or grayscale per-prop) rather than drift-dependent bytes. Build clean 777/777. `PerfectDark.exe` 53,232,611 / `PerfectDarkServer.exe` 23,154,674. **[Prior-pass history]:** Four prior passes (S372 SAVED-EFFECT strip; S381 effect_normal_tint mod folder rule-out; S382 visual-evidence + G_COL diag ship; S382 fourth-pass static diff audit of the AP Phase sprint). All rule-outs stand (SAVED EFFECT dead, effect_normal_tint inert, meshDebug inert, dlights uniform-white, no G_LIGHTING clears on char/prop draws, AP Phase 3 rename byte-identical, S322/S323 IS4MB strips touched zero render state). The tint is NOT normal-visualisation, NOT UV corruption, NOT texture selection — it was vertex-colour data misread from a misaligned vcn pointer, exactly matching the "per-object consistent tint on correct textures" pattern. | `src/include/constants.h` | Playtest CI main menu + free-roam: character skin + clothing should be their authored colours (not olive-green); computer props should be their authored tint (not yellow/cyan); elevator top should be gray (not cyan); doors should be their authored colour (not yellow). `GFX.DIAG: G_COL` lines in `pd-client.log` should show stable `vtx0` bytes that match the visible prop colour. Repeat on other stages (Dam, Carrington Villa, Felicity) to regression-check. |
| **B-185** | MED | **FIXED-PENDING-PLAYTEST 2026-04-19 (thirsty-torvalds-9c7e0f)**. Game sometimes loads with no rendered scene (inv