# PD2 PROJECT CONTEXT BRIEFING
**Generated:** 2026-03-30 (automated pre-session broker)
**Branch:** `dev` @ `726ba7d` (feat(tools): add parse-log.sh for AI session log analysis)
**Build Health:** ⚠️ UNVERIFIABLE from broker environment — last verified clean S82 (client + server)

> **Note on context file locations**: The canonical context is in `perfect_dark-mike/context/`. The files in the workspace root (`Perfect-Dark-2/`) are stale mirrors last updated at Session 48. Always use `perfect_dark-mike/context/` as the source of truth.

---

## Git State

- **Uncommitted changes:** YES — CMakeLists.txt (4 lines, likely encoding/em-dash), plus `tools/build.sh`, `tools/build.cmd`, `tools/build-cleanup.sh` (line-ending-only diff, CRLF→LF as seen from Linux mount). Functionally no content changes. Also: `tools/parse-log.cmd` and `tools/parse-log.sh` appear staged-for-deletion AND as untracked — likely a stale `.git/index.lock` artifact. Do not commit or stage anything until this is resolved on the Windows side.
- **Unpushed commits:** 3 commits ahead of `origin/dev`:
  - `726ba7d` feat(tools): add parse-log.sh for AI session log analysis
  - `6da84f7` merge: stupefied-leakey — log parsing tool for AI sessions
  - `3f2858c` merge: loving-colden — clean build system for AI sessions
- **Stash entries:** 3 stashes present (buildtool-autostash, release WIP, dev WIP). Do not pop without verifying contents are not needed.
- **Active worktrees:** 15 total. 13 are prunable (no unmerged commits). **2 have unmerged commits — see Unmerged Worktree Work below.**

---

## Active Workstreams

### 1. NAT Traversal — MERGED into dev, worktrees STALE
- **Goal:** STUN-based external address discovery + UDP hole punch for connecting clients through NAT
- **Task IDs:** D8 (NAT Traversal)
- **Status:** ✅ DONE in dev. `8d58090` (feat: NAT Phase 1-4) + `982ac4b` (fix: ENetAddress forward-decl) are on dev. NET_PROTOCOL_VER is now **23**.
- **Key files:** `port/src/net/netstun.c`, `port/include/net/netstun.h`, `port/src/net/netholepunch.c`, `port/include/net/netholepunch.h`, `port/src/net/net.c`, `port/src/net/connectcode.c`, `port/include/net/connectcode.h`, `CMakeLists.txt`
- **Last known state:** STUN background thread discovers external IP/port. Two-probe NAT type detection. UDP hole punch waterfall (DIRECT → PUNCH → PUNCH_ENET). `connectCodeEncodeWithPort()` for 4-word (default port) or 6-word (non-default) codes.
- **Dependencies:** None — additive on top of existing ENet stack.
- **⚠️ WARNING:** Two worktrees (`claude/optimistic-austin`, `claude/romantic-kirch`) contain older, parallel NAT implementations that are NOT the version in dev. These should be discarded after verification. Do not attempt to re-merge — dev's implementation is more complete and already fixed.

### 2. Solo Room Screen (Combat Simulator routing) — MERGED
- **Goal:** Route "Combat Simulator" on main menu to Solo Room screen (offline mode) instead of old N64 lobby
- **Task IDs:** S82 / J-series adjacent
- **Status:** ✅ MERGED — commit at `c120ed9` (merged from `claude/optimistic-jackson`)
- **Key files:** `port/fast3d/pdgui_lobby.cpp`, `port/fast3d/pdgui_menu_room.cpp`, `port/fast3d/pdgui_menu_mainmenu.cpp`
- **Last known state:** Solo Room screen implemented. Build-verified clean. Needs playtest: Main Menu → Combat Simulator → configure bots/map → Start Match → verify match loads and runs. "Back to Menu" should return cleanly.
- **Dependencies:** None pending.

### 3. Networked MP Stability (B-51/52/53) — NEEDS PLAYTEST
- **Goal:** Verify bots visible/in-bounds, weapons/ammo pickable, doors openable in networked match
- **Task IDs:** B-51, B-52, B-53
- **Status:** NEEDS PLAYTEST — may be resolved by chrslots+options sync fix already in dev
- **Key files:** `port/src/net/netmsg.c` (CLC_LOBBY_START payload), `port/src/net/net.c`, `src/game/mplayer/mplayer.c`
- **Last known state (S81):** chrslots and options now correctly transmitted in SVC_STAGE_START and CLC_LOBBY_START. Weapon array (6 elements) added to CLC_LOBBY_START. Protocol bumped to v22 for weapon array; subsequently bumped to v23 for NAT STUN.
- **Dependencies:** None — just needs playtest.

### 4. B-50: Dedicated Server Match-End Timer — FIXED, NEEDS PLAYTEST
- **Goal:** Dedicated server sends SVC_STAGE_END when timelimit expires
- **Task IDs:** B-50
- **Status:** FIXED (S81) — wall-clock timer in `hub.c` sends `netServerStageEnd()` at timelimit. Needs verification: start timed match on dedicated server, confirm match ends at correct time.
- **Key files:** `port/src/hub.c`
- **Dependencies:** None.

### 5. Catalog Activation (C-5 / C-6) — PENDING
- **Goal:** Wire texture override and anim override through Asset Catalog intercepts
- **Task IDs:** C-5, C-6
- **Status:** C-5 CODED (wiring needs confirmation); C-6 NEXT
- **Key files:** `port/src/assetcatalog_load.c`, `src/game/texload.c` (C-5), `src/game/anim.c` (C-6)
- **Last known state:** C-7 (sound override via `audioPlayFileSound`) is DONE. C-4 (file override intercept in `romdataFileLoad`) is DONE. C-5 (texture) is coded but wiring unconfirmed. C-6 (anim) is NEXT.
- **Dependencies:** C-5 before C-6 (same intercept pattern).

### 6. Room Architecture (R-2) — NEXT
- **Goal:** Expand hub to multi-room with `HUB_MAX_ROOMS=16`, `HUB_MAX_CLIENTS=32`, `leader_client_id`, `room_id`, on-demand creation, remove permanent room 0
- **Task IDs:** R-2
- **Status:** NOT STARTED (R-1 done in S52)
- **Key files:** `port/src/hub.c`, `port/include/hub.h`, `port/src/room.c`, `port/include/room.h`
- **Dependencies:** R-2 must complete before R-3 (room sync protocol), which blocks L-1/L-2 lobby UI.

### 7. J-2: Server GUI Connect Code Display — PENDING
- **Goal:** Add connect code display + Copy button to `server_gui.cpp` Server tab
- **Task IDs:** J-2
- **Status:** NOT STARTED
- **Key files:** `port/fast3d/server_gui.cpp`
- **Dependencies:** None — purely additive.

### 8. B-12 Phase 3: Remove chrslots — READY
- **Goal:** Remove `chrslots` field from netmsg, remove legacy shims, BOT_SLOT_OFFSET, bump to v23 (already at v23 due to NAT, re-confirm protocol policy)
- **Task IDs:** B-12 Phase 3
- **Status:** READY — depends on Phase 2 QC which is DONE
- **Key files:** `src/game/mplayer/participant.c/h`, `port/src/net/netmsg.c`, `port/src/net/net.h`
- **Dependencies:** Must confirm Phase 2 QC. Note: protocol was to bump to v22 for this; protocol is now at v23. Coordinate with whoever manages protocol version.

### 9. UI Scaling — NOT STARTED (v0.1.0 blocker)
- **Goal:** UI scaling across resolutions — required for v0.1.0
- **Task IDs:** Listed as Critical Blocker in tasks-current.md
- **Status:** NOT STARTED
- **Key files:** TBD — affects `port/fast3d/pdgui_*.cpp` broadly
- **Dependencies:** None. Blocks v0.1.0 release.

### 10. Playtest Backlog Verification — ONGOING
- Several coded features from S68–S82 are awaiting playtest verification:
  - T-7: mod.json body/head/arena catalog registration
  - T-8/T-9: Stage table restore + texture cache flush on reload
  - B-46: Void spawn on MP stages
  - B-47: Exit freeze on window close
  - Combat Sim scenario save/load (S71)
  - Solo Room screen (S82)
  - B-39/40/41/42 (all build-verified but need live play)

---

## Collision Map

### 🔴 HIGH RISK: Protocol Version (NET_PROTOCOL_VER)

| Resource at Risk | `port/include/net/net.h` → `#define NET_PROTOCOL_VER` |
|---|---|
| **Conflicting workstreams** | B-12 Phase 3 (was planning to bump to v22), NAT Traversal (already bumped to v23), any future message format change |
| **Collision type** | Protocol version coupling — incompatible clients/servers if bumped independently |
| **Current value** | v23 (set by NAT traversal, already in dev) |
| **Mitigation** | SERIALIZE all protocol changes through a single session. Never let two sessions both touch NET_PROTOCOL_VER. B-12 Phase 3 was scheduled for v22 but v23 is already live — the version bump plan needs to be revisited before that task starts. |

### 🔴 HIGH RISK: Unmerged NAT Implementations

| Resource at Risk | `port/src/net/netstun.c`, `port/include/net/netstun.h`, `port/src/net/netholepunch.c/h`, `port/src/net/connectcode.c/h`, `port/include/net/net.h` |
|---|---|
| **Conflicting workstreams** | dev branch (NAT v1-4 merged, fixed) vs. `claude/optimistic-austin` (parallel NAT v1-4, unmerged) and `claude/romantic-kirch` (parallel NAT v1-2, unmerged) |
| **Collision type** | Duplicate implementation — if either worktree is accidentally merged into dev, these files will conflict severely |
| **Mitigation** | Do NOT merge `claude/optimistic-austin` or `claude/romantic-kirch` into dev. Mark them for deletion. Verify dev's NAT is functional before closing the issue. |

### 🟡 MEDIUM RISK: hub.c / room.c — Match Timer + R-2

| Resource at Risk | `port/src/hub.c`, `port/src/room.c`, `port/include/hub.h`, `port/include/room.h` |
|---|---|
| **Conflicting workstreams** | B-50 fix (wall-clock timer, already merged) + R-2 (room lifecycle expansion, next up) |
| **Collision type** | Edit conflict — R-2 will significantly restructure hub.c and room.c. The B-50 timer logic (`s_MatchStartMs`, `s_MatchEndSent`) must be preserved. |
| **Mitigation** | R-2 session must read the B-50 fix in hub.c before modifying it. The timer logic is in `hubTick()` — preserve it during the R-2 restructure. |

### 🟡 MEDIUM RISK: netmsg.c — Protocol Message Changes

| Resource at Risk | `port/src/net/netmsg.c` |
|---|---|
| **Conflicting workstreams** | B-12 Phase 3 (chrslots removal from SVC_STAGE_START), R-3 (new room messages), any protocol change session |
| **Collision type** | Edit conflict — netmsg.c is one of the most-modified files in the codebase |
| **Mitigation** | Do not run B-12 Phase 3 and R-3 in parallel. Serialize: B-12 Phase 3 first (removes chrslots), then R-3 (adds room messages). |

### 🟡 MEDIUM RISK: CMakeLists.txt — Multi-session build system changes

| Resource at Risk | `CMakeLists.txt` |
|---|---|
| **Conflicting workstreams** | Any task that adds new .c files (R-2 will add room management, B-12 Phase 3 may remove files) |
| **Collision type** | The server target uses a manually curated `SRC_SERVER` list. Any new .c files in `port/src/` must be explicitly added. Client uses GLOB_RECURSE (auto-discovers). |
| **Mitigation** | Every session that adds new .c files must check if they need to be added to `SRC_SERVER` block. This was the root cause of the S47e server build failure. |

### 🟡 MEDIUM RISK: pdgui_menu_room.cpp — Solo Room + R-3/R-4

| Resource at Risk | `port/fast3d/pdgui_menu_room.cpp` |
|---|---|
| **Conflicting workstreams** | Solo Room screen (S82, merged) + L-3 (Room Interior from R-series) |
| **Collision type** | Both tasks add solo-mode logic to the same file; L-3 will substantially restructure the room screen |
| **Mitigation** | Assign Solo Room playtest verification before starting L-3. L-3 must read S82's implementation before modifying the file. |

### 🟡 MEDIUM RISK: context/ files — Concurrent session log updates

| Resource at Risk | `context/session-log.md`, `context/tasks-current.md` |
|---|---|
| **Conflicting workstreams** | Any two parallel sessions that both write to context files |
| **Collision type** | Edit conflict — parallel sessions updating task status or appending session log entries |
| **Mitigation** | Only ONE session at a time should write to context files. Serialize context updates. Do not use worktrees for context file changes (standard no-worktree policy applies here too). |

---

## Recommended Session Plan

### Safe to run in parallel (touch different files)
- **C-5 texture override wiring** (assetcatalog_load.c, texload.c) — completely independent of networking
- **B-50 playtest verification** — just a game test, no code changes expected
- **J-2 server GUI connect code** (server_gui.cpp only) — isolated UI change
- **B-51/52/53 playtest verification** — just a networked match test, no code changes expected

### Must be serialized (touch same files or protocol)
1. **B-12 Phase 3** (chrslots removal from netmsg.c) → THEN **R-3** (room protocol messages in netmsg.c)
2. **R-2** (hub.c/room.c restructure) → THEN **R-3** → THEN **L-1/L-2** (lobby UI)
3. Any **NET_PROTOCOL_VER bump** must be the ONLY protocol change in that session — no stacking with other message format changes

### Requires specific execution order (dependencies)
1. Playtest **Solo Room screen** (S82) before starting L-3 room interior work
2. Complete **R-2** before starting **R-3** (room sync protocol)
3. **R-3** must complete before **L-1/L-2** (lobby UI) can be wired
4. Verify **B-12 Phase 2 QC** before starting **B-12 Phase 3**

### Should NOT be delegated without human (Mike) review
- **B-12 Phase 3** (removes chrslots field + BOT_SLOT_OFFSET — broad structural change across netmsg.c and mplayer code; protocol implications need review given v23 is already live)
- **UI Scaling** (touches all pdgui_*.cpp files — architectural scope, needs design decision first)
- **Stash cleanup** (3 stashes present, including release-wip and buildtool-autostash — Mike should review before any pop or drop)
- **Worktree cleanup** (`claude/optimistic-austin` and `claude/romantic-kirch` — Mike should explicitly confirm these should be deleted before anyone discards them)

---

## Critical Build/Project Notes

Every code session MUST know these:

1. **Use ONLY the Edit tool on existing files — NEVER the Write tool.** Write truncates; Edit patches. This is the standing protection against the file-truncation incident.
2. **Build command:** `C:/msys64/usr/bin/bash.exe -lc "cd /c/Users/mikeh/Perfect-Dark-2/perfect_dark-mike && TEMP=/tmp cmake --build build/client 2>&1"` (client). Replace `build/client` with `build/server` for server. The `TEMP=/tmp` prefix is mandatory — MSYS2 cannot create temp files without it.
3. **`bool` is `s32`** — defined in `types.h` as `#define bool s32`. NEVER `#include <stdbool.h>` in game code (`src/game/`, `src/lib/`). For new port/ files in C, use `int` or `s32` for boolean-like values.
4. **NET_PROTOCOL_VER is currently 23** (NAT STUN query advertising). Any session planning a protocol bump must be aware of this. B-12 Phase 3 was planned for v22 — the plan needs updating.
5. **No worktrees** — all code changes in main working copy only. The S48 principle: no worktrees, ever. Changes land in the working copy and get committed directly.
6. **SRC_SERVER in CMakeLists.txt is manually curated.** Client uses GLOB_RECURSE (auto). Server does not. Every new .c file added to `port/src/` must be manually added to the SRC_SERVER block (lines ~478–490 in CMakeLists.txt). Forgetting this was the root cause of S47e.
7. **Run headless build for BOTH client AND server before marking any task complete.** Even client-only changes can break server build due to shared headers. This is non-negotiable.
8. **EVERY code session must merge changes into main working copy, build-verify BOTH targets, and confirm before reporting done.** No handing off unmerged or unverified work.
9. **Context canonical location:** `C:\Users\mikeh\Perfect-Dark-2\perfect_dark-mike\context\`. Root workspace copies (in `Perfect-Dark-2/`) are stale mirrors — do not trust them. The session-log.md and tasks-current.md at the workspace root are from Session 48.
10. **Index domain warning:** Three separate spaces exist — stage table index (g_Stages[], 0-86), solo stage index (g_SoloStages[], 0-20), and stagenum (logical ID). Never cross these. Mod stages have table indices 61-86 but no solo stage index.

---

## Unmerged Worktree Work

### ⚠️ claude/optimistic-austin — 1 commit ahead of dev

- **What it contains:** `1383ce8` feat(net): NAT traversal Phase 1-4 — STUN + UDP hole punch
- **Risk:** dev ALREADY contains a separate NAT traversal implementation (`8d58090` + `982ac4b`). These are parallel, conflicting implementations of the same feature.
- **Recommendation:** DO NOT MERGE. Dev's NAT implementation is complete, fixed (982ac4b), and in production. The worktree's version is orphaned. Mark for deletion. Mike should confirm before discarding.

### ⚠️ claude/romantic-kirch — 1 commit ahead of dev

- **What it contains:** `b402aef` net: Phase 1+2 NAT traversal — STUN client + query advertising (subset of the above)
- **Risk:** Same issue — superseded by dev's NAT implementation. This is the older, Phase 1+2-only version of the work that eventually became the full Phase 1-4 in `optimistic-austin` and then the final merged version in dev.
- **Recommendation:** DO NOT MERGE. Discard after Mike confirms.

### All other 13 worktrees

All marked prunable by git. No commits ahead of dev. Safe to `git worktree prune` at Mike's discretion.

---

## Open Questions / Blockers

1. **NET_PROTOCOL_VER reconciliation:** B-12 Phase 3 was planned to bump to v22, but v22 was used by weapon array in CLC_LOBBY_START (S81) and v23 was used by NAT STUN. B-12 Phase 3 must be revised to target v24 (or whatever the next available version is). Mike should confirm the protocol bump strategy before this session begins.

2. **Stale index.lock:** The `.git/index.lock` file was present and could not be unlinked from the broker environment. This may indicate a git process is actively running on the Windows side, or the lock is stale. Mike should check for stale lock files on the Windows side before the next git operation.

3. **Stash 1 (release-wip):** `stash@{1}: On release: release-wip: dynamic curl, ROM constants, version bumps, SSL fix` — this looks substantial. Does Mike want to carry this forward or discard it?

4. **Build health unverifiable from broker:** The broker runs in a Linux sandbox and cannot execute MSYS2/GCC. Last confirmed clean build was S82 (S81 notes: client + server verified). If any session wants to depend on a clean baseline, Mike should run `.\tools\build.cmd --target both` before delegating work.

5. **UI Scaling design decision needed:** This is a v0.1.0 blocker but has no design, no plan file, and "not started." Before any session picks this up, Mike should articulate the target scope (what resolutions, what minimum, how to handle the ImGui DPI).

6. **Worktree/branch cleanup:** 13 prunable worktrees and ~150 local `claude/` branches. This is administrative debt. Running `git worktree prune` will remove the prunable worktrees. Branch cleanup (`git branch -d`) can follow. Mike's call on timing.

7. **Context file staleness at workspace root:** `session-log.md` and `tasks-current.md` at `C:\Users\mikeh\Perfect-Dark-2\` are from Session 48. The canonical files are in `perfect_dark-mike/context/`. This is by design (per S48 decisions), but any session that reads the workspace root files will get severely outdated context. Sessions must be explicitly told to use the context/ subdirectory.
