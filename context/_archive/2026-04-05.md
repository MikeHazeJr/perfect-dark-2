# Daily Summary — 2026-04-05

## Completed

- **S151 — Playtest bug diagnosis and fixes**: Addressed 5 critical post-playtest bugs in a single session. FIX-PLAYTEST-1: Bot body/head resolution — CLC_LOBBY_START server handler wasn't storing body_id/head_id strings, causing all bots to get dark_combat body (invisible). MATCH_MAX_SLOTS bumped 32→40. FIX-PLAYTEST-2: Prop resync spam — desync counter reset on 0-prop receive stops infinite polling. FIX-PLAYTEST-3: Death-in-hub crash — guard titleSetNextStage against stagenum=0x00, redirect to CI. FIX-PLAYTEST-4: Bot HP too low (maxdamage set to 8.0f). Commit `da40788`.

- **S152 — Hotswap frame-1 CI crash fix (B-114)**: After mission fail → legacy menu exit → CI load, `screenManifestTick` fired on frame 0 during catalog reinit, triggering `catalogUnloadAsset` and crashing. Fix: guard `screenManifestTick` behind `lvframe60 >= 2`; guard hotswap-close mouse capture flush behind `lvframe60 > 0`. Bridge function `pdmainGetLvFrame60()` exposed for C++ access to `g_Vars.lvframe60`.

- **S153 — Audit, recovery, and lobby unification close-out**: Massive session covering multiple workstreams:
  - **Codebase audit**: Full verification of all completed tasks S130–S152 — 22/22 confirmed present.
  - **Git repo recovery**: Restored missing `.git/objects/` from GitHub; cleaned `.git.broken` (200MB) + 7 orphaned worktrees (~23GB freed); pushed `dev` to origin.
  - **S131 cleanup**: 10 bare `strcpy` → `strncpy` in `input.c`; local `MATCH_MAX_SLOTS` defines removed from 3 UI files; `headnum`/`bodynum` → `body_id`/`head_id` field renames in mpsettings + teamsetup. Commit `05d5f1d`.
  - **U-7 matchsetup.cpp retired (Lobby Unification Phase 2)**: 3D char preview ported to room.cpp bot modal; 4 dialog push points redirected; file renamed `.cpp.retired` (1,582 lines removed). Commit `9fe169e`.
  - **U-10 Deferred bot authority (Lobby Unification Phase 4)**: `g_NetPendingBotAuthority` flag — `botTick` promotes pending→active when pads loaded. Commit `6f471a7`.
  - **B-116 fix**: SVC_STAGE_START writer used wrong slot index; pre-built `botSlotMap[]` from actual `SLOT_BOT` entries. Committed (not pushed to origin).

- **Context maintenance (automated)**: Backfilled 6 missing sessions (S145–S150) into `session-log.md`. Added 5 new bugs (B-110 through B-114) to `bugs.md`. Updated README and `tasks-current.md`. Commit `bf6bece`.

- **Nightly build report (automated)**: 24 commits summarized for the prior 24h. Build health verified (CMake configure passes; can't compile in Linux sandbox). Version at v0.0.38.

- **Context briefing (automated)**: Produced dispatch briefing for Apr 5 — identified 11 active workstreams, flagged file truncation incident (recovery steps documented), collision map for overlapping work.

- **Lobby Unification declared COMPLETE** (U-1 through U-10): All phases done as of S153. `matchsetup.cpp` retired.

## In Progress

- **Catalog ID Migration (Phases 0–6 bodies/heads)**: Execution began late evening / early Apr 6 in S154–S155. Not part of Apr 5 proper, but seeds were planted in S153's close-out which freed bandwidth for this track.

## Changed / Decisions Made

- **Lobby Unification declared complete** — all 10 phases (U-1 through U-10) confirmed done. `matchsetup.cpp` is `.retired` (not deleted pending edge-case audit).
- **B-116 fix committed but intentionally not pushed** — will push with next playtest build for batched verification.
- **MATCH_MAX_SLOTS canonical value is 40** — all local `#define` overrides removed; single source of truth in `matchsetup.h`.
- **NET_PROTOCOL_VER at v29** as of end of day (will jump to v31 during Apr 6 work).
- **Deferred bot authority model adopted** — deterministic pad-availability check replaces 60-frame timeout gate.

## Incomplete / Blocked

- **B-112: Chr pointer corruption in 31-bot matches** — guard + VEH diagnostics in place (S150), but root cause unknown. Awaiting next crash log from a Windows playtest session.
- **B-115: Post-game menu mouse unresponsive** — legacy menu steals input during ImGui hotswap; not yet addressed.
- **Event-driven prop sync** — current CRC polling is a stop-gap (FIX-PLAYTEST-2). Full event-driven system deferred.
- **Git push of B-116 and maintenance commits** — sandbox lacks GitHub credentials; must be pushed from Mike's dev machine.
- **5 stale worktrees need pruning** — Linux sandbox can't delete Windows-path directories; `git worktree prune` needed from MSYS2/Git Bash.

## Bugs Found / Fixed

| Bug | Status | Notes |
|-----|--------|-------|
| B-110 (bot spawn void geometry) | **Fixed** (S145–S149) | Confirmed in codebase audit |
| B-111 (bot stuck-detect all fire at frame 180) | **Fixed** (S150) | Confirmed in codebase audit |
| B-113 (stack overflow → silent crash) | **Fixed** (S150) | 8MB stack + VEH |
| B-114 (CI crash frame 1 after mission fail exit) | **Fixed** (S152) | screenManifestTick deferred to lvframe60>=2 |
| B-116 (bot body/head wrong catalog IDs on server) | **Fixed** (S153) | botSlotMap[] + direct slots[] read |
| B-112 (chr pointer corruption) | **Open** | Guard in place, root cause unknown |
| B-115 (post-game mouse unresponsive) | **Open** | Not yet addressed |
| Invisible bots (dark_combat body) | **Fixed** (S151) | CLC_LOBBY_START storage fix |
| Prop resync spam | **Fixed** (S151) | Stop-gap; event-driven sync TBD |
| Death-in-hub crash | **Fixed** (S151) | titleSetNextStage guard |

## Direction & Next Steps

- **Catalog ID Migration** is now the primary workstream (already started in S154–S155 on Apr 6).
- **B-112 diagnosis** remains the top crash blocker — need a VEH crash log from Windows.
- **D5.3 Pause Menu** is the highest-priority menu system item (not started, unblocked).
- **Build verification** needed for the large batch of changes from S151–S153.
- Push pending commits to origin from dev machine.

## Short-Term Goals (this week)

- **Catalog ID Migration Phases 0–6**: bodies/heads DONE (S155); weapons (~660 refs), stages (~80 refs), models (~83 refs) still pending.
- **B-112 root cause**: Capture VEH crash log, identify corruption source.
- **D5.3 Pause Menu**: Start implementation (highest priority menu item).
- **Playtest build**: Verify S151–S155 fixes hold end-to-end.

## Long-Term Goals

- **D5 Full Menu System**: ~30% complete — infrastructure done, visual layer (D5.0) is the next blocker after pause menu.
- **D13 Update System**: ~70% complete but at risk of bitrot (17+ days without build integration).
- **D14a Counter-Op**: Not started.
- **Public release readiness**: Tier 2 bugs (B-78, B-79, B-80, B-81) still open.

## Notes

- **Exceptionally productive day**: 3 full manual sessions (S151–S153) plus 3 automated sessions. ~24 commits landed. Lobby Unification fully closed out. Multiple crash bugs fixed.
- **File truncation incident** was flagged by the Apr 4 briefing — recovery steps were documented and the working copy was restored via `git checkout HEAD`.
- **Debug session**: Diagnosed a Claude Code API 400 error ("Could not process image") — caused by ~19MB transcript size, not the image itself. Recommendation: start fresh sessions when transcripts grow large.
- **Context system gap**: 6 sessions (S145–S150) had gone unlogged during the intense playtest crash-fix sprint. The automated maintenance session backfilled all of them. Context is now current through v0.0.38/S150 (with S151–S153 logged manually during their sessions).
