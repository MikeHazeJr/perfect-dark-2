# D5 Phase 3 -- Batch 4 Session Notes (Cheats & Cinema)
**Date**: 2026-04-11
**Session**: S202 (worktree: friendly-morse)
**Files**: port/fast3d/pdgui_menu_cheats.cpp (NEW), port/fast3d/pdgui_menu_mainmenu.cpp, port/include/pdgui_menus.h

---

## Scope

10 legacy menudialogdefs:

| # | Dialog | cheats.c line | Current | Role |
|---|--------|---------------|---------|------|
| 1 | g_CheatsMenuDialog | 1654 | OG | Root hub (6 category entries + Turn-Off-All + Unlock-Everything + Done) |
| 2 | g_CheatsFunMenuDialog | 1024 | OG | 6 Fun checkboxes |
| 3 | g_CheatsGameplayMenuDialog | 1141 | OG | 9 Gameplay checkboxes (incl. Dual-Wield-All-Guns) |
| 4 | g_CheatsSoloWeaponsMenuDialog | 1250 | OG | 8 Solo Weapons checkboxes |
| 5 | g_CheatsClassicWeaponsMenuDialog | 1359 | OG | 8 Classic Weapons checkboxes |
| 6 | g_CheatsWeaponsMenuDialog | 1468 | OG | 8 Weapons checkboxes |
| 7 | g_CheatsBuddiesMenuDialog | 1553 | OG | 5 Buddy radios (Velvet + 4 unlocked) |
| 8 | g_CheatsWarningMenuDialog | 478 | TYPE-FB (SUCCESS) | Initial warning ("If you activate cheats...") |
| 9 | g_CheatsConfirmUnlockMenuDialog | 930 | TYPE-FB (DANGER) | Confirm-Unlock-Everything (Yes/No) |
| 10 | g_CinemaMenuDialog | mainmenu.c:4847 | OG | Cutscene viewer (grouped list) |

## Design (integrates with Batch 0-3 shared system)

Per Mike's S202 guidance: **do not do 1:1 legacy port — integrate with patterns already established by Batches 0-3**.

Shared primitives reused from the Batch 0-3 system:
- `pdguiPopupDarkenBehind(0.55f)` -- scrim (Batch 0)
- `pdguiDrawPdDialog` / `drawPdWindowFrame` -- title frame
- `pdguiBeginActionBar` / `pdguiActionBarButton` / `pdguiBodyHeightForActionBar` -- docked action bar (Batch 0)
- `langSafe` with English fallbacks -- per-row text (Batch 2)
- D-pad nav with wrap -- from S194 `renderCoopAntiOptionsImpl`
- `PDGUI_SND_TOGGLEON/OFF/FOCUS/OPENDIALOG/KBCANCEL/SELECT/ERROR` -- per-event SFX (Batch 2)
- `inputCtxPush/Pop(&g_CtxImGuiMenu)` on appearance/close (Batch 3)
- **Legacy-handler callthrough pattern** (s194 pattern from solomission.cpp): re-declare `menuitem`/`handlerdata` stubs locally, call the legacy C handler through a function pointer. Reuses `cheatCheckboxMenuHandler`, `cheatMenuHandleBuddyCheckbox`, `cheatMenuHandleTurnOffAllCheats`, `menuhandlerUnlockEverything`, `menuhandlerCinema` -- no duplicate state logic.

### Cheats consolidation

The 7 cheat dialogs (hub + 6 category sub-dialogs) collapse into a **single tabbed hub renderer** `renderCheatsHub`:
- 6 tabs: Fun / Gameplay / Jo Solo Weapons / Classic Weapons / Weapons / Buddies
- Each tab is a scrollable list of checkbox rows (radio behaviour for Buddies)
- Docked action bar: `Turn Off All` / `Unlock All...` / `Back`
- `Unlock All...` → opens inline Yes/No confirm modal (reuses `renderCheatsConfirmUnlock`)

Registration policy:
- `g_CheatsMenuDialog` → `renderCheatsHub` (no initial tab override, keeps last-used tab)
- `g_CheatsFunMenuDialog` → `renderCheatsSubRedirect` (pops self, sets `s_CheatsTab = TAB_FUN`). Safety net: if any legacy path still pushes the sub-dialog directly, we pop it back to the hub and switch the tab. Same pattern as Batch 3 `renderCiSettingsRedirect`.
- `g_CheatsGameplayMenuDialog` → redirect → TAB_GAMEPLAY
- `g_CheatsSoloWeaponsMenuDialog` → redirect → TAB_SOLO_WEAPONS
- `g_CheatsClassicWeaponsMenuDialog` → redirect → TAB_CLASSIC_WEAPONS
- `g_CheatsWeaponsMenuDialog` → redirect → TAB_WEAPONS
- `g_CheatsBuddiesMenuDialog` → redirect → TAB_BUDDIES
- `g_CheatsWarningMenuDialog` → `renderCheatsWarning` (dedicated, SUCCESS modal)
- `g_CheatsConfirmUnlockMenuDialog` → `renderCheatsConfirmUnlock` (dedicated, DANGER modal)

**Note on dialog handlers**: `cheatMenuHandleDialog` (cheats.c:405) is fired automatically by the menu runtime on OPEN/CLOSE for any of the 7 cheat dialogs whether hot-swapped or not. This means `func0f14a52c()` (HUD darken flag) and the piracy-check fingerprint both still fire. Preserved without duplicating.

### Cinema consolidation

`g_CinemaMenuDialog` → `renderCinemaList` (lives in `pdgui_menu_mainmenu.cpp` per plan).

Reuses the legacy `menuhandlerCinema` through the s194 call-through pattern:
- MENUOP_GETOPTIONCOUNT / MENUOP_GETOPTIONTEXT / MENUOP_GETOPTGROUPCOUNT / MENUOP_GETOPTGROUPTEXT / MENUOP_GETGROUPSTARTINDEX → populate the grouped list
- MENUOP_SET → delegate play-trigger to handler with `data->list.value` (handler itself sets `g_Vars.autocutgroupcur`/`left`, calls `menuPopDialog`/`menuStop`)

This way we **do not duplicate** the getNumCompletedMissions / g_CutsceneCountsByMission logic — we call the same handler the legacy menu list would.

## Legacy → new-system function map

### cheats.c symbols

| Legacy symbol | Used by new system? | Where / How | Notes |
|---------------|--------------------|-------------|-------|
| `cheatIsUnlocked(cheat_id)` | YES | Called by `renderCheatsHub` to determine checkbox row label ("----------" vs cheat name) and locked-row styling | Same as legacy `cheatGetNameIfUnlocked` |
| `cheatCheckboxMenuHandler` | YES | Called via s194 pattern with `item->param = CHEAT_ID` for GET/SET | Handles Marquis/EnemyRockets mutual exclusion automatically — zero extra logic needed |
| `cheatMenuHandleBuddyCheckbox` | YES | Called via s194 pattern for each buddy row | Handles Velvet (param=0) special case and mutual-exclusion |
| `cheatMenuHandleTurnOffAllCheats` | YES | Called via s194 pattern on `Turn Off All` action-bar button | Handler resets both banks |
| `menuhandlerUnlockEverything` | YES | Called via s194 pattern on confirm `Yes` | Handler calls `gamefileUnlockEverything()` |
| `cheatMenuHandleDialog` | YES (auto) | Fires via menu runtime on OPEN/CLOSE | Preserves `func0f14a52c`/`func0f14a560` HUD flag + piracy check + GAMEFILEFLAG_USED_TRANSFERPAK |
| `cheatGetNameIfUnlocked` | NO (not needed) | We inline the (cheatIsUnlocked ? langGet(name) : "----------") pattern | Takes `menuitem*` which we don't have; inlining is cleaner |
| `cheatGetMarquee` | NO (not needed) | Marquee only rendered on N64. Replaced by locked-row styling + optional hover tooltip | Complex version-specific code with 4 copies (NTSC/PAL/JPN/original). Covered by hover tooltip showing "Unlock in Stage X on Diff Y" for locked rows. **Note: full localized text retained via langSafe(L_MPWEAPONS_137..141) when building the tooltip.** |
| `g_CheatsEnabledBank0/1` | Indirect | Only touched via `cheatCheckboxMenuHandler` GET/SET — we never read/write banks directly | Keeps bank logic single-owner |
| `g_CheatsActiveBank0/1` | NO | Only meaningful mid-level (set by `cheatsReset()`). Menu state is the "enabled" banks. | N/A at menu time |
| `g_Cheats[]` (cheat metadata table) | YES | Read via `cheatIsUnlocked()`; also for tooltip unlock-requirement text via `g_Cheats[id].stage_index`, `.time`, `.difficulty`, `.flags`, `.nametextid` | Accessed through a C-side read-only accessor or via existing struct extern |
| `cheatGetByTimedStageIndex` / `cheatGetByCompletedStageIndex` / `cheatGetTime` / `cheatGetName` | NO | Used only from end-screen reward code, not menu | Out of batch scope |
| `g_CheatsWarningMenuItems[]` | NO (content preserved in new renderer) | Warning text `L_MPMENU_479` rendered directly in `renderCheatsWarning` with OK/Cancel action-bar buttons | Content parity, not literal item parity |
| `g_CheatsConfirmUnlockMenuItems[]` | NO (content preserved in new renderer) | "Are you sure?" body + "Unlocks all cheats, weapons..." marquee both drawn directly in `renderCheatsConfirmUnlock` with Yes/No action-bar buttons | Content parity |
| `g_CheatsBuddiesMenuDialog` forward decl @ cheats.c:24 | Preserved | Still needed because `cheatGetMarquee` references it; leave in place | Unaffected |

### mainmenu.c cinema symbols

| Legacy symbol | Used by new system? | Where / How | Notes |
|---------------|--------------------|-------------|-------|
| `menuhandlerCinema` | YES | Called via s194 pattern for all MENUOP_* operations | Single source of truth for group math |
| `getNumCompletedMissions` | NO (indirect) | Called by `menuhandlerCinema` internally | Don't duplicate |
| `g_Cutscenes[]` / `g_CutsceneCountsByMission[]` | Indirect | Read only through `menuhandlerCinema` | Single-owner |
| `g_Vars.autocutgroupcur` / `autocutgroupleft` | Indirect | Written only through `menuhandlerCinema` SET path | No direct g_Vars access from C++ |
| `cutscenegroup groups[]` (local to handler) | Indirect | Used internally by the handler to answer MENUOP_GETOPTGROUPCOUNT/TEXT/GETGROUPSTARTINDEX | Don't duplicate |

### What is NOT covered (flagged)

Nothing load-bearing is dropped. Two items flagged for note:

1. **Marquee ticker animation**: the legacy menu shows an animated horizontal-scroll marquee for locked cheats with full localized strings (`L_MPWEAPONS_136..143`, `L_OPTIONS_251`). Our replacement uses a static hover tooltip with the same content. Visual animation is lost; informational content is preserved. Acceptable per Batch 3 precedent ("content absorbed, UX modernized").
2. **Tri-state `cheatIsUnlocked()` return value**: legacy `cheatIsUnlocked` returns 0/1/2 (2 = both unlocked via completion AND transfer pak). Our checkbox display is binary. The `2` case only affects display ordering in the legacy screen, not the actual enabled state. No behavioural loss.

## Implementation notes

- File: `port/fast3d/pdgui_menu_cheats.cpp` (new), discovered by GLOB_RECURSE
- Registration function: `pdguiMenuCheatsRegister(void)` added to `port/include/pdgui_menus.h`, called from `pdguiMenusRegisterAll()`
- `pdgui_menu_mainmenu.cpp`: adds cinema renderer + one registration line in `pdguiMenuMainMenuRegister()`
- `CLAUDE.md` constraint: pdgui_menu_solomission.cpp is **UNTOUCHED** (reserved per CRITICAL COLLISION RULE)
- types.h rule: `#define bool s32` -- use `s32` for menu-handler bool returns; bool-typed ImGui parameters remain `bool` (C++ native)

## Build plan

1. Write pdgui_menu_cheats.cpp (new)
2. Edit pdgui_menu_mainmenu.cpp (add `renderCinemaList` + forward decl + register + Cinema extern)
3. Edit pdgui_menus.h (declare + call `pdguiMenuCheatsRegister`)
4. Full link headless build of both PerfectDark.exe and PerfectDarkServer.exe with TEMP=/tmp
5. Verify line counts post-merge
6. Merge worktree to dev

## Post-build results

_(to be filled in after build)_

### Line counts

| File | Before | After | Delta |
|------|--------|-------|-------|
| pdgui_menu_cheats.cpp (NEW) | 0 | _TBD_ | _TBD_ |
| pdgui_menu_mainmenu.cpp | 3123 | _TBD_ | _TBD_ |
| pdgui_menus.h | 62 | _TBD_ | _TBD_ |

### Exe sizes + link times

| Target | Size | Freshly linked |
|--------|------|----------------|
| PerfectDark.exe | _TBD_ | _TBD_ |
| PerfectDarkServer.exe | _TBD_ | _TBD_ |

## Zero-function-loss audit

_(to be filled in after code written)_
