{
  "scenario_name": "main_menu_joanna_litdiag",
  "description": "B-936 lighting-fix diagnosis: only Joanna, real textures, room hidden, with --debug-chr-light which logs (in gfx_sp_vertex) whether G_LIGHTING fired for her vertices and what scene lights are active (num_lights, ambient) + the resulting lit colour. Tells us if the mcount=3 gate kept lighting on AND whether lights are actually set up in the held-menu world render.",
  "boot_args": ["--debug-generated-mesh-render-audit", "--debug-hide-menu", "--debug-track-chr", "--debug-show-only-mesh", "combat", "--debug-hide-scene", "--debug-chr-light"],
  "timeout_seconds": 100,
  "install_state": "clean",
  "input_sequence": [
    { "at_ms": 0,     "type": "wait", "comment": "boot; CHRLIGHT fires at first render ~30s" },
    { "at_ms": 60000, "type": "exit", "comment": "done" }
  ],
  "screenshots": [
    { "at_ms": 50000, "name": "lit_a" }
  ],
  "assertions": {
    "required_lines": ["SMOKE: scenario=main_menu_joanna_litdiag", "SMOKE: result=scripted_exit"],
    "forbidden_patterns": [],
    "required_counts": []
  }
}
