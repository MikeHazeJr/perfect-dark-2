{
  "scenario_name": "joanna_romcamlook",
  "description": "ROM-CONTROL leg comparison: Joanna's body rendered from the ORIGINAL ROM path (--debug-rom-chrbody) framed with the same camlook camera (--debug-cam-look-chr --debug-show-only-mesh) as main_menu_joanna_camlook. Lets the migrated camlook render be compared 1:1 against the reference for leg-mesh + proportions.",
  "boot_args": ["--debug-generated-mesh-render-audit", "--debug-hide-menu", "--debug-cam-look-chr", "--debug-show-only-mesh", "--debug-rom-chrbody", "combat"],
  "timeout_seconds": 200,
  "install_state": "current",
  "input_sequence": [
    { "at_ms": 0,      "type": "wait", "comment": "reuse the clean install from camlook" },
    { "at_ms": 100000, "type": "exit", "comment": "done" }
  ],
  "screenshots": [
    { "at_ms": 60000, "name": "romcamlook_a" },
    { "at_ms": 75000, "name": "romcamlook_b" },
    { "at_ms": 90000, "name": "romcamlook_c" }
  ],
  "assertions": {
    "required_lines": [
      "SMOKE: scenario=joanna_romcamlook",
      "SMOKE: result=scripted_exit"
    ],
    "forbidden_patterns": ["SMOKE: result=timeout"],
    "required_counts": []
  }
}
