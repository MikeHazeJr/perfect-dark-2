#ifndef ASSET_AUDIO_IDENTITY_HARNESS_H
#define ASSET_AUDIO_IDENTITY_HARNESS_H

/* Installed-client smoke; exits immediately after the caller records results. */
int assetAudioIdentityHarnessRun(void);

#endif
