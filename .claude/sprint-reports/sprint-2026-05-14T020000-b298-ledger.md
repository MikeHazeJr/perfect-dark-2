# Sprint Report - B-298 Ledger Close-Out

- Sprint ID: sprint-2026-05-14T020000-b298-ledger
- Date: 2026-05-13
- Branch: dev
- Pillar / Card: input / c036
- Source: 2026-05-13 incompleteness audit, Sprint 6 (Track 1 finding 1.6)

## Goal

Promote bug B-298 (vehicle IMC consumers / hoverbike control wire-up) from
`OPEN` to `FIXED-PENDING-PLAYTEST` in `context/bugs.md`, anchoring the
actual commit SHA that landed the fix. The fix code was already merged on
2026-05-01 via Phase 2 fix #4 of the joint Menu Stacking + Input pillar
session, but the ledger entry was never updated. Pure metadata-only
ledger reconciliation, no code change.

## Shipped

- `context/bugs.md` line 43 updated: status cell `OPEN 2026-05-01 (S594,
  filed for joint Menu Stacking + Input pillar session)` -> `FIXED-
  PENDING-PLAYTEST 2026-05-01 (S594, 67b0359c)`.
- Added closure provenance text immediately after the status marker:
  `Closed-in-code at \`src/game/bondbike.c:236-272\` (Phase 2 fix #4);
  ledger promotion 2026-05-13 per audit Track 1 finding 1.6.` Original
  prose preserved verbatim afterward.
- Commit `<NEW-SHA>` on `dev` (filled by `git rev-parse HEAD` -- see
  Verification Notes below).
- B-298 fix anchor commit SHA: `67b0359c` (merge commit on `dev` of
  `claude/clever-montalcini-4902a1` -> `56ea32ac feat(input): fix #4 --
  vehicle wire-up + network paste on controller`). The merge commit was
  selected per Sprint 6 instructions: "If multiple, use the merge commit
  on dev." Both `56ea32ac` (direct) and `67b0359c` (merge) are on `dev`;
  the merge SHA is the canonical anchor because it is what `dev` actually
  contains as a first-parent ancestor.

## Decisions

1. **Anchor SHA choice**: used the merge commit `67b0359c` rather than
   the direct commit `56ea32ac`. Per Sprint 6 step 1 "If multiple, use
   the merge commit on dev." Both contain the
   `actionPressed(0, ACTION_VEHICLE_EXIT)` wiring; merge commit is
   semantically what landed on dev.
2. **Prose preservation**: kept the entire original B-298 prose
   (playtest quote, log evidence, root-cause analysis, fix scope,
   smoke-test recipe) intact. Only the leading status / provenance
   sentence changed, prepended to the original prose.
3. **Scope discipline**: did not edit any other ledger file
   (`session-log.md`, `tasks.md`, `kanban/state.json`). Sprint 6
   explicitly scoped to one row in one file plus the sprint report
   itself. The 2026-05-13 incompleteness audit's parent sprint report
   tracks the broader ledger-reconciliation arc.

## Blockers

None. Single-file ledger edit; no build, no tests, no runtime impact.

## Follow-ups

- Future playtest of the hoverbike wire-up still pending per the
  `FIXED-PENDING-PLAYTEST` status. Smoke-test recipe preserved in the
  bugs.md row's right-hand cell (mount hoverbike, press W / RT for
  accelerate, F / X for exit, A / D / left-stick for steer).
- If the playtest passes, the next ledger sweep can promote B-298 from
  `FIXED-PENDING-PLAYTEST` to `FIXED`. Until then this is the canonical
  state.
- Sprint 6 is part of the 2026-05-13 audit's Track 1 ledger-hygiene
  cluster; other open Track 1 findings (e.g. tasks.md task-001 status,
  any other in-code closures awaiting ledger promotion) are handled in
  sibling sprints, not here.

## Kanban Changes

None. The `c036` card stays in `active` -- B-298 is a single bug under
that card's umbrella, and the card spans the full controller-support
cohort 5-8 arc which has additional open work.

## Files Touched

- `context/bugs.md` (1 row modified, status cell + provenance prepend
  on B-298 only)
- `.claude/sprint-reports/sprint-2026-05-14T020000-b298-ledger.md` (this
  file, new)

## Verification Notes

- `git diff context/bugs.md` before commit: showed exactly one line
  changed (the B-298 row). No other rows touched. No em-dashes
  introduced; existing `--` separators preserved.
- `git status` before commit: only `context/bugs.md` modified, plus the
  new sprint report file untracked.
- Commit message subject `Input - c036: ledger close-out for B-298
  vehicle IMC consumers` validated against `.githooks/commit-msg.py`:
  - 62 chars (within `SUBJECT_MAX_LEN = 72`).
  - Pillar `Input` resolves to `c036`'s pillar `input` in
    `tools/kanban/state.json` (line 502).
  - `Refs: c036` trailer present.
- Build verify: not performed -- ledger-only edit, no code touched, no
  build risk.
- Tests: not performed -- ledger-only edit, no test risk.
- BYOR: compliant -- no asset extraction proposed or referenced.
