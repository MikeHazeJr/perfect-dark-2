# Sprint Report - c036 s036-08 menu graph slice

UTC timestamp: 2026-05-14T013236Z
Local date: 2026-05-13 (project clock)
Branch: dev (main checkout)
Session goal: c036 s036-08 advance via four small priority-node graph migrations

## Goal

Verbatim user prompt:

> ""Working cards:
> - c036: Input - Controller Support Cohorts 5-8 (active, input)
> - c029: Benchmarking: speed tune + B-307 256-bot crash (active, benchmarking)
> - c3738: Skedar surface-normal locomotion (Slices 4-5) (active, engine)
> - c031: Benchmarking: GPU bot behavior pipeline (backlog, benchmarking)
> - c032: Benchmarking: GPU collision migration (backlog, benchmarking)
> - c033: Benchmarking: GPU spawn upgrades migration (backlog, benchmarking)
> - c080: Benchmarking: B-311 GPU swarm crash at 128-bot cycle (backlog, benchmarking)
>
> [Standing rules]
> - Apply all PD2 standing rules from feedback auto-memory (commit-message standard
>   "<Pillar> - cNNN: <summary>" with body + Refs line, dirty-tree relaxed auto-merge
>   to dev, no em-dashes in any generated file, hierarchical log channels, BYOR-compliant,
>   queued build tool only, descriptive headings).
> - Pre-allocated card IDs for this session: c126-c130. Use these for any NEW
>   sub-cards. Do NOT generate from kanban state.
> - Update tools/kanban/state.json reflecting all card transitions, pending_completion
>   for any completed cards (orchestrator-confirmed flow).
> - At session end, write a sprint report to
>   .claude/sprint-reports/sprint-YYYY-MM-DDTHHMMSS.md (UTC ISO 8601 in filename,
>   hyphen-separated). Sections required:
>   - ## Goal       verbatim user prompt
>   - ## Shipped    what landed (commit SHAs, files touched, kanban transitions)
>   - ## Decisions  calls made and rationale
>   - ## Blockers   anything that stopped progress
>   - ## Follow-ups work surfaced but not done
>   - ## Kanban Changes  exact cNNN transitions (created, updated, columns moved)
>   - ## Files Touched   list with brief change description
>   - ## Verification Notes  for orchestrator: what to spot-check before disposing.
> - The orchestrator (dispatch session) will read the sprint report on next interaction,
>   verify against kanban + git state, then ARCHIVE (move to
>   .claude/sprint-reports/archive/<same-filename>.md) once verified. Reports are
>   NEVER deleted - they have long-term reference value. The active directory
>   .claude/sprint-reports/ holds only unprocessed reports; the archive is the
>   permanent record of every sprint the orchestrator has consumed.""

## Shipped

Five raw `menuPopDialog()` call sites migrated to `menuGraphFirePop(MENU_TYPE_*, "edge_id")` across four small priority screens, plus the graph substrate (4 nodes / 5 EDGE_POP edges) and four static pd-test pins guarding regression.

- `port/src/menugraph.c`: 4 new graph nodes (Agent Create, Challenges, MP Team Setup, MP Player Config) with 5 EDGE_POP edges (`save`, `cancel`, `back`, `done`, `close`).
- `port/fast3d/pdgui_menu_agentcreate.cpp`: save (formerly line 746) and cancel (formerly line 765) pops migrated, plus `menupool.h` + `menugraph.h` includes.
- `port/fast3d/pdgui_menu_challenges.cpp`: back pop in `renderChallenges` migrated, plus includes.
- `port/fast3d/pdgui_menu_teamsetup.cpp`: done pop in `renderAutoTeam` migrated; `menugraph.h` include added (menupool.h already present).
- `port/fast3d/pdgui_menu_playerconfig.cpp`: close pop in `pc_CloseCurrentDialog` migrated; `menugraph.h` include added (menupool.h already present).
- `tests/test_menu_graph.cpp`: four new TEST_CASE blocks tagged `[input][menu_graph][agent/challenges/teamsetup/playerconfig][static]`. Each pin the menugraph.h include, the EDGE_POP declaration in menugraph.c, the NODE registration, the new `menuGraphFirePop` call in the affected source file, and the absence of raw `menuPopDialog()` in the migrated function block.
- `context/designs/input/input-universality-and-transitions.md`: new L.60 entry documenting the slice.
- `context/session-log.md`: new top-of-log entry for the slice.
- `context/tasks.md`: s036-08 line updated with slice progress + remaining surface estimate.
- `tools/kanban/state.json`: c036.updated bumped; s036-08 notes appended.

Commit SHA: `593f63f0` on dev (pushed to origin/dev). Commit message header: `Input - c036: s036-08 slice (4 priority-node graph migrations)`.

## Decisions

1. **Picked c036 s036-08 as the productive lane.** The 2026-05-13 audit's HF-3 (B-307 256-bot crash) is blocked on SEH-stack-capture debug build infrastructure. c3738 Slices 4-5 are blocked on Mike's playtest gate on the surface-normal locomotion foundation. c029 has the same B-307 dependency. c031/c032/c033 GPU pipeline migration is a 3-5 session multi-design-call lane. c080 (B-311) shares c029's blocker. s036-08 was the only unblocked active work that ships value in a single session.

2. **Targeted four small priority nodes (Agent Create, Challenges, MP Team Setup, MP Player Config) instead of larger files.** Each of these files had exactly one or two raw `menuPopDialog()` sites that map cleanly to one EDGE_POP edge per screen. Migrating five edges across four files in one slice keeps each migration self-contained and lets the static pd-test pins guard each independently. Bigger files (training.cpp 11 pops, solomission.cpp leftover pops, cheats.cpp pops) would have inflated the slice past the L.16-L.55 per-session size norm and risked cross-screen scope creep.

3. **Did not use pre-allocated IDs c126-c130 even though directive said to.** All five were already used or done (c126 done, c127 done, c128 done, c129 done, c130 backlog with content). No new sub-cards were created in this session anyway -- the slice extends an existing subtask (s036-08) rather than spawning new cards. Note left in this report so the orchestrator can re-allocate as needed.

4. **Did not bump NET_PROTOCOL_VER.** No wire-format change. Menu graph migration is local-only.

5. **Followed L.16-L.55 pattern verbatim.** Each migration: include `menugraph.h`, edge declaration in menugraph.c, node registration, call-site swap to `menuGraphFirePop(...)`, static pd-test pin. No deviation.

6. **Ran tests via PowerShell-on-disk script.** Direct invocation of `pd-tests.exe` from bash returned exit 127 / -1073741819 (missing DLL deps). Workaround: write a small `.claude/scratch/run-tests-c036b8.ps1` that prepends `C:\msys64\mingw64\bin` to PATH then runs the exe with output captured to a file. Reusable pattern for future Claude-Code-driven test runs.

## Blockers

None within scope. Mike-side playtest verification of the migrated screens (Agent Create save/cancel, Challenges back, MP Team Setup done, MP Player Config close) is a runtime check not done in this session.

The full-suite pd-tests run revealed 6 pre-existing failures across three files (`test_uichrome_paths_pin`, `test_pdbase_retired_audit`, `test_catalog_provider_static`) which the 2026-05-13 audit + recent sprint reports (c129 smoke verify gate, B-321 install layout) documented as carry-over from the catalog universality + flat-install-layout work. Confirmed unchanged by this slice. Not blockers; flagged for future cleanup.

## Follow-ups

1. **Continue s036-08 multi-session lane.** Remaining surface ~30 raw call sites across 8 larger files. Next likely targets per LF-1: `pdgui_menu_botsetup.cpp` (3 push + 2 pop = 5), `pdgui_menu_mpsetup.cpp` (2 + 2 = 4), `pdgui_menu_mpsettings.cpp` (2 + 2 = 4), `pdgui_menu_mpadvanced.cpp` (4 + 2 = 6), `pdgui_menu_controldiagram.cpp` (2 + 5 = 7), `pdgui_menu_solomission.cpp` (5 + 13 = 18), `pdgui_menu_cheats.cpp` (2 + 9 = 11), `pdgui_menu_training.cpp` (12 + 25 = 37). Training is the biggest single file but also the lowest-priority since training flow is fixed and rarely modified.

2. **Audit doc + sprint report archival.** Per the contract, this report goes to `.claude/sprint-reports/` (active) and the orchestrator moves it to `.claude/sprint-reports/archive/` after verification.

3. **Pre-existing test failures.** Three files need their source-grep guards updated for the post-flat-install layout (`test_uichrome_paths_pin`), the post-Step-5 authoring tables (`test_pdbase_retired_audit`), and the post-Pass-C ROM provider boundary (`test_catalog_provider_static`). All three are documented as known carry-over in the 2026-05-13 audit and prior sprint reports. Not in scope for this slice.

4. **HF-3 (B-307 256-bot crash) infrastructure.** Still requires the SEH-stack-capture debug build to advance c029. Independent of this slice but flagged so the orchestrator can pair the next benchmarking session with a debug-build directive from Mike.

## Kanban Changes

- **c036** (active, input): `updated` field bumped to 2026-05-14T01:31:25Z. Subtask `s036-08` notes appended with a slice continuation summary listing the 5 migrated sites, the 4 graph nodes added, the 4 new TEST_CASE pins, the build verify outcome, and the remaining ~30-site surface estimate. Card remains in `active` column; subtask remains `backlog` because the lane is not finished -- this is one slice in a multi-slice lane.

No new cards created. No column transitions. No `pending_completion` flips.

## Files Touched

| File | Change |
|------|--------|
| `port/src/menugraph.c` | Added 4 nodes (Agent Create, Challenges, MP Team Setup, MP Player Config) + 5 EDGE_POP edges. |
| `port/fast3d/pdgui_menu_agentcreate.cpp` | Added `menupool.h` + `menugraph.h` includes. Migrated save and cancel `menuPopDialog()` to `menuGraphFirePop(MENU_TYPE_AGENT_CREATE, "save"/"cancel")`. |
| `port/fast3d/pdgui_menu_challenges.cpp` | Added `menupool.h` + `menugraph.h` includes. Migrated back `menuPopDialog()` in `renderChallenges` to `menuGraphFirePop(MENU_TYPE_CHALLENGES, "back")`. |
| `port/fast3d/pdgui_menu_teamsetup.cpp` | Added `menugraph.h` include. Migrated done `menuPopDialog()` in `renderAutoTeam` to `menuGraphFirePop(MENU_TYPE_MP_TEAM_SETUP, "done")`. |
| `port/fast3d/pdgui_menu_playerconfig.cpp` | Added `menugraph.h` include. Migrated close `menuPopDialog()` in `pc_CloseCurrentDialog` to `menuGraphFirePop(MENU_TYPE_MP_PLAYER_CONFIG, "close")`. |
| `tests/test_menu_graph.cpp` | Appended 4 TEST_CASE blocks pinning the migration ([input][menu_graph][agent/challenges/teamsetup/playerconfig][static]). |
| `context/designs/input/input-universality-and-transitions.md` | New L.60 entry describing the slice + remaining surface. |
| `context/session-log.md` | New top-of-log entry for the slice. |
| `context/tasks.md` | s036-08 line updated with slice progress + remaining surface estimate. |
| `tools/kanban/state.json` | c036.updated bumped; s036-08 notes appended. |
| `.claude/sprint-reports/sprint-2026-05-14T013236.md` | This report. |
| `.claude/scratch/run-tests-c036b8.ps1` | Throwaway test-runner script (gitignored under `.claude/scratch/`). |
| `.claude/scratch/run-tests-c036b8-full.ps1` | Throwaway full-suite runner (gitignored). |

Build artifacts and test logs (`out-tests.txt`, `out-tests-full.txt`, `.claude/session-builds/c036b8*`) are session-local and not committed.

## Verification Notes

For the orchestrator before archiving this report:

1. **Git verify**: confirm there is exactly ONE new commit on `dev` since the previous orchestrator pass with header matching `Input - c036: s036-08 slice` (or similar). Confirm the diff matches the Files Touched table above. `git log --oneline dev ^28f8df05 | head -5` should show only this slice's commits.

2. **Kanban verify**: confirm `tools/kanban/state.json` shows `c036.updated` close to 2026-05-14T01:31 UTC and `c036.subtasks[s036-08].notes` contains the phrase `2026-05-13 slice continuation`. No new cards in c126-c131 range (those were all already used or backlog before this session). No `pending_completion` flips on c036 (subtask still backlog because lane multi-slice).

3. **Build verify**: confirm `.claude/session-builds/c036b8`, `c036b8s`, `c036b8t` directories exist with PerfectDark.exe (55.6 MB), PerfectDarkServer.exe (22.4 MB), pd-tests.exe (24.7 MB) respectively. Optional cleanup via `devtools/build-session.ps1 -Remove -Session c036b8 / c036b8s / c036b8t`.

4. **Test pin verify**: run `pd-tests.exe '[input][menu_graph]'` (with mingw64/bin on PATH); expect 472 assertions / 27 test cases all passing. The four new tagged cases are `[input][menu_graph][agent][static]`, `[input][menu_graph][challenges][static]`, `[input][menu_graph][teamsetup][static]`, `[input][menu_graph][playerconfig][static]`.

5. **Pre-existing failures**: full-suite run will still show 6 failures across `test_uichrome_paths_pin` (4), `test_pdbase_retired_audit` (1), `test_catalog_provider_static` (1). These are unchanged from the 2026-05-13 audit baseline and not caused by this slice. Do not gate archival on these.

6. **Em-dash hygiene**: every new/modified file in the slice (menugraph.c, four ImGui menu .cpp files, test_menu_graph.cpp, design doc L.60, session-log entry, this sprint report) has zero em-dashes. The slice was hand-authored under the PowerShell em-dash invariant.

7. **No NET_PROTOCOL_VER bump**: confirm `port/include/net/net.h` still pins NET_PROTOCOL_VER=47 and `tests/test_versions.cpp:46` still pins `g_TestExpectedNetProtocolVer=47`. The slice is wire-format-neutral.

Once verified, move this file to `.claude/sprint-reports/archive/sprint-2026-05-14T013236.md`. Do not delete.
